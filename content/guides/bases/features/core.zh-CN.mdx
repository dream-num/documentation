---
title: 核心功能
icon: 'Cpu'
---

<MetaData
  lang="zh-CN"
  isPro
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/bases',
        locale: '@univerjs-pro/bases/locale/zh-CN',
        facade: '@univerjs-pro/bases/facade',
      },
      {
        client: '@univerjs-pro/bases-ui',
        locale: '@univerjs-pro/bases-ui/locale/zh-CN',
        style: '@univerjs-pro/bases-ui/lib/index.css',
        facade: '@univerjs-pro/bases-ui/facade',
      },
    ],
    server: false,
  }}
/>

Univer Bases 核心功能将结构化数据模型与浏览器工作台组合在一起。模型和 UI 由两个 Univer Pro 包提供，并作为独立的 `UNIVER_BASE` 单元运行。

## 功能

- **表、字段和记录**：在每张表中存储带类型的字段和有序记录。
- **多种视图**：通过网格、看板、日历、甘特图和画廊视图呈现同一组记录。
- **命令驱动的编辑**：让模型变更统一经过命令层，并提供权限及可选历史记录和协同集成所需的挂钩。
- **公式字段**：将 Base 字段接入 Univer 公式计算链路。
- **浏览器工作台**：渲染视图，并提供选择、编辑、剪贴板、滚动和面板功能。
- **持久化**：无需直接修改运行中的快照，即可创建、恢复和保存 `IBaseSnapshot`。

## 预设模式

Univer Bases 目前没有专用预设。模型和浏览器 UI 都应使用插件模式。

## 插件模式

### 安装

在[安装与基础使用](/guides/bases/getting-started/installation)介绍的 Univer 运行时中加入两个 Bases 产品包：

```package-install
npm install @univerjs-pro/bases @univerjs-pro/bases-ui
```

### 注册产品插件

按照安装指南先注册共享运行时、公式引擎和 Pro 许可证，再注册 Bases 插件：

```ts
import { UniverBasesPlugin } from '@univerjs-pro/bases'
import { UniverBasesUIPlugin } from '@univerjs-pro/bases-ui'

import '@univerjs-pro/bases-ui/lib/index.css'

univer.registerPlugin(UniverBasesPlugin)
univer.registerPlugin(UniverBasesUIPlugin)
```

`UniverBasesPlugin` 注册 Base 模型、命令、字段与视图注册表、公式集成和权限。`UniverBasesUIPlugin` 添加渲染工作台与浏览器交互。

## Facade API

创建 `FUniver` 前，先导入模型和 UI 的 Facade 入口：

```ts
import { FUniver } from '@univerjs/core/facade'

import '@univerjs-pro/bases/facade'
import '@univerjs-pro/bases-ui/facade'

const univerAPI = FUniver.newAPI(univer)
const base = univerAPI.createBase({ name: '产品跟踪' })
const snapshot = base.save()
```

模型 Facade 提供 `createBase`、`getActiveBase`、`getBase`，以及对 Base、表、字段、记录和视图的操作。UI Facade 则添加激活、选择、滚动、编辑和面板 API。接下来可阅读 [Bases Facade API 指南](/guides/bases/getting-started/facade)和 [Base 快照](/guides/bases/model/base-snapshot)。
