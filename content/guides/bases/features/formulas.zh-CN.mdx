---
title: 公式
icon: '#pro/Sigma'
---

Bases 公式字段使用 Univer 公式引擎，根据表数据计算记录值。当 Base 包含公式字段，或 UI 需要显示公式计算进度时，请注册公式依赖项。

## 注册公式支持

```ts
import { UniverFormulaEnginePlugin } from '@univerjs/engine-formula'
import { UniverSheetsFormulaPlugin } from '@univerjs/sheets-formula'

univer.registerPlugin(UniverFormulaEnginePlugin)
univer.registerPlugin(UniverSheetsFormulaPlugin)
univer.registerPlugin(UniverBasesPlugin)
univer.registerPlugin(UniverBasesUIPlugin)
```

## 创建公式字段

```ts
const total = table.addField({
  name: 'Total',
  type: univerAPI.Enum.BaseFieldTypeEnum.Formula,
  config: {
    formula: '{Price} * {Quantity}',
  },
})
```

## 注意事项

- 公式字段的值由计算得出；请避免从应用代码直接写入公式单元格。
- 公式表达式应使用当前 Bases 公式解析器所要求的语法引用字段。
- 导入大型快照后，请先重新计算公式，再向用户显示结果。
