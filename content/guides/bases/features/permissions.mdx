---
title: Permissions
icon: '#pro/ShieldCheck'
---

The current Bases packages do not export a public permission Facade. The former `base.getPermission()` and its Base, table, field, and record permission methods have been removed.

Use server-side authorization as the source of truth. Bases UI may consume internal permission state to show or disable actions, but application code should not depend on those internal services.
