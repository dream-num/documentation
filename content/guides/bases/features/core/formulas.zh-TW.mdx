---
title: 公式
---

Bases 公式欄位會根據資料表中的資料計算記錄值。`UniverBasesPlugin` 會自動註冊 Pro 公式引擎相依項目。

## 註冊公式支援

```ts
import { UniverBasesPlugin } from '@univerjs-pro/bases'
import { UniverBasesUIPlugin } from '@univerjs-pro/bases-ui'

univer.registerPlugin(UniverBasesPlugin)
univer.registerPlugin(UniverBasesUIPlugin)
```

## 建立公式欄位

```ts
const formulaTableName = table.getFormulaName()
const total = table.addField('Total', univerAPI.Enum.BaseFieldType.Formula, {
  field: {
    config: {
      formula: `=SUM(${formulaTableName}[[#This Row],[Price]],${formulaTableName}[[#This Row],[Tax]])`,
    },
  },
  externalReferences: [],
})
```

每次寫入公式欄位都必須提供 `externalReferences`。只有公式僅參照目前 Base 的欄位時才能使用 `[]`。跨 Unit 公式應使用 [`buildReference`](/reference/facade/formula#buildreference)，並在此陣列中傳入所有穩定的來源繫結。

## 注意事項

- 公式欄位是衍生值；請避免透過應用程式程式碼直接寫入公式儲存格。
- 公式運算式使用 `Table[[#This Row],[Price]]` 這類結構化欄位參照。
- 匯入大型快照後，請先重新計算公式，再向使用者顯示結果。
