---
title: 빠른 시작
icon: '#pro/Rocket'
---

## Bases 등록

아래 코드 조각은 브라우저 편집기에 필요한 Bases 관련 설정을 보여줍니다. 공유 Univer 런타임, Pro 라이선스, 렌더링/UI 플러그인, 수식 종속성을 등록한 다음 Bases 모델 및 UI 플러그인을 등록합니다.

`@univerjs-pro/bases`는 `UNIVER_BASE` 단위를 만듭니다. `@univerjs-pro/bases-ui`는 독립형 데이터베이스 워크벤치를 렌더링합니다.

```ts
import { UniverBasesPlugin } from '@univerjs-pro/bases'
import { UniverBasesUIPlugin } from '@univerjs-pro/bases-ui'
import BasesUIEnUS from '@univerjs-pro/bases-ui/locale/en-US'
import BasesEnUS from '@univerjs-pro/bases/locale/en-US'
import { UniverLicensePlugin } from '@univerjs-pro/license'
import { LocaleType, mergeLocales, Univer, UniverInstanceType } from '@univerjs/core'
import { UniverFormulaEnginePlugin } from '@univerjs/engine-formula'
import { UniverRenderEnginePlugin } from '@univerjs/engine-render'
import { UniverSheetsFormulaPlugin } from '@univerjs/sheets-formula'
import { UniverUIPlugin } from '@univerjs/ui'

import '@univerjs/design/lib/index.css'
import '@univerjs/ui/lib/index.css'
import '@univerjs-pro/bases-ui/lib/index.css'

import '@univerjs-pro/bases/facade'
import '@univerjs-pro/bases-ui/facade'

const univer = new Univer({
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: mergeLocales(BasesEnUS, BasesUIEnUS),
  },
})

univer.registerPlugin(UniverRenderEnginePlugin)
univer.registerPlugin(UniverUIPlugin, {
  container: 'app',
})

univer.registerPlugin(UniverFormulaEnginePlugin)
univer.registerPlugin(UniverSheetsFormulaPlugin)

univer.registerPlugin(UniverLicensePlugin, {
  license: process.env.CLIENT_LICENSE_TEXT,
})

univer.registerPlugin(UniverBasesPlugin)
univer.registerPlugin(UniverBasesUIPlugin)

univer.createUnit(UniverInstanceType.UNIVER_BASE, {})
```

## 최소 설정

더 작은 빌드가 필요하다면 모델 및 UI 패키지만으로 시작하세요.

```ts
univer.registerPlugin(UniverBasesPlugin)
univer.registerPlugin(UniverBasesUIPlugin)
univer.createUnit(UniverInstanceType.UNIVER_BASE, {})
```

테이블에 수식 필드가 있거나 UI에 수식 계산 상태가 필요할 때는 수식 플러그인을 등록하세요.

## 서버 측 설정

Node.js에서는 모델 패키지를 등록하고 브라우저 UI 플러그인은 생략합니다.

```ts
univer.registerPlugin(UniverBasesPlugin)
```

이 패턴은 스냅샷 처리, 서버 측 검증, 협업 서비스에 유용합니다.
