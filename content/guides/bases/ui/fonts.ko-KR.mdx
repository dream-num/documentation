---
title: 글꼴
icon: '#pro/BookType'
---


## 글꼴 구조 정의

Univer에서는 `IFontConfig` 인터페이스를 사용하여 글꼴을 정의합니다.

| 속성 | 타입 | 설명 |
| --- | --- | --- |
| `value` | `string` | 글꼴의 고유 식별자이며, 일반적으로 CSS font-family에 사용할 우선값입니다. |
| `label` | `string` | 국제화를 위한 번역 키입니다. Univer 인스턴스를 생성할 때 이 키가 지정되지 않았다면 전달된 값이 그대로 표시됩니다. |
| `category` | `'sans-serif' \| 'serif' \| 'monospace' \| 'display' \| 'handwriting'` | UI에서 그룹화할 때 사용하는 글꼴 카테고리입니다(선택 사항). |

<Callout>
  Univer는 사용자가 설치되지 않은 글꼴을 선택하는 것을 막지 않습니다. 클라이언트에 글꼴이 설치되어 있지 않으면 브라우저가 기본 글꼴로 대체합니다.
</Callout>

{/* <AutoTypeTable
  name="IFontConfig"
  type={`
    export type { IFontConfig } from '@univerjs/ui'
  `}
/> */}

## 사용자 지정 글꼴 사용하기

`UniverSheetsCorePreset`(프리셋 모드) 또는 `UniverUIPlugin`(플러그인 모드)의 `customFontFamily` 매개변수를 구성하여 글꼴 드롭다운 메뉴의 옵션을 정의할 수 있습니다.

<Tabs items={['프리셋 모드', '플러그인 모드']}>
  <Tab>
    ```typescript
    const { univerAPI } = createUniver({
      presets: [
        UniverSheetsCorePreset({
          container: 'app',
          customFontFamily: {
            list: [
              {
                value: 'PingFang SC',
                label: '苹方（简）',
                category: 'sans-serif',
              },
              {
                value: 'Helvetica Neue',
                label: 'Helvetica Neue',
                category: 'sans-serif',
              },
            ],
            // Whether to override the default font list, defaults to false
            override: true,
          },
        }),
      ],
    })
    ```
  </Tab>
  <Tab>
    ```typescript
    univer.registerPlugin(UniverUIPlugin, {
      container: 'app',
      customFontFamily: {
        list: [
          {
            value: 'PingFang SC',
            label: '苹方（简）',
            category: 'sans-serif',
          },
          {
            value: 'Helvetica Neue',
            label: 'Helvetica Neue',
            category: 'sans-serif',
          },
        ],
        // Whether to override the default font list, defaults to false
        override: true,
      },
    })
    ```
  </Tab>
</Tabs>

### Facade API

초기화할 때 구성하는 것 외에도 Facade API를 통해 런타임에 새 글꼴을 동적으로 등록할 수 있습니다. 글꼴 리소스를 비동기로 불러와야 하는 경우에 특히 유용합니다.

```typescript
univerAPI.addFonts([{
  value: 'PingFang SC',
  label: 'PingFang SC',
  category: 'sans-serif',
}, {
  value: 'Helvetica Neue',
  label: 'Helvetica Neue',
  category: 'sans-serif',
}])
```


Bases는 테이블 텍스트, 필드 편집기, 메뉴, 패널에 공유 Univer UI 글꼴 파이프라인을 사용합니다. 레이아웃 이동을 방지하려면 Bases를 렌더링하기 전에 호스트 애플리케이션에서 웹 글꼴을 불러오세요.

## 호스트 글꼴 설정

```css
:root {
  font-family: Inter, system-ui, sans-serif;
}
```

## 셀 텍스트

그리드, 칸반, 캘린더, 간트, 갤러리 뷰에 렌더링되는 텍스트는 필드 값과 필드별 포매터에서 가져옵니다. 레코드의 원시 텍스트에 스타일을 적용하지 말고 필드 구성을 사용하여 숫자, 통화, 진행률, 평점, 날짜 형식을 지정하세요.
