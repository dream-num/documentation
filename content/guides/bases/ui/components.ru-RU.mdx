---
title: Компоненты
icon: '#pro/Component'
---

<include cwd>content/guides/shared/ui/components.ru-RU.mdx</include>

Интерфейс Bases состоит из панелей рабочего пространства, представлений, редакторов полей, панелей сведений о записи и служб всплывающих окон. В большинстве приложений достаточно зарегистрировать `UniverBasesUIPlugin`, чтобы использовать стандартные компоненты.

## Настройка видимости рабочего пространства

```ts
univer.registerPlugin(UniverBasesUIPlugin, {
  workbench: {
    header: true,
    headerMenu: true,
    content: true,
    leftSidebar: true,
    rightSidebar: false,
    rightPanelMode: 'overlay',
  },
})
```

## Настройка загрузки вложений

Для работы полей вложений в промышленной эксплуатации приложению необходим обработчик загрузки:

```ts
univer.registerPlugin(UniverBasesUIPlugin, {
  attachment: {
    accept: ['image/png', 'image/jpeg', 'application/pdf'],
    maxSize: 20 * 1024 * 1024,
    async upload(file) {
      const result = await uploadToServer(file)
      return {
        id: result.id,
        name: file.name,
        url: result.url,
        size: file.size,
        mimeType: file.type,
      }
    },
  },
})
```

## Настройка пользователей и групп

Укажите списки пользователей и групп, которые будут доступны в редакторах соответствующих полей.

```ts
univer.registerPlugin(UniverBasesUIPlugin, {
  personOptions: [{ id: 'u1', name: 'Ada Lovelace' }],
  groupOptions: [{ id: 'g1', name: 'Product' }],
})
```
