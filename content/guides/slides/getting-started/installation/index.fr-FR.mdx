---
title: Installation et utilisation de base
icon: 'Blocks'
---

Ce chapitre présente l’installation et l’utilisation de base d’Univer Slides. Ce guide utilise les packages Univer Pro pour Slides. Il n’existe actuellement aucun preset Slides ; créez donc l’application en mode plugin.

Si vous ne connaissez pas encore les concepts fondamentaux d’Univer, commencez par lire le [guide de démarrage rapide](/guides/slides/getting-started/quickstart).

## Créer une application en mode plugin

Univer met Slides à disposition sous la forme d’un ensemble de plugins. Un éditeur destiné au navigateur enregistre généralement l’environnement d’exécution partagé d’Univer, le plugin de licence Pro, les dépendances Docs et Drawing, puis les plugins du modèle et de l’interface utilisateur de Slides.

### Utiliser un gestionnaire de paquets

Si votre projet utilise déjà des outils frontend modernes, nous recommandons Vite, esbuild ou Webpack 5. Les paquets Slides reposent sur des points d’entrée ESM ; les bundlers plus anciens peuvent donc nécessiter une configuration supplémentaire.

#### Installation

Installez les paquets minimaux nécessaires à un éditeur Slides dans le navigateur :

<Tabs items={['npm', 'pnpm', 'yarn']}>
  <Tab>
    ```shell
    npm install @univerjs/core @univerjs/design @univerjs/docs @univerjs/docs-ui @univerjs/drawing @univerjs/engine-render @univerjs/ui @univerjs-pro/license @univerjs-pro/slides @univerjs-pro/slides-ui
    ```
  </Tab>
  <Tab>
    ```shell
    pnpm add @univerjs/core @univerjs/design @univerjs/docs @univerjs/docs-ui @univerjs/drawing @univerjs/engine-render @univerjs/ui @univerjs-pro/license @univerjs-pro/slides @univerjs-pro/slides-ui
    ```
  </Tab>
  <Tab>
    ```shell
    yarn add @univerjs/core @univerjs/design @univerjs/docs @univerjs/docs-ui @univerjs/drawing @univerjs/engine-render @univerjs/ui @univerjs-pro/license @univerjs-pro/slides @univerjs-pro/slides-ui react react-dom rxjs
    ```
  </Tab>
</Tabs>

#### Utilisation

<Callout type="warning" title="Attention">
  - Enregistrez `UniverLicensePlugin` avant les plugins Slides.
  - Importez le CSS partagé de l’interface utilisateur avant le CSS propre au produit.
  - N’ajoutez les paquets de fonctionnalités facultatives, tels que l’impression, les tableaux, les graphiques et l’importation/exportation, que si le produit en a besoin.
</Callout>

<Steps>
  <Step>
    Importez les styles, les packs de langue et les plugins requis :

    ```typescript
    import { UniverLicensePlugin } from '@univerjs-pro/license'
    import { UniverSlidesPlugin } from '@univerjs-pro/slides'
    import { UniverSlidesUIPlugin } from '@univerjs-pro/slides-ui'
    import SlidesUIEnUS from '@univerjs-pro/slides-ui/locale/en-US'
    import SlidesEnUS from '@univerjs-pro/slides/locale/en-US'
    import { LocaleType, mergeLocales, Univer, UniverInstanceType } from '@univerjs/core'
    import DesignEnUS from '@univerjs/design/locale/en-US'
    import { UniverDocsPlugin } from '@univerjs/docs'
    import { UniverDocsUIPlugin } from '@univerjs/docs-ui'
    import DocsUIEnUS from '@univerjs/docs-ui/locale/en-US'
    import { UniverDrawingPlugin } from '@univerjs/drawing'
    import { UniverRenderEnginePlugin } from '@univerjs/engine-render'
    import { UniverUIPlugin } from '@univerjs/ui'
    import UIEnUS from '@univerjs/ui/locale/en-US'

    import '@univerjs/design/lib/index.css'
    import '@univerjs/ui/lib/index.css'
    import '@univerjs/docs-ui/lib/index.css'
    import '@univerjs-pro/slides-ui/lib/index.css'
    ```
  </Step>
  <Step>
    Importez les points d’entrée de la facade si vous souhaitez utiliser la Facade API :

    ```typescript
    import { FUniver } from '@univerjs/core/facade'
    import '@univerjs-pro/slides/facade'
    ```
  </Step>
  <Step>
    Créez une instance d’Univer, enregistrez les plugins, puis créez une unité Slides :

    ```typescript
    const univer = new Univer({
      locale: LocaleType.EN_US,
      locales: {
        [LocaleType.EN_US]: mergeLocales(
          DesignEnUS,
          UIEnUS,
          DocsUIEnUS,
          SlidesEnUS,
          SlidesUIEnUS,
        ),
      },
    })

    univer.registerPlugin(UniverRenderEnginePlugin)
    univer.registerPlugin(UniverUIPlugin, {
      container: 'app',
    })

    univer.registerPlugin(UniverDocsPlugin)
    univer.registerPlugin(UniverDocsUIPlugin)
    univer.registerPlugin(UniverDrawingPlugin)

    univer.registerPlugin(UniverLicensePlugin, {
      license: process.env.CLIENT_LICENSE_TEXT,
    })

    univer.registerPlugin(UniverSlidesPlugin)
    univer.registerPlugin(UniverSlidesUIPlugin)

    univer.createUnit(UniverInstanceType.UNIVER_SLIDE, {})

    const univerAPI = FUniver.newAPI(univer)
    ```
  </Step>
</Steps>

L’objet `univerAPI` renvoyé par `FUniver.newAPI(univer)` est la Facade API d’Univer. Utilisez-le pour créer des présentations et appeler les API de Slides.

#### `univer.registerPlugin` et `univer.registerPlugins`

`univer.registerPlugin` enregistre un plugin dans l’instance d’Univer. Utilisez `univer.registerPlugins` pour transmettre un tableau et enregistrer plusieurs plugins au même endroit.

```typescript
univer.registerPlugins([
  UniverRenderEnginePlugin,
  [UniverUIPlugin, {
    container: 'app',
  }],
  UniverDocsPlugin,
  UniverDocsUIPlugin,
  UniverDrawingPlugin,
  [UniverLicensePlugin, {
    license: process.env.CLIENT_LICENSE_TEXT,
  }],
  UniverSlidesPlugin,
  UniverSlidesUIPlugin,
])
```

#### Paquets de fonctionnalités facultatives

Ajoutez les paquets de fonctionnalités lorsque l’éditeur a besoin des capacités correspondantes :

```shell
pnpm add @univerjs-pro/slides-print
pnpm add @univerjs-pro/slides-table @univerjs-pro/slides-table-ui
pnpm add @univerjs-pro/chart-ui @univerjs-pro/slides-chart @univerjs-pro/slides-chart-ui
pnpm add @univerjs-pro/slides-exchange-client
```

Pour consulter des exemples d’enregistrement de fonctionnalités, reportez-vous aux pages [Impression](/guides/slides/features/print), [Tableaux](/guides/slides/features/tables), [Graphiques](/guides/slides/features/charts) et [Importation et exportation](/guides/slides/features/import-export).

#### Charger les plugins facultatifs à la demande

Le mode plugin vous permet de différer le chargement des plugins de fonctionnalités jusqu’à ce que le produit en ait réellement besoin :

```typescript
univer.createUnit(UniverInstanceType.UNIVER_SLIDE, {})

import('@univerjs-pro/slides-chart').then(({ UniverSlidesChartPlugin }) => {
  univer.registerPlugin(UniverSlidesChartPlugin)
})
```

### Utilisation côté serveur

Dans Node.js, enregistrez les plugins du modèle et omettez ceux de l’interface utilisateur destinés au navigateur :

```typescript
univer.registerPlugin(UniverSlidesPlugin)
```

Cette configuration convient au traitement des instantanés, à l’importation/exportation côté serveur et aux services de collaboration.

## Autres canaux de publication

Outre les versions stables, Univer propose des canaux alpha, bêta et nightly. Ils vous permettent de tester plus tôt les fonctionnalités à venir, mais peuvent présenter des comportements incomplets ou instables. Évitez de les utiliser en production sans plan de validation clairement défini.

```package-install
npm install @univerjs-pro/slides@alpha
npm install @univerjs-pro/slides@beta
```

## Étapes suivantes

- Découvrez comment utiliser la [Facade API](/guides/slides/getting-started/facade).
- Consultez [Passer à Pro](/guides/slides/getting-started/pro) pour connaître les exigences relatives à la licence et au déploiement.
- Ajoutez des plugins depuis la section [Fonctionnalités](/guides/slides/features/print).
