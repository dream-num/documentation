---
title: 가져오기 및 내보내기
icon: '#pro/FolderSync'
---

<Callout type="warning">
  **0.25.x** 사용자는 [이전 Pro 문서](https://dream-num.github.io/documentation/v0.25/zh-CN/guides/pro/)를 참고하세요. 새 통합에는 자체 호스팅 Collaboration SDK를 사용하며, 이전 Univer Server 배포 방법과 API는 적용되지 않습니다.
</Callout>

아래 브라우저 API를 사용하기 전에 [가져오기·내보내기 통합](/guides/server-integration/import-export)을 완료하세요. 앱에서 업로드, 변환, 작업 조회 및 권한이 적용된 다운로드 계약을 구현해야 합니다. 스냅샷 API도 변환 백엔드가 필요합니다. [Exchange 예제](https://office.univer.ai/collaboration/examples#exchange)는 Sheets를 다루므로 다른 제품과 형식의 지원 범위는 별도로 확인하세요.

<MetaData
  lang="ko-KR"
  isPro
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/exchange-client',
        locale: '@univerjs-pro/exchange-client/locale/ko-KR',
        style: '@univerjs-pro/exchange-client/lib/index.css',
        facade: '@univerjs-pro/exchange-client/facade',
      },
      {
        client: '@univerjs-pro/slides-exchange-client',
        locale: '@univerjs-pro/slides-exchange-client/locale/ko-KR',
        facade: '@univerjs-pro/slides-exchange-client/facade',
      },
    ],
    server: true,
  }}
/>

## 지원 형식

아래 표는 이 페이지의 브라우저 API가 지원하는 형식입니다. Office 파일 변환에는 해당 문서 유형과 형식을 지원하는 백엔드가 필요합니다.

| 가져오기 | 내보내기 |
| --- | --- |
| `.ppt`, `.pptx` | `.pptx` |

## Facade API

플러그인 모드에서는 facade 패키지를 한 번 가져옵니다.

```ts
import '@univerjs-pro/exchange-client/facade'
import '@univerjs-pro/slides-exchange-client/facade'
```

PPTX를 서버 측 unit으로 가져옵니다.

```ts
const unitId = await univerAPI.importSlideToUnitIdAsync(file)
```

PPTX를 로컬 slide 데이터로 가져옵니다.

```ts
const slideData = await univerAPI.importSlideToSnapshotAsync(file)

if (slideData) {
  univerAPI.createPresentation(slideData)
}
```

현재 slide deck을 내보냅니다.

```ts
const presentation = univerAPI.getActivePresentation()
const file = await univerAPI.exportSlideBySnapshotAsync(presentation.save())
```

서버 측 slide unit을 내보냅니다.

```ts
const file = await univerAPI.exportSlideByUnitIdAsync(unitId)
```
