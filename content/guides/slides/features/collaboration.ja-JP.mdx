---
title: 共同編集
icon: 'CloudCheck'
---

<MetaData
  lang="ja-JP"
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/collaboration',
      },
      {
        client: '@univerjs-pro/collaboration-client',
        facade: '@univerjs-pro/collaboration-client/facade',
        locale: '@univerjs-pro/collaboration-client/locale/ja-JP',
      },
      {
        client: '@univerjs-pro/collaboration-client-ui',
        facade: '@univerjs-pro/collaboration-client-ui/facade',
        locale: '@univerjs-pro/collaboration-client-ui/locale/ja-JP',
        style: '@univerjs-pro/collaboration-client-ui/lib/index.css',
      },
    ],
    server: true,
  }}
/>

複数のユーザーが同じ プレゼンテーション を編集し、互いの変更を確認できます。[共同編集の統合](/server/collaboration/browser-integration)に従ってバックエンド、認証、権限、ストレージを接続し、[ライセンス](/server/license)を設定してください。

## 設定とドキュメントの読み込み

[Office クイックスタート](/server/collaboration/quick-start)を参考に、既存の Slides エディターに Collaboration Client の設定を追加します。この例は Sheets を使用しています。バックエンドでは Slides ドキュメントを作成して永続化し、その ID をブラウザーに返してください。

```ts
import '@univerjs-pro/slides/facade'
import '@univerjs-pro/collaboration-client/facade'

const document = await univerAPI.getCollaboration().loadSlideAsync('your-document-id')
if (!document) throw new Error('Document could not be loaded')
```

ドキュメントを再度開く場合や共有する場合も、同じ ID を使います。サーバー上のドキュメントを読み込む代わりに、ローカルの空のドキュメントを作成しないでください。

## 同期と権限の確認

独立した 2 つのブラウザーセッションで同じ ID を開きます。一方でテキストを編集するか要素を移動し、もう一方に反映されることを確認します。ページの再読み込み、再接続、サービスの再起動後も内容が保持されることを確認してください。閲覧者アカウントと権限のないアカウントを使い、許可されていない書き込みと読み取りがサーバーで拒否されることも検証します。

[編集履歴](/guides/slides/features/edit-history)と[インポート・エクスポート](/guides/slides/features/import-export)は別途設定が必要です。共同編集を導入しても自動的には有効になりません。
