---
title: Diagramme
icon: '#pro/ChartPie'
---

## Diagrammunterstützung registrieren

Die Diagrammunterstützung von Slides besteht aus einem Modellpaket und einem UI-Paket. `@univerjs-pro/chart-ui` stellt die gemeinsamen Editor-Steuerelemente, Gebietsschema-Einträge und Stile bereit.

```ts
import ChartUIEnUS from '@univerjs-pro/chart-ui/locale/en-US'
import { UniverSlidesChartPlugin } from '@univerjs-pro/slides-chart'
import { UniverSlidesChartUIPlugin } from '@univerjs-pro/slides-chart-ui'
import SlidesChartUIEnUS from '@univerjs-pro/slides-chart-ui/locale/en-US'

import '@univerjs-pro/chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart/facade'

univer.registerPlugin(UniverSlidesChartPlugin)
univer.registerPlugin(UniverSlidesChartUIPlugin)
```

Registrieren Sie die Diagrammunterstützung, bevor Sie eine Präsentation öffnen oder importieren, die möglicherweise Diagrammelemente enthält.

## Erstellen und aktualisieren Sie Diagramme

Verwalten Sie Diagramme über die folienbasierte `charts`-Sammlung. `create()` gibt einen getrennten Builder zurück und `insert()` hängt ihn an die aktuelle Folie an.

```ts
const presentation = univerAPI.getActivePresentation()
const slide = presentation.getActiveSlide()

const builder = slide.charts.create()
  .setType(univerAPI.Enum.ChartTypeString.Column)
  .setData([
    ['Month', 'Revenue', 'Cost'],
    ['Jan', 120, 80],
    ['Feb', 180, 96],
    ['Mar', 240, 132],
  ])
  .setTitle('Quarterly revenue')
  .setAbsolutePosition(80, 120)
  .setSize(520, 320)

const chart = await slide.charts.insert(builder)

chart
  .setType(univerAPI.Enum.ChartTypeString.Line)
  .setLegend({ visible: true })
  .commit()
```

Verwenden Sie `slide.charts.list()`, `slide.charts.get(id)` und `await slide.charts.remove(chartOrId)`, um vorhandene Diagramme zu verwalten. Folienersteller unterstützen auch `setData`, `getData`, `setPlaceholder`, `setStroke` und `setZOrder`.

Informationen zur gemeinsamen Diagrammkonfiguration finden Sie in der [Chart Facade-Referenz](/reference/facade/chart).
