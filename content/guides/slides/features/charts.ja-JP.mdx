---
title: グラフ
icon: '#pro/ChartPie'
---

## グラフ機能を登録する

Slides のグラフ機能は、モデルパッケージと UI パッケージに分かれています。

```ts
import ChartUIJaJP from '@univerjs-pro/chart-ui/locale/ja-JP'
import { UniverSlidesChartPlugin } from '@univerjs-pro/slides-chart'
import { UniverSlidesChartUIPlugin } from '@univerjs-pro/slides-chart-ui'
import SlidesChartUIJaJP from '@univerjs-pro/slides-chart-ui/locale/ja-JP'

import '@univerjs-pro/chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart/facade'

univer.registerPlugin(UniverSlidesChartPlugin)
univer.registerPlugin(UniverSlidesChartUIPlugin)
```

## パッケージの役割

- `@univerjs-pro/slides-chart` はグラフ要素を slide モデルとグラフエンジンに接続します。
- `@univerjs-pro/chart-ui` は Pro chart UI packages が共有する編集 controls、locale entry、`@univerjs-pro/chart-ui/lib/index.css` を提供します。
- `@univerjs-pro/slides-chart-ui` はグラフ編集 UI、描画連携、挿入パネル、データソース編集を提供します。

## ランタイムの選択

ブラウザー上のエディターでは両方のパッケージを使います。サーバー側で snapshot を処理するだけなら、モデルパッケージを core Slides モデルと一緒に登録します。

```ts
univer.registerPlugin(UniverSlidesPlugin)
univer.registerPlugin(UniverSlidesChartPlugin)
```

グラフ要素を含む可能性がある deck を開く、またはインポートする前に、グラフ機能を登録してください。

## グラフを作成する

Slides のグラフは、インラインの二次元配列をデータソースとして使います。sheet range は直接読み取りません。

```ts
const fPresentation = univerAPI.getActivePresentation()
const fSlide = fPresentation.getSlideById('slide-1')

const chartInfo = fSlide.newChart()
  .setChartType(univerAPI.Enum.ChartType.Column)
  .setData([
    ['月', '売上', 'コスト'],
    ['1月', 120, 80],
    ['2月', 180, 96],
    ['3月', 240, 132],
  ])
  .setUseFirstRowAsHeaders(true)
  .setUseFirstColumnAsDomain(true)
  .setTitle('四半期売上')
  .setAbsolutePosition(80, 120)
  .setSize(520, 320)
  .build()

const chart = fSlide.insertChart(chartInfo)
```

## グラフを更新する

既存のグラフは `getCharts()` または `getChartById()` で取得できます。`updateDataSource()` はグラフ要素の位置とサイズを保ったまま、インラインデータを更新します。

```ts
const chart = fSlide.getCharts()[0]

chart?.updateDataSource(
  [
    ['月', '売上', 'コスト'],
    ['4月', 260, 140],
    ['5月', 300, 168],
  ],
  {
    useFirstRowAsHeaders: true,
    useFirstColumnAsDomain: true,
  },
)
```

グラフの種類やスタイルを変更する場合は、グラフを builder に戻して `updateChart()` を呼び出します。

```ts
if (chart) {
  const nextChartInfo = chart.toBuilder()
    .setChartType(univerAPI.Enum.ChartType.Line)
    .setOptions('legend.position', univerAPI.Enum.ChartLegendPositionEnum.Right)
    .setSeriesStyle(0, { color: '#2F80ED' })
    .build()

  fSlide.updateChart(nextChartInfo)
}
```

## グラフを削除する

```ts
chart?.remove()
// または
if (chart) {
  fSlide.removeChart(chart)
}
```
