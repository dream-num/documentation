---
title: Импорт и экспорт
icon: '#pro/FolderSync'
---

<Callout type="warning">
  Используете **0.25.x**? Откройте [архив документации Pro](https://dream-num.github.io/documentation/v0.25/zh-CN/guides/pro/). Для новых интеграций используется самостоятельно размещаемый Collaboration SDK; инструкции и API прежнего Univer Server не применяются.
</Callout>

Перед использованием API браузера выполните [интеграцию импорта и экспорта](/guides/server-integration/import-export). Приложение реализует загрузку, конвертацию, опрос задач и скачивание с проверкой доступа. API снимков также требуют сервера конвертации. [Пример Exchange](https://office.univer.ai/collaboration/examples#exchange) предназначен для Sheets; поддержку других продуктов и форматов проверяйте отдельно.

<MetaData
  lang="ru-RU"
  isPro
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/exchange-client',
        locale: '@univerjs-pro/exchange-client/locale/ru-RU',
        style: '@univerjs-pro/exchange-client/lib/index.css',
        facade: '@univerjs-pro/exchange-client/facade',
      },
      {
        client: '@univerjs-pro/slides-exchange-client',
        locale: '@univerjs-pro/slides-exchange-client/locale/ru-RU',
        facade: '@univerjs-pro/slides-exchange-client/facade',
      },
    ],
    server: true,
  }}
/>

## Поддерживаемые форматы

В таблице указаны форматы браузерных API этой страницы. Для преобразования файлов Office нужен сервер с поддержкой соответствующего типа документа и формата.

| Импорт | Экспорт |
| --- | --- |
| `.ppt`, `.pptx` | `.pptx` |

## Facade API

В режиме плагинов один раз импортируйте пакет facade:

```ts
import '@univerjs-pro/exchange-client/facade'
import '@univerjs-pro/slides-exchange-client/facade'
```

Импорт PPTX в серверный юнит:

```ts
const unitId = await univerAPI.importSlideToUnitIdAsync(file)
```

Импорт PPTX в виде локальных данных слайдов:

```ts
const slideData = await univerAPI.importSlideToSnapshotAsync(file)

if (slideData) {
  univerAPI.createPresentation(slideData)
}
```

Экспорт текущей презентации:

```ts
const presentation = univerAPI.getActivePresentation()
const file = await univerAPI.exportSlideBySnapshotAsync(presentation.save())
```

Экспорт серверного юнита со слайдами:

```ts
const file = await univerAPI.exportSlideByUnitIdAsync(unitId)
```
