---
title: Importation et exportation
icon: '#pro/FolderSync'
---

<Callout type="warning">
  Vous utilisez **0.25.x** ? Consultez la [documentation Pro archivée](https://dream-num.github.io/documentation/v0.25/zh-CN/guides/pro/). Les nouvelles intégrations utilisent le Collaboration SDK auto-hébergé ; les procédures et API de l’ancien Univer Server ne s’appliquent pas.
</Callout>

Avant d’utiliser les API ci-dessous, suivez l’[intégration des imports/exports](/guides/server-integration/import-export). Votre application doit fournir les contrats de téléversement, conversion, suivi des tâches et téléchargement autorisé. Les API de snapshot nécessitent aussi un backend de conversion. L’[exemple Exchange](https://office.univer.ai/collaboration/examples#exchange) concerne Sheets ; vérifiez séparément les autres produits et formats.

<MetaData
  lang="fr-FR"
  isPro
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/exchange-client',
        locale: '@univerjs-pro/exchange-client/locale/fr-FR',
        style: '@univerjs-pro/exchange-client/lib/index.css',
        facade: '@univerjs-pro/exchange-client/facade',
      },
      {
        client: '@univerjs-pro/slides-exchange-client',
        locale: '@univerjs-pro/slides-exchange-client/locale/fr-FR',
        facade: '@univerjs-pro/slides-exchange-client/facade',
      },
    ],
    server: true,
  }}
/>

## Formats pris en charge

Le tableau décrit les API du navigateur présentées ici. La conversion des fichiers Office nécessite un backend compatible avec le même type de document et le même format.

| Importation | Exportation |
| --- | --- |
| `.ppt`, `.pptx` | `.pptx` |

## API facade

En mode plugin, importez une fois le package facade :

```ts
import '@univerjs-pro/exchange-client/facade'
import '@univerjs-pro/slides-exchange-client/facade'
```

Importez un fichier PPTX dans une unité côté serveur :

```ts
const unitId = await univerAPI.importSlideToUnitIdAsync(file)
```

Importez un fichier PPTX sous forme de données de diapositives locales :

```ts
const slideData = await univerAPI.importSlideToSnapshotAsync(file)

if (slideData) {
  univerAPI.createPresentation(slideData)
}
```

Exportez la présentation actuelle :

```ts
const presentation = univerAPI.getActivePresentation()
const file = await univerAPI.exportSlideBySnapshotAsync(presentation.save())
```

Exportez une unité de diapositives côté serveur :

```ts
const file = await univerAPI.exportSlideByUnitIdAsync(unitId)
```
