---
title: Charts
icon: '#pro/ChartPie'
---

## Register chart support

Slides chart support consists of a model package and a UI package. `@univerjs-pro/chart-ui` provides the shared editor controls, locale entries, and styles.

```ts
import ChartUIEnUS from '@univerjs-pro/chart-ui/locale/en-US'
import { UniverSlidesChartPlugin } from '@univerjs-pro/slides-chart'
import { UniverSlidesChartUIPlugin } from '@univerjs-pro/slides-chart-ui'
import SlidesChartUIEnUS from '@univerjs-pro/slides-chart-ui/locale/en-US'

import '@univerjs-pro/chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart/facade'

univer.registerPlugin(UniverSlidesChartPlugin)
univer.registerPlugin(UniverSlidesChartUIPlugin)
```

Register chart support before opening or importing a presentation that might contain chart elements.

## Create and update charts

Manage charts through the slide-scoped `charts` collection. `create()` returns a detached builder, and `insert()` attaches it to the current slide.

```ts
const presentation = univerAPI.getActivePresentation()
const slide = presentation.getActiveSlide()

const builder = slide.charts
  .create()
  .setType(univerAPI.Enum.ChartTypeString.Column)
  .setData([
    ['Month', 'Revenue', 'Cost'],
    ['Jan', 120, 80],
    ['Feb', 180, 96],
    ['Mar', 240, 132],
  ])
  .setTitle('Quarterly revenue')
  .setAbsolutePosition(80, 120)
  .setSize(520, 320)

const chart = await slide.charts.insert(builder)

chart.setType(univerAPI.Enum.ChartTypeString.Line).setLegend({ visible: true }).commit()
```

Use `slide.charts.list()`, `slide.charts.get(id)`, and `await slide.charts.remove(chartOrId)` to manage existing charts. Slide builders also support `setData`, `getData`, `setPlaceholder`, `setStroke`, and `setZOrder`.

## Bind a chart to Sheet data

Use a resource reference when the chart should follow a Sheet range instead of storing copied values:

```ts
const chart = await slide.charts.insert(
  slide.charts.create().setDataSource({
    unit: { selector: 'sales-workbook', type: 'sheet' },
    part: {
      kind: 'range',
      sheetName: 'Data',
      range: 'A1:B20',
      ref: 'Data!A1:B20',
    },
  }),
)

await chart.refreshDataSource()
```

See the [Chart Facade reference](/reference/facade/chart) for shared chart configuration.
