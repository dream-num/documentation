---
title: Importación y exportación
icon: '#pro/FolderSync'
---

<Callout type="warning">
  ¿Usas **0.25.x**? Consulta la [documentación Pro archivada](https://dream-num.github.io/documentation/v0.25/zh-CN/guides/pro/). Las nuevas integraciones usan Collaboration SDK autoalojado; las instrucciones y API del antiguo Univer Server no se aplican.
</Callout>

Antes de usar las API del navegador, completa la [integración de importación y exportación](/guides/server-integration/import-export). Tu aplicación debe implementar los contratos de carga, conversión, consulta de tareas y descarga autorizada. Las API de snapshot también necesitan un backend de conversión. El [ejemplo Exchange](https://office.univer.ai/collaboration/examples#exchange) demuestra Sheets; verifica por separado otros productos y formatos.

<MetaData
  lang="es-ES"
  isPro
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/exchange-client',
        locale: '@univerjs-pro/exchange-client/locale/es-ES',
        style: '@univerjs-pro/exchange-client/lib/index.css',
        facade: '@univerjs-pro/exchange-client/facade',
      },
      {
        client: '@univerjs-pro/slides-exchange-client',
        locale: '@univerjs-pro/slides-exchange-client/locale/es-ES',
        facade: '@univerjs-pro/slides-exchange-client/facade',
      },
    ],
    server: true,
  }}
/>

## Formatos compatibles

La tabla describe las API del navegador de esta página. La conversión de archivos Office requiere un backend compatible con el mismo tipo de documento y formato.

| Importación | Exportación |
| --- | --- |
| `.ppt`, `.pptx` | `.pptx` |

## API de fachada

Importa el paquete de fachada una vez en modo complemento:

```ts
import '@univerjs-pro/exchange-client/facade'
import '@univerjs-pro/slides-exchange-client/facade'
```

Importa un PPTX a una unidad del lado del servidor:

```ts
const unitId = await univerAPI.importSlideToUnitIdAsync(file)
```

Importa un PPTX como datos de diapositiva local:

```ts
const slideData = await univerAPI.importSlideToSnapshotAsync(file)

if (slideData) {
  univerAPI.createPresentation(slideData)
}
```

Exporta el conjunto de diapositivas actual:

```ts
const presentation = univerAPI.getActivePresentation()
const file = await univerAPI.exportSlideBySnapshotAsync(presentation.save())
```

Exporta una diapositiva del lado del servidor unidad:

```ts
const file = await univerAPI.exportSlideByUnitIdAsync(unitId)
```
