---
title: Gráficos
icon: '#pro/ChartPie'
---

## Registrar compatibilidad con gráficos

La compatibilidad con gráficos de diapositivas consta de un paquete de modelo y un paquete de interfaz de usuario. `@univerjs-pro/chart-ui` proporciona controles de editor, entradas de configuración regional y estilos compartidos.

```ts
import ChartUIEnUS from '@univerjs-pro/chart-ui/locale/en-US'
import { UniverSlidesChartPlugin } from '@univerjs-pro/slides-chart'
import { UniverSlidesChartUIPlugin } from '@univerjs-pro/slides-chart-ui'
import SlidesChartUIEnUS from '@univerjs-pro/slides-chart-ui/locale/en-US'

import '@univerjs-pro/chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart-ui/lib/index.css'
import '@univerjs-pro/slides-chart/facade'

univer.registerPlugin(UniverSlidesChartPlugin)
univer.registerPlugin(UniverSlidesChartUIPlugin)
```

Registre la compatibilidad con gráficos antes de abrir o importar una presentación que pueda contener elementos de gráficos.

## Crear y actualizar gráficos

Administrar gráficos a través de la colección `charts` con alcance de diapositivas. `create()` devuelve un constructor separado y `insert()` lo adjunta a la diapositiva actual.

```ts
const presentation = univerAPI.getActivePresentation()
const slide = presentation.getActiveSlide()

const builder = slide.charts.create()
  .setType(univerAPI.Enum.ChartTypeString.Column)
  .setData([
    ['Month', 'Revenue', 'Cost'],
    ['Jan', 120, 80],
    ['Feb', 180, 96],
    ['Mar', 240, 132],
  ])
  .setTitle('Quarterly revenue')
  .setAbsolutePosition(80, 120)
  .setSize(520, 320)

const chart = await slide.charts.insert(builder)

chart
  .setType(univerAPI.Enum.ChartTypeString.Line)
  .setLegend({ visible: true })
  .commit()
```

Utilice `slide.charts.list()`, `slide.charts.get(id)` y `await slide.charts.remove(chartOrId)` para administrar gráficos existentes. Los creadores de diapositivas también admiten `setData`, `getData`, `setPlaceholder`, `setStroke` y `setZOrder`.

Consulte la [referencia de Chart Facade](/reference/facade/chart) para la configuración de gráficos compartidos.
