---
title: インポートとエクスポート
icon: 'FolderSync'
---


以下のブラウザー API を使用する前に[インポート・エクスポート統合](/server/import-export)を完了してください。アップロード、変換、タスク照会、認可されたダウンロードの契約をアプリ側で実装します。スナップショット API にも変換バックエンドが必要です。[Exchange サンプル](/server/collaboration/examples#exchange)は Sheets を対象とするため、他の製品や形式の対応状況は個別に確認してください。

<MetaData
  lang="ja-JP"
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/exchange-client',
        locale: '@univerjs-pro/exchange-client/locale/ja-JP',
        style: '@univerjs-pro/exchange-client/lib/index.css',
        facade: '@univerjs-pro/exchange-client/facade',
      },
      {
        client: '@univerjs-pro/slides-exchange-client',
        locale: '@univerjs-pro/slides-exchange-client/locale/ja-JP',
        facade: '@univerjs-pro/slides-exchange-client/facade',
      },
    ],
    license: true,
    server: true,
  }}
/>

## 対応形式

以下は本ページのブラウザー API が対応する形式です。Office ファイルの変換には、同じドキュメント種類と形式に対応したバックエンドが必要です。

| インポート | エクスポート |
| --- | --- |
| `.ppt`, `.pptx` | `.pptx` |

## Facade API

Plugin モードでは facade パッケージを一度インポートします。

```ts
import '@univerjs-pro/exchange-client/facade'
import '@univerjs-pro/slides-exchange-client/facade'
```

PPTX をサーバー側 unit としてインポートします。

```ts
const unitId = await univerAPI.importSlideToUnitIdAsync(file)
```

PPTX をローカルの slide データとしてインポートします。

```ts
const slideData = await univerAPI.importSlideToSnapshotAsync(file)

if (slideData) {
  univerAPI.createPresentation(slideData)
}
```

現在の slide deck をエクスポートします。

```ts
const presentation = univerAPI.getActivePresentation()
const file = await univerAPI.exportSlideBySnapshotAsync(presentation.save())
```

サーバー側の slide unit をエクスポートします。

```ts
const file = await univerAPI.exportSlideByUnitIdAsync(unitId)
```
