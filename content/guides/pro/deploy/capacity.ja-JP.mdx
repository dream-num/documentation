---
title: キャパシティ計画
---

import { CapacityCalculator } from './components/capacity-calculator'

## キャパシティ計画

実際の業務規模に応じてキャパシティ要件を評価する必要があります。需要が大きい場合、Docker Compose によるデプロイは適さない可能性があります。特記事項：

- **exchange worker**（インポート／エクスポートサービス）は計算集約型サービスであり、インポート／エクスポートタスクの処理時に短時間で大量の CPU とメモリを消費することがあります。特に大きなドキュメントをインポートする場合です。インポート／エクスポートの需要を評価し、適切な CPU とメモリリソースを割り当ててください。
- **collaboration-server**（共同編集エンジン）はステートフルな Node サービスであり、同時に多くのドキュメントが編集状態で開かれていると、メモリを大量に消費します。必要なインスタンス数と各インスタンスのメモリ構成を評価してください。超大規模なスプレッドシート（500 万セルを超える）を編集する必要がある場合、各インスタンスに最低 8 GB のメモリを割り当てることを推奨します。

さらに、`v0.15.0` から導入された共同編集の階層スケジューリングでは、使用シナリオにおけるスプレッドシートの規模分布（ドキュメントとスライドは階層スケジューリングに参加しません）を統計して、階層スケジューリングを構成する必要があるかどうかを判断する必要があります。必要な場合は、クラスタの階層数と各階層が処理するスプレッドシートの規模（総セル数、インポート所要時間など）も評価する必要があります。具体的な構成手順については、対応するデプロイプラットフォームの構成ドキュメントを参照してください。

### 共同編集のリソース要件計算

ほとんどが小規模なドキュメントであれば、共同編集サービスは 2c4g の構成を選択できます。そうでない場合は 4c8g を推奨します。CPU リソースの浪費を招くため、単一の共同編集サービスインスタンスの仕様をさらに増やすことは推奨しません。

- 単一の共同編集サービスインスタンス（2c4g / 4c8g）は、同時編集可能な小規模ドキュメント数として約 1100 / 1200 件をサポートし、編集保存 QPS で換算すると約 360 / 400 です。
- 単一の universer サービス（2c4g / 4c8g）は、同時編集可能な小規模ドキュメント数として約 1200 / 2200 件をサポートし、編集保存 QPS で換算すると約 400 / 730 です。

### 多人協同のリソース要件計算

多人協同では、編集保存によるリソース消費に加えて、協同メッセージのブロードキャストによる CPU 需要がより顕著です。協同メッセージのブロードキャストは universer サービスによって行われ、collaboration-server のリソース占有には影響しません。

2c4g × 2 の構成であれば、1400 人に対して毎秒 10 回の編集操作をプッシュできます。平均して、1 コアの CPU は毎秒最大 3500 回の協同ブロードキャストをサポートできます。

協同ブロードキャスト QPS の計算式は以下の通りです：

```
協同ブロードキャスト QPS = 編集保存 QPS × (アクティブユーザー数 / アクティブドキュメント数 − 1)
```

ここで 1 を引くのは、自分自身へのプッシュは既に編集保存 QPS に含まれているためです。

### キャパシティ要件計算機

以下の計算機を使用して、実際の業務パラメータに基づき必要なリソース構成を迅速に見積もることができます。アクティブドキュメント数、保存頻度、協同比率などのパラメータを入力すると、計算機は自動的に編集 QPS、協同ブロードキャスト QPS、および推奨される Node サービス、Universer、データベース構成を出力します。

<CapacityCalculator locale="ja-JP" />
