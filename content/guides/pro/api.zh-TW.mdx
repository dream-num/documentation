---
title: API
---

本文列出常用 Univer Server API，預設 Base URL 為 `http://localhost:8000`。

> 說明：`type` 參數值：`1` 表示 Docs，`2` 表示 Sheets。

## 文件管理

### 建立文件

<APITable
  request={{
    method: 'POST',
    url: '/universer-api/snapshot/{type}/unit/-/create',
    headers: <code>content-type: application/json</code>,
    parametersType: 'Body',
    parameters: [{
      name: 'type',
      type: 'enum(int)',
      required: true,
      description: '1（docs），2（sheets）路徑與請求體參數',
    }, {
      name: 'name',
      type: 'string',
      required: true,
      description: '文件名稱',
    }, {
      name: 'creator',
      type: 'string',
      required: true,
      description: '建立者 id',
    }],
    example: `curl http://localhost:8000/universer-api/snapshot/{type}/unit/-/create \\
    -X POST \\
    -H 'Content-Type: application/json' \\
    --data-raw '{"type":2,"name":"New Sheet By Univer","creator":"userID"}'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }, {
      name: 'unitID',
      type: 'string',
      description: '文件 id',
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: 'success',
      },
      unitID: 'ETVf-B4lQqOSE_p09mcp9Q',
    }, null, 2),
  }}
/>

### 取得文件列表

<APITable
  request={{
    method: 'GET',
    url: '/universer-api/snapshot/{type}/units',
    parametersType: 'Query',
    parameters: [{
      name: 'type',
      type: 'enum(int)',
      required: true,
      description: '1（doc），2（sheet）路徑與請求體參數',
    }, {
      name: 'nextCursor',
      type: 'string',
      required: false,
      description: '分頁下一頁游標',
    }],
    example: `curl -X GET 'http://localhost:8000/universer-api/snapshot/1/units?nextCursor=100'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }, {
      name: 'units',
      type: 'array[object]',
      description: '文件列表',
      properties: [{
        name: 'unitID',
        type: 'string',
        description: '文件 id',
      }, {
        name: 'name',
        type: 'string',
        description: '文件名稱',
      }, {
        name: 'type',
        type: 'enum(int)',
        description: '1（doc），2（sheet）',
      }],
    }, {
      name: 'nextCursor',
      type: 'string',
      description: '下一頁游標，空值代表最後一頁',
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: '',
      },
      units: [{
        unitID: '1',
        name: 'a',
        type: 1,
      }],
      nextCursor: '200',
    }, null, 2),
  }}
/>

### 刪除文件

<APITable
  request={{
    method: 'DELETE',
    url: '/universer-api/snapshot/-/units',
    headers: <code>content-type: application/json</code>,
    parametersType: 'Query',
    parameters: [{
      name: 'unitIds',
      type: 'array[string]',
      required: true,
      description: '文件 id 陣列',
    }],
    example: `curl -X DELETE 'http://localhost:8000/universer-api/snapshot/-/units?unitIds=1&unitIds=2'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: '',
      },
    }, null, 2),
  }}
/>

## 檔案與匯入匯出

### 上傳檔案

<APITable
  request={{
    method: 'POST',
    url: '/universer-api/stream/file/upload',
    headers: <code>content-type: multipart/form-data</code>,
    parametersType: 'Body',
    parameters: [{
      name: 'size',
      type: 'int',
      required: true,
      description: '檔案大小（byte），query 參數；必須與實際大小一致，否則回傳 error',
    }, {
      name: 'file',
      type: 'Form.file',
      required: true,
      description: 'HTML form 上傳檔案',
    }],
    example: `curl 'http://localhost:8000/universer-api/stream/file/upload?size=125466' \\
    --header 'cookie: _univer=XXXXXX' \\
    --form 'file=@"demo.xlsx"'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'FileId',
      type: 'string',
      description: '檔案 id，供後續匯入匯出使用',
    }],
    example: JSON.stringify({
      FileId: 'xxxx',
    }, null, 2),
  }}
/>

### 匯入

<APITable
  request={{
    method: 'POST',
    url: '/universer-api/exchange/{type}/import',
    headers: <code>content-type: application/json</code>,
    parametersType: 'Body',
    parameters: [{
      name: 'type',
      type: 'enum(int)',
      required: true,
      description: '1（doc），2（sheet）',
    }, {
      name: 'outputType',
      type: 'enum(int)',
      required: true,
      description: '1（unit），2（json）',
    }, {
      name: 'fileID',
      type: 'string',
      required: true,
      description: '上傳檔案 id',
    }, {
      name: 'minSheetRowCount',
      type: 'int',
      required: true,
      description: '最小列數，匯入時檢查列數是否達標',
    }, {
      name: 'minSheetColumnCount',
      type: 'int',
      required: true,
      description: '最小欄數，匯入時檢查欄數是否達標',
    }],
    example: `curl -X POST 'http://localhost:8000/universer-api/exchange/2/import' \\
    -H 'Content-Type: application/json' \\
    --data-raw '{"fileID":"123","outputType":1,"minSheetRowCount":1000,"minSheetColumnCount":20}'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }, {
      name: 'taskID',
      type: 'string',
      description: '任務 id，用於輪詢結果',
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: '',
      },
      taskID: '456',
    }, null, 2),
  }}
/>

### 匯出

<APITable
  request={{
    method: 'POST',
    url: '/universer-api/exchange/{type}/export',
    headers: <code>content-type: application/json</code>,
    parametersType: 'Body',
    parameters: [{
      name: 'type',
      type: 'enum(int)',
      required: true,
      description: '1（doc），2（sheet）',
    }, {
      name: 'unitID',
      type: 'string',
      required: true,
      description: '文件 id',
    }, {
      name: 'sscSwitch',
      type: 'boolean',
      required: false,
      description: '是否啟用 SSC（Server Side Calculation），true 觸發服務端公式計算，預設 false',
    }],
    example: `curl -X POST 'http://localhost:8000/universer-api/exchange/2/export' \\
    -H 'Content-Type: application/json' \\
    --data-raw '{"unitID":"xxxx","type":2}'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }, {
      name: 'taskID',
      type: 'string',
      description: '任務 id，用於輪詢結果',
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: '',
      },
      taskID: '456',
    }, null, 2),
  }}
/>

### 取得轉換結果

<APITable
  request={{
    method: 'GET',
    url: '/universer-api/exchange/task/{taskID}',
    parametersType: 'Path',
    parameters: [{
      name: 'taskID',
      type: 'string',
      required: true,
      description: '任務 id',
    }],
    example: `curl -X GET 'http://localhost:8000/universer-api/exchange/task/123'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }, {
      name: 'status',
      type: 'enum(string)',
      description: '"pending", "done", "failed"',
    }, {
      name: 'export',
      type: 'object',
      properties: [{
        name: 'fileID',
        type: 'string',
        description: '匯出結果檔案 id',
      }],
    }, {
      name: 'import',
      type: 'object',
      properties: [{
        name: 'unitID',
        type: 'string',
        description: '匯入後的 unitID',
      }, {
        name: 'jsonID',
        type: 'string',
        description: '匯入 JSON 結果檔案 id',
      }],
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: '',
      },
      status: 'done',
      export: {
        fileID: '456',
      },
      import: {
        unitID: '789',
        jsonID: '012',
      },
    }, null, 2),
  }}
/>

### 取得檔案

<APITable
  request={{
    method: 'GET',
    url: '/universer-api/file/{fileID}/sign-url',
    parametersType: 'Path',
    parameters: [{
      name: 'fileID',
      type: 'string',
      required: true,
      description: '結果檔案 id',
    }],
    example: `curl -X GET 'http://localhost:8000/universer-api/file/1234/sign-url'`,
  }}
  response={{
    type: 'application/json',
    parameters: [{
      name: 'error',
      type: 'object',
      properties: [{
        name: 'code',
        type: 'enum(int)',
        description: '1（成功）',
      }, {
        name: 'message',
        type: 'string',
        description: '錯誤訊息',
      }],
    }, {
      name: 'url',
      type: 'string',
      description: '下載連結',
    }],
    example: JSON.stringify({
      error: {
        code: 1,
        message: '',
      },
      url: 'https://example.com/path/to/file.xlsx',
    }, null, 2),
  }}
/>
