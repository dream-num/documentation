---
title: インポート/エクスポート
---

インポート/エクスポートはサーバーで提供され、Office ファイルを Univer 文書へ変換したり、Office 形式で出力できます。

## 前提条件

- サーバーが導入済み（[クイックスタート](/guides/pro/server)）
- exchange worker と temporal が稼働
- Object Storage が到達可能

## インポート手順

1. ファイルを Object Storage に[アップロード](/guides/pro/api#upload-file)し `fileID` を取得
2. `outputType` を指定して[インポート](/guides/pro/api#import-file) API を呼び出す
   - `1`：unit 文書としてインポート
   - `2`：JSON としてインポート
3. [タスクの状態](/guides/pro/api#get-task-result)をポーリング：
   - `pending`：継続
   - `done`：取得 `import.unitID` または `import.jsonID`
   - `failed`：`error.message` を参照
4. ドキュメントの読み込み
   - unit 文書としてインポートした場合は、`import.unitID` を使用して直接読み込む。使用方法は[共同編集](/guides/sheets/features/collaboration)章を参照
   - JSON としてインポートした場合は、`import.jsonID` を使用して[ファイル取得](/guides/pro/api#get-file)しダウンロード URL を取得。JSON をダウンロードした後、[サーバー側変換](/guides/sheets/features/import-export#server-side-data-conversion)に従って処理し、読み込む

![import](./import-export/import.png)

## エクスポート手順

unit 協同文書のエクスポート手順：

1. `unitID` で[エクスポート](/guides/pro/api#export-file) API を呼び出し `taskID` を取得
2. [タスクの状態](/guides/pro/api#get-task-result)をポーリング：
   - `pending`：継続
   - `done`：取得 `export.fileID`
   - `failed`：`error.message` を参照
3. `export.fileID` を使用して[ファイル取得](/guides/pro/api#get-file)でダウンロード URL を取得

![export](./import-export/export.png)

非協同文書のエクスポート手順：

1. 前端 snapshot json 生成的文件を Object Storage に[アップロード](/guides/pro/api#upload-file)し `fileID` を取得
2. `jsonID` を使用して[エクスポート](/guides/pro/api#export-file) API を呼び出し `taskID` を取得；`jsonID` は前のステップでアップロードしたファイルの `fileID`
3. [タスクの状態](/guides/pro/api#get-task-result)をポーリング：
   - `pending`：継続
   - `done`：取得 `export.fileID`
   - `failed`：`error.message` を参照
4. `export.fileID` を使用して[ファイル取得](/guides/pro/api#get-file)でダウンロード URL を取得

## 実装例

フロントエンドはすでにインポート/エクスポートの API 呼び出しを統合しており、詳細は [Facade API](/guides/sheets/features/import-export#facade-api) を参照してください。

以下は `fetch` を使った TypeScript の実装例です。`BASE_URL` を Univer サーバーのエンドポイントに置き換え、各リクエストに認証ヘッダー（例：`cookie` や `Authorization`）を含めてください。

### 1. ファイルをアップロードする

```typescript
async function uploadFile(file: File): Promise<string> {
  const formData = new FormData()
  formData.append('file', file)

  const res = await fetch(
    `${BASE_URL}/universer-api/stream/file/upload?size=${file.size}`,
    {
      method: 'POST',
      headers: {
        // 認証ヘッダーを追加してください。例：
        // cookie: '_univer=XXXXXX',
      },
      body: formData,
    },
  )

  const data = await res.json()
  if (data.error?.code !== 1) {
    throw new Error(data.error?.message || 'アップロードに失敗しました')
  }
  return data.FileId // 次のステップで fileID として使用
}
```

### 2. タスク状態をポーリングする

```typescript
interface TaskResult {
  status: 'pending' | 'done' | 'failed'
  error?: { code: number, message: string }
  import?: { unitID?: string, jsonID?: string }
  export?: { fileID?: string }
}

async function pollTask(taskID: string, interval = 2000): Promise<TaskResult> {
  while (true) {
    const res = await fetch(
      `${BASE_URL}/universer-api/exchange/task/${taskID}`,
      {
        headers: {
          // cookie: '_univer=XXXXXX',
        },
      },
    )
    const data: TaskResult = await res.json()

    if (data.status === 'done' || data.status === 'failed') {
      return data
    }

    await new Promise(resolve => setTimeout(resolve, interval))
  }
}
```

### 3. ファイルをインポートする

```typescript
interface ImportParams {
  fileID: string
  type: 1 | 2 // 1 = ドキュメント, 2 = スプレッドシート
  outputType: 1 | 2 // 1 = unit 文書, 2 = JSON
  minSheetRowCount?: number
  minSheetColumnCount?: number
}

async function importFile(params: ImportParams): Promise<string> {
  const res = await fetch(
    `${BASE_URL}/universer-api/exchange/${params.type}/import`,
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        // cookie: '_univer=XXXXXX',
      },
      body: JSON.stringify({
        fileID: params.fileID,
        outputType: params.outputType,
        minSheetRowCount: params.minSheetRowCount ?? 1000,
        minSheetColumnCount: params.minSheetColumnCount ?? 20,
      }),
    },
  )

  const data = await res.json()
  if (data.error?.code !== 1) {
    throw new Error(data.error?.message || 'インポートリクエストに失敗しました')
  }

  // タスクが完了または失敗するまでポーリング
  const taskResult = await pollTask(data.taskID)
  if (taskResult.status === 'failed') {
    throw new Error(taskResult.error?.message || 'インポートに失敗しました')
  }

  return params.outputType === 1
    ? taskResult.import!.unitID!
    : taskResult.import!.jsonID!
}
```

### 4. ファイルをエクスポートする

```typescript
interface ExportParams {
  unitID: string
  jsonID: string
  type: 1 | 2 // 1 = ドキュメント, 2 = スプレッドシート
  sscSwitch?: boolean
}

async function exportFile(params: ExportParams): Promise<string> {
  const res = await fetch(
    `${BASE_URL}/universer-api/exchange/${params.type}/export`,
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        // cookie: '_univer=XXXXXX',
      },
      body: JSON.stringify({
        unitID: params.unitID,
        type: params.type,
        sscSwitch: params.sscSwitch ?? false,
      }),
    },
  )

  const data = await res.json()
  if (data.error?.code !== 1) {
    throw new Error(data.error?.message || 'エクスポートリクエストに失敗しました')
  }

  const taskResult = await pollTask(data.taskID)
  if (taskResult.status === 'failed') {
    throw new Error(taskResult.error?.message || 'エクスポートに失敗しました')
  }

  // 署名付きダウンロード URL を取得
  const urlRes = await fetch(
    `${BASE_URL}/universer-api/file/${taskResult.export!.fileID!}/sign-url`,
    {
      headers: {
        // cookie: '_univer=XXXXXX',
      },
    },
  )
  const urlData = await urlRes.json()
  if (urlData.error?.code !== 1) {
    throw new Error(urlData.error?.message || 'ダウンロード URL の取得に失敗しました')
  }

  return urlData.url
}
```

## API / サンプル

- API： [Univer Server API](/guides/pro/api)
- 例：https://github.com/dream-num/usip-example/tree/main/import
