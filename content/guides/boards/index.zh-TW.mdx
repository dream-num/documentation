---
title: Univer Boards
description: Univer Boards 提供白板、圖形、表格元素和心智圖使用的畫布工作區。
icon: BoardsMultiIcon
---

## Univer Boards 是什麼

Univer Boards 是 Univer Pro 中的畫布產品。它適合用來做白板、流程圖、便條牆、連接線圖、嵌入式表格和心智圖。當內容不再是儲存格、頁面或投影片，而是需要在大畫布上自由擺放時，就應該看 Boards。

Boards 在執行時是獨立的 Univer unit。應用程式需要建立 `UniverInstanceType.UNIVER_BOARD`，再按需要註冊 Board 模型、畫布 UI、表格元素和心智圖外掛。

## 什麼時候使用 Boards

如果你的應用需要空間化的編輯區域，例如腦力激盪、流程梳理、專案規劃、回顧白板或產品結構圖，可以從 Boards 開始。如果核心資料是結構化記錄和多視圖管理，請看 [Univer Bases](/guides/bases)。如果核心物件是試算表，請看 [Univer Sheets](/guides/sheets)。

## 安裝套件

Boards 的核心執行時分成模型套件和 UI 套件。表格元素、心智圖元素是建立在 Boards 之上的可選能力。

```bash
pnpm add @univerjs-pro/boards @univerjs-pro/boards-ui
pnpm add @univerjs-pro/boards-table @univerjs-pro/boards-table-ui
pnpm add @univerjs-pro/boards-mind @univerjs-pro/boards-mind-ui
```

## 套件結構

- `@univerjs-pro/boards`：Board 資料模型、元素 schema、命令、mutation、適配服務、Facade 入口與 `UniverBoardsPlugin`
- `@univerjs-pro/boards-ui`：Board 畫布 UI、視口、選取、形狀編輯、連接線、便條、墨跡互動、工具列、右鍵選單與 `UniverBoardsUIPlugin`
- `@univerjs-pro/boards-table`：Board 表格元素整合與 `UniverBoardsTablePlugin`
- `@univerjs-pro/boards-table-ui`：表格元素 UI、渲染整合、多語言入口與 `UniverBoardsTableUIPlugin`
- `@univerjs-pro/boards-mind`：心智圖元素整合與 `UniverBoardsMindPlugin`
- `@univerjs-pro/boards-mind-ui`：心智圖 UI 整合與 `UniverBoardsMindUIPlugin`

## 註冊外掛

下面只展示 Boards 相關的註冊程式碼。完整應用還需要 Univer core、渲染引擎、UI、License、Ink 等通用外掛。

```ts
import { UniverBoardsPlugin } from '@univerjs-pro/boards'
import { UniverBoardsMindPlugin } from '@univerjs-pro/boards-mind'
import { UniverBoardsMindUIPlugin } from '@univerjs-pro/boards-mind-ui'
import { UniverBoardsTablePlugin } from '@univerjs-pro/boards-table'
import { UniverBoardsTableUIPlugin } from '@univerjs-pro/boards-table-ui'
import { UniverBoardsUIPlugin } from '@univerjs-pro/boards-ui'
import { UniverInstanceType } from '@univerjs/core'
import '@univerjs-pro/boards/facade'

univer.registerPlugin(UniverBoardsPlugin)
univer.registerPlugin(UniverBoardsUIPlugin)
univer.registerPlugin(UniverBoardsMindPlugin)
univer.registerPlugin(UniverBoardsMindUIPlugin)
univer.registerPlugin(UniverBoardsTablePlugin)
univer.registerPlugin(UniverBoardsTableUIPlugin)

univer.createUnit(UniverInstanceType.UNIVER_BOARD, {})
```

## 繼續閱讀

- 授權和服務端相關設定請看 [Univer Pro](/guides/pro)。
- 想理解 unit、外掛和渲染模組之間的關係，可以先讀 [架構實踐](/guides/recipes/architecture/univer)。
- 整合時可以先根據上面的套件結構決定是否啟用表格元素和心智圖元素。
