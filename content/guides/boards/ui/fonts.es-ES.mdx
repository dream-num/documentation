---
title: Fuentes
icon: '#pro/BookType'
---


## Definición de estructura de fuentes

En Univer, las fuentes se definen usando la interfaz `IFontConfig`:

| Propiedad | Tipo | Descripción |
| --- | --- | --- |
| `value` | `string` | El identificador único de la fuente, normalmente el valor preferido para la familia de fuentes CSS. |
| `label` | `string` | La clave de traducción para la internacionalización. Si esta clave no se especifica cuando se crea la instancia de Univer, el valor pasado se muestra directamente. |
| `category` | `'sans-serif' \| 'serif' \| 'monospace' \| 'display' \| 'handwriting'` | Categoría de fuente, utilizada para agrupar la interfaz de usuario (opcional). |

<Callout>
Univer no impide que los usuarios seleccionen fuentes desinstaladas. Si una fuente no está instalada en el cliente, el navegador la sustituirá por la fuente predeterminada.
</Callout>

{/* <AutoTypeTable
  name="IFontConfig"
  type={`
    export type { IFontConfig } from '@univerjs/ui'
  `}
/> */}

## Uso de fuentes personalizadas

Puede definir las opciones en el menú desplegable de fuentes configurando el parámetro `customFontFamily` para `UniverSheetsCorePreset` (modo preestablecido) o `UniverUIPlugin` (modo complemento).

<Tabs items={['Preset Mode', 'Plugin Mode']}>
  <Tab>
    ```typescript
    const { univerAPI } = createUniver({
      presets: [
        UniverSheetsCorePreset({
          container: 'app',
          customFontFamily: {
            list: [
              {
                value: 'PingFang SC',
                label: '苹方（简）',
                category: 'sans-serif',
              },
              {
                value: 'Helvetica Neue',
                label: 'Helvetica Neue',
                category: 'sans-serif',
              },
            ],
            // Whether to override the default font list, defaults to false
            override: true,
          },
        }),
      ],
    })
    ```
  </Tab>
  <Tab>
    ```typescript
    univer.registerPlugin(UniverUIPlugin, {
      container: 'app',
      customFontFamily: {
        list: [
          {
            value: 'PingFang SC',
            label: '苹方（简）',
            category: 'sans-serif',
          },
          {
            value: 'Helvetica Neue',
            label: 'Helvetica Neue',
            category: 'sans-serif',
          },
        ],
        // Whether to override the default font list, defaults to false
        override: true,
      },
    })
    ```
  </Tab>
</Tabs>

### API de Facade

Además de la configuración durante la inicialización, también puede registrar dinámicamente nuevas fuentes en tiempo de ejecución mediante la API de fachada. Esto es muy útil en escenarios donde los recursos de fuentes deben cargarse de forma asincrónica.

```typescript
univerAPI.addFonts([{
  value: 'PingFang SC',
  label: 'PingFang SC',
  category: 'sans-serif',
}, {
  value: 'Helvetica Neue',
  label: 'Helvetica Neue',
  category: 'sans-serif',
}])
```


Los tableros utilizan el documento compartido de Univer y el canal de fuentes de la interfaz de usuario para texto enriquecido, cuadros de texto y texto de formas. Registre la interfaz de usuario de Docs y los complementos de la interfaz de usuario compartidos antes del `UniverBoardsUIPlugin` para que la edición de texto tenga los servicios necesarios.

## Configure las fuentes del tema

El tema Board puede definir familias de fuentes para encabezados y cuerpos:

```ts
const currentThemeId = board.getThemeData().id
if (!currentThemeId || !board.setTheme(currentThemeId)) {
  throw new Error('Cannot apply Board theme')
}
```

## Fuentes de elementos de texto

Los elementos de texto también pueden llevar un estilo de texto explícito:

```ts
const label = board.insertText({
  id: 'label-1',
  left: 120,
  top: 96,
  width: 240,
  height: 64,
  text: 'Milestone',
  textStyle: {
    fontFamily: 'Inter',
    fontSize: 18,
    bold: true,
  },
})
if (!label) throw new Error('Cannot insert label')
```

Cargue fuentes web en la aplicación host antes de renderizar el Board para evitar cambios de diseño.
