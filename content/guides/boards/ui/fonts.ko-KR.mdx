---
title: 글꼴
icon: '#pro/BookType'
---

Boards는 서식 있는 텍스트, 텍스트 상자 및 도형 텍스트에 공통 Univer 문서 및 UI 글꼴 파이프라인을 사용합니다. 텍스트 편집에 필요한 서비스가 제공되도록 Docs UI와 공통 UI 플러그인을 `UniverBoardsUIPlugin`보다 먼저 등록하세요.

## 테마 글꼴 구성

Board 테마에서 제목과 본문 글꼴 모음을 정의할 수 있습니다.

```ts
await board.setTheme({
  name: 'Internal Theme',
  fontScheme: {
    heading: 'Inter',
    body: 'Inter',
  },
})
```

## 텍스트 요소 글꼴

텍스트 요소에 명시적인 텍스트 스타일을 지정할 수도 있습니다.

```ts
await board.addElement({
  id: 'label-1',
  type: 'text',
  transform: { left: 120, top: 96, width: 240, height: 64 },
  text: 'Milestone',
  textStyle: {
    fontFamily: 'Inter',
    fontSize: 18,
    bold: true,
  },
}, { pageId })
```

레이아웃 변경을 방지하려면 Board를 렌더링하기 전에 호스트 애플리케이션에서 웹 글꼴을 불러오세요.
