---
title: API de Facade
description: Aprende a habilitar la API Facade en una aplicación Univer Boards.
icon: 'PlugZap'
---

La API de Facade proporciona una capa orientada a la aplicación sobre los servicios y modelos de unidades de Univer. Úselo para operaciones comunes del tablero, como crear tableros, leer páginas activas, agregar elementos, actualizar contenedores y administrar carriles.

<Callout type="warning" title="Precaución">
Las mutaciones de Board son síncronas y devuelven `boolean` o un identificador anulable. Compruebe el valor devuelto.
</Callout>

## Habilite la entrada de fachada

Los tableros se distribuyen como complementos de Univer Pro. Importe la entrada de fachada para la capacidad del tablero utilizada por su aplicación:

```ts
import { FUniver } from '@univerjs/core/facade'
import '@univerjs-pro/boards/facade'
```

Cree el objeto API después de que se registren la instancia de Univer y los complementos necesarios:

```ts
const univerAPI = FUniver.newAPI(univer)
```

## Siguientes pasos

La extensión de Facade ya está habilitada. Continúa con:

- [API general](/guides/boards/features/core/general-api) para comandos, eventos, deshacer y rehacer, extensiones de UI, red, enumeraciones y utilidades compartidas.
- [Univer Boards API](/guides/boards/features/core/boards-api) para crear y leer Boards.
- [Elementos](/guides/boards/features/core/elements) y la [referencia de la facade de Boards](/reference/facade/boards) para las operaciones detalladas.
