---
title: Facade API
description: 瞭解如何在 Univer Boards 應用程式中使用 Facade API。
icon: '#pro/PlugZap'
---

Facade API 在 Univer 服務和單元模型之上提供一層供應用程式使用的介面。您可以使用它執行常見的 Board 操作，例如建立畫板、讀取作用中頁面、新增元素、更新容器及管理泳道。

<Callout type="warning" title="注意">
  部分 Facade API 採非同步方式運作。如需在變更資料後立即讀取，請使用 `await` 或 `.then()`。
</Callout>

## 啟用 Facade 進入點

Boards 以 Univer Pro 外掛程式的形式發行。請匯入應用程式所用 Board 功能的 Facade 進入點：

```ts
import { FUniver } from '@univerjs/core/facade'
import '@univerjs-pro/boards/facade'
```

建立 Univer 執行個體並註冊必要的外掛程式後，再建立 API 物件：

```ts
const univerAPI = FUniver.newAPI(univer)
```

## 建立及讀取畫板

使用 `createBoard` 透過 Facade 層建立 Board 單元。在較底層的設定程式碼中，請使用 `univer.createUnit(UniverInstanceType.UNIVER_BOARD, snapshot)` 建立單元。

```ts
const board = univerAPI.createBoard({
  id: 'board-demo',
  name: 'Board Demo',
})

const activeBoard = univerAPI.getActiveBoard()
const sameBoard = univerAPI.getBoard(board.getId())
```

## 操作頁面與元素

`FBoard` Facade 可讀取作用中頁面、查詢元素及更新元素資料，應用程式程式碼不必自行解析內部服務。

```ts
const pageId = board.getActivePageId()
const elements = board.getElements(pageId)

await board.addElement({
  id: 'note-1',
  type: 'text',
  transform: { left: 120, top: 80, width: 240, height: 80 },
  text: 'Planning note',
}, { pageId })

await board.updateElement('note-1', {
  patch: {
    transform: { left: 160, top: 120, width: 280, height: 96 },
  },
  pageId,
})
```

## 容器與泳道

請使用 Board Facade 變更元素所屬的容器。容器 API 會保留 Board 快照中的順序與父子關係。

```ts
const container = await board.createContainer({
  pageId,
  title: 'Backlog',
  transform: { left: 80, top: 80, width: 520, height: 360 },
})

await board.moveElementsToContainer(['note-1'], container.id, { pageId })
await board.fitContainerToContent(container.id, { pageId })
```

## 參考資料

- [Boards Facade 參考資料](/reference/facade/boards)
- [Board 資料模型](/guides/boards/model/board-data)
- [Board 元素](/guides/boards/model/board-elements)
