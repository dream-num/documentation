---
title: Facade API
description: 瞭解如何在 Univer Boards 應用程式中使用 Facade API。
icon: 'PlugZap'
---

Facade API 在 Univer 服務和單元模型之上提供一層供應用程式使用的介面。您可以使用它執行常見的 Board 操作，例如建立畫板、讀取作用中頁面、新增元素、更新容器及管理泳道。

<Callout type="warning" title="注意">
  Board 變更 API 是同步的，並回傳 `boolean` 或可為空的控制代碼。讀取結果前請檢查回傳值。
</Callout>

## 啟用 Facade 進入點

Boards 以 Univer SDK Pro 外掛程式的形式發行。請匯入應用程式所用 Board 功能的 Facade 進入點：

```ts
import { FUniver } from '@univerjs/core/facade'
import '@univerjs-pro/boards/facade'
```

建立 Univer 執行個體並註冊必要的外掛程式後，再建立 API 物件：

```ts
const univerAPI = FUniver.newAPI(univer)
```

## 後續閱讀

Facade 擴充功能現已啟用。接下來可以閱讀：

- [通用 API](/guides/boards/features/core/general-api)：瞭解共用的命令、事件、復原與重做、UI 擴充、網路、列舉和工具方法。
- [Univer Boards API](/guides/boards/features/core/boards-api)：瞭解如何建立和讀取 Board。
- [元素](/guides/boards/features/core/elements)和 [Boards facade 參考文件](/reference/facade/boards)：瞭解詳細的 Board 操作。
