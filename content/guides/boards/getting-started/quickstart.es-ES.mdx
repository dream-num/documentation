---
title: Inicio rápido
icon: '#pro/Rocket'
---

## Registrar Boards

El siguiente fragmento muestra la configuración específica de Board para un editor de navegador. Registre el tiempo de ejecución compartido de Univer, la licencia Pro, las dependencias de documentos y dibujos utilizadas por el texto enriquecido y los elementos de dibujo, luego registre el modelo de placa y los complementos de interfaz de usuario.

`@univerjs-pro/boards` crea la unidad `UNIVER_BOARD`. `@univerjs-pro/boards-ui` representa el lienzo y la interfaz de usuario de edición. Los elementos de tabla y mapa mental son complementos de funciones opcionales.

```ts
import { UniverBoardsPlugin } from '@univerjs-pro/boards'
import { UniverBoardsMindPlugin } from '@univerjs-pro/boards-mind'
import { UniverBoardsMindUIPlugin } from '@univerjs-pro/boards-mind-ui'
import BoardsMindUIEnUS from '@univerjs-pro/boards-mind-ui/locale/en-US'
import { UniverBoardsTablePlugin } from '@univerjs-pro/boards-table'
import { UniverBoardsTableUIPlugin } from '@univerjs-pro/boards-table-ui'
import BoardsTableUIEnUS from '@univerjs-pro/boards-table-ui/locale/en-US'
import { UniverBoardsUIPlugin } from '@univerjs-pro/boards-ui'
import BoardsUIEnUS from '@univerjs-pro/boards-ui/locale/en-US'
import { UniverInkUIPlugin } from '@univerjs-pro/ink-ui'
import InkUIEnUS from '@univerjs-pro/ink-ui/locale/en-US'
import { UniverLicensePlugin } from '@univerjs-pro/license'
import { UniverShapeEditorUIPlugin } from '@univerjs-pro/shape-editor-ui'
import { LocaleType, mergeLocales, Univer, UniverInstanceType } from '@univerjs/core'
import { UniverDocsPlugin } from '@univerjs/docs'
import { UniverDocsUIPlugin } from '@univerjs/docs-ui'
import { UniverDrawingPlugin } from '@univerjs/drawing'
import { UniverDrawingUIPlugin } from '@univerjs/drawing-ui'
import { UniverRenderEnginePlugin } from '@univerjs/engine-render'
import { UniverUIPlugin } from '@univerjs/ui'

import '@univerjs/design/lib/index.css'
import '@univerjs/ui/lib/index.css'
import '@univerjs/docs-ui/lib/index.css'
import '@univerjs/drawing-ui/lib/index.css'
import '@univerjs-pro/ink-ui/lib/index.css'
import '@univerjs-pro/boards-ui/lib/index.css'
import '@univerjs-pro/boards-table-ui/lib/index.css'
import '@univerjs-pro/boards-mind-ui/lib/index.css'

import '@univerjs-pro/boards/facade'

const univer = new Univer({
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: mergeLocales(
      InkUIEnUS,
      BoardsUIEnUS,
      BoardsTableUIEnUS,
      BoardsMindUIEnUS,
    ),
  },
})

univer.registerPlugin(UniverRenderEnginePlugin)
univer.registerPlugin(UniverUIPlugin, {
  container: 'app',
})

univer.registerPlugin(UniverDocsPlugin)
univer.registerPlugin(UniverDocsUIPlugin)
univer.registerPlugin(UniverDrawingPlugin)
univer.registerPlugin(UniverDrawingUIPlugin)
univer.registerPlugin(UniverInkUIPlugin)
univer.registerPlugin(UniverShapeEditorUIPlugin)

univer.registerPlugin(UniverLicensePlugin, {
  license: process.env.CLIENT_LICENSE_TEXT,
})

univer.registerPlugin(UniverBoardsPlugin)
univer.registerPlugin(UniverBoardsUIPlugin)
univer.registerPlugin(UniverBoardsTablePlugin)
univer.registerPlugin(UniverBoardsTableUIPlugin)
univer.registerPlugin(UniverBoardsMindPlugin)
univer.registerPlugin(UniverBoardsMindUIPlugin)

univer.createUnit(UniverInstanceType.UNIVER_BOARD, {})
```

## Configuración mínima

Para una compilación más pequeña, comience solo con el modelo de tablero y los paquetes de interfaz de usuario:

```ts
univer.registerPlugin(UniverBoardsPlugin)
univer.registerPlugin(UniverBoardsUIPlugin)
univer.createUnit(UniverInstanceType.UNIVER_BOARD, {})
```

Agregue paquetes de tabla, mapa mental, importación/exportación o colaboración solo cuando la superficie de su producto necesite esas capacidades.

## Configuración del lado del servidor

En Node.js, registre paquetes de modelos y omita los complementos de la interfaz de usuario del navegador:

```ts
univer.registerPlugin(UniverBoardsPlugin)
univer.registerPlugin(UniverBoardsTablePlugin)
univer.registerPlugin(UniverBoardsMindPlugin)
```

Este patrón es útil para el procesamiento de instantáneas, servicios de importación/exportación y backends de colaboración.
