---
title: Facade API
description: 了解如何在 Univer Boards 应用中使用 Facade API。
icon: '#pro/PlugZap'
---

Facade API 在 Univer 服务和单元模型之上提供了面向应用的接口层。你可以用它执行常见的 Board 操作，例如创建画板、读取当前页面、添加元素、更新容器和管理泳道。

<Callout type="warning" title="注意">
  部分 Facade API 是异步的。如果需要在更改数据后立即读取，请使用 `await` 或 `.then()`。
</Callout>

## 启用 Facade 入口

Boards 以 Univer Pro 插件的形式分发。请为应用所使用的 Board 功能导入对应的 Facade 入口：

```ts
import { FUniver } from '@univerjs/core/facade'
import '@univerjs-pro/boards/facade'
```

注册 Univer 实例和所需插件后，创建 API 对象：

```ts
const univerAPI = FUniver.newAPI(univer)
```

## 创建和读取画板

使用 `createBoard` 通过 Facade 层创建 Board 单元。在底层初始化代码中，请使用 `univer.createUnit(UniverInstanceType.UNIVER_BOARD, snapshot)` 创建该单元。

```ts
const board = univerAPI.createBoard({
  id: 'board-demo',
  name: 'Board Demo',
})

const activeBoard = univerAPI.getActiveBoard()
const sameBoard = univerAPI.getBoard(board.getId())
```

## 操作页面和元素

通过 `FBoard` Facade，可以读取当前页面、查找元素和更新元素数据，而无需在应用代码中解析内部服务。

```ts
const pageId = board.getActivePageId()
const elements = board.getElements(pageId)

await board.addElement({
  id: 'note-1',
  type: 'text',
  transform: { left: 120, top: 80, width: 240, height: 80 },
  text: 'Planning note',
}, { pageId })

await board.updateElement('note-1', {
  patch: {
    transform: { left: 160, top: 120, width: 280, height: 96 },
  },
  pageId,
})
```

## 容器和泳道

使用 Board Facade 更改元素所属的容器。容器 API 会保留 Board 快照中的排列顺序和父子关系。

```ts
const container = await board.createContainer({
  pageId,
  title: 'Backlog',
  transform: { left: 80, top: 80, width: 520, height: 360 },
})

await board.moveElementsToContainer(['note-1'], container.id, { pageId })
await board.fitContainerToContent(container.id, { pageId })
```

## 参考资料

- [Boards Facade 参考](/reference/facade/boards)
- [Board 数据模型](/guides/boards/model/board-data)
- [Board 元素](/guides/boards/model/board-elements)
