---
title: Facade API
description: Univer Boards アプリケーションで Facade API を使用する方法を説明します。
icon: 'PlugZap'
---

Facade API は、Univer のサービスとユニットモデルをアプリケーションから利用するためのレイヤーです。ボードの作成、アクティブなページの読み取り、要素の追加、コンテナの更新、スイムレーンの管理といった一般的なボード操作に使用します。

<Callout type="warning" title="注意">
  Board の変更 API は同期処理で、`boolean` または null 許容のハンドルを返します。戻り値を確認してください。
</Callout>

## Facade エントリを有効にする

Boards は Univer Pro プラグインとして配布されています。アプリケーションで使用するボード機能に対応した Facade エントリをインポートします。

```ts
import { FUniver } from '@univerjs/core/facade'
import '@univerjs-pro/boards/facade'
```

Univer インスタンスと必要なプラグインを登録した後に、API オブジェクトを作成します。

```ts
const univerAPI = FUniver.newAPI(univer)
```

## 次のステップ

Facade 拡張が有効になりました。次のガイドに進んでください。

- [General API](/guides/boards/features/core/general-api)では、共通のコマンド、イベント、元に戻す・やり直し、UI 拡張、ネットワーク、列挙型、ユーティリティを説明します。
- [Univer Boards API](/guides/boards/features/core/boards-api)では、Board の作成と取得を説明します。
- [要素](/guides/boards/features/core/elements)と [Boards facade リファレンス](/reference/facade/boards)では、詳しい Board 操作を確認できます。
