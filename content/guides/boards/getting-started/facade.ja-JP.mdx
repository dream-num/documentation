---
title: Facade API
description: Univer Boards アプリケーションで Facade API を使用する方法を説明します。
icon: 'PlugZap'
---

Facade API は、Univer のサービスとユニットモデルをアプリケーションから利用するためのレイヤーです。ボードの作成、アクティブなページの読み取り、要素の追加、コンテナの更新、スイムレーンの管理といった一般的なボード操作に使用します。

<Callout type="warning" title="注意">
  Board の変更 API は同期処理で、`boolean` または null 許容のハンドルを返します。戻り値を確認してください。
</Callout>

## Facade エントリを有効にする

Boards は Univer Pro プラグインとして配布されています。アプリケーションで使用するボード機能に対応した Facade エントリをインポートします。

```ts
import { FUniver } from '@univerjs/core/facade'
import '@univerjs-pro/boards/facade'
```

Univer インスタンスと必要なプラグインを登録した後に、API オブジェクトを作成します。

```ts
const univerAPI = FUniver.newAPI(univer)
```

## ボードを作成・読み取りする

Facade レイヤーを介してボードユニットを作成するには、`createBoard` を使用します。低レベルのセットアップコードでは、`univer.createUnit(UniverInstanceType.UNIVER_BOARD, snapshot)` を使用してユニットを作成します。

```ts
const board = univerAPI.createBoard({
  id: 'board-demo',
  name: 'Board Demo',
})

const activeBoard = univerAPI.getActiveBoard()
const sameBoard = univerAPI.getBoard(board.getId())
```

## ページと要素を操作する

`FBoard` Facade を使用すると、アプリケーションコードから内部サービスを解決せずに、アクティブなページの読み取り、要素の検索、要素データの更新を行えます。

```ts
const elements = board.getElements()

const note = board.insertText({
  id: 'note-1',
  left: 120,
  top: 80,
  width: 240,
  height: 80,
  text: 'Planning note',
})
if (!note) throw new Error('Cannot insert note')

board.setElementTransform('note-1', {
  left: 160,
  top: 120,
  width: 280,
  height: 96,
})
```

## コンテナとスイムレーン

コンテナに属する要素を変更するには、ボードの Facade を使用します。コンテナ API は、ボードのスナップショット内の順序と親子関係を維持します。

```ts
const containerId = 'backlog'
const containerCreated = board.createContainer({
  id: containerId,
  title: 'Backlog',
  left: 80,
  top: 80,
  width: 520,
  height: 360,
})
if (!containerCreated) {
  throw new Error('Cannot create container')
}

board.moveElementsToContainer(['note-1'], containerId)
board.fitContainerToContent(containerId)
```

## リファレンス

- [Boards Facade リファレンス](/reference/facade/boards)
- [ボードのデータモデル](/guides/boards/model/board-data)
- [ボードの要素](/guides/boards/model/board-elements)
