---
title: Univer Boards
description: Univer Boards 提供白板、图形、表格元素和思维导图使用的画布工作区。
icon: BoardsMultiIcon
---

## Univer Boards 是什么

Univer Boards 是 Univer Pro 中的画布产品。它适合用来做白板、流程图、便签墙、连接线图、嵌入式表格和思维导图。换句话说，当内容不再是单元格、页面或幻灯片，而是需要在一个大画布上自由摆放时，就应该看 Boards。

Boards 在运行时是独立的 Univer unit。应用需要创建 `UniverInstanceType.UNIVER_BOARD`，再按需要注册 Board 模型、画布 UI、表格元素和思维导图插件。

## 什么时候使用 Boards

如果你的应用需要一个空间化的编辑区域，比如头脑风暴、流程梳理、项目规划、复盘白板或产品结构图，可以从 Boards 开始。如果核心数据是结构化记录和多视图管理，请看 [Univer Bases](/guides/bases)。如果核心对象是电子表格，请看 [Univer Sheets](/guides/sheets)。

## 安装包

Boards 的核心运行时分成模型包和 UI 包。表格元素、思维导图元素是建立在 Boards 之上的可选能力。

```bash
pnpm add @univerjs-pro/boards @univerjs-pro/boards-ui
pnpm add @univerjs-pro/boards-table @univerjs-pro/boards-table-ui
pnpm add @univerjs-pro/boards-mind @univerjs-pro/boards-mind-ui
```

## 包结构

- `@univerjs-pro/boards`：Board 数据模型、元素 schema、命令、mutation、适配服务、Facade 入口与 `UniverBoardsPlugin`
- `@univerjs-pro/boards-ui`：Board 画布 UI、视口、选区、形状编辑、连接线、便签、墨迹交互、工具栏、右键菜单与 `UniverBoardsUIPlugin`
- `@univerjs-pro/boards-table`：Board 表格元素集成与 `UniverBoardsTablePlugin`
- `@univerjs-pro/boards-table-ui`：表格元素 UI、渲染集成、多语言入口与 `UniverBoardsTableUIPlugin`
- `@univerjs-pro/boards-mind`：思维导图元素集成与 `UniverBoardsMindPlugin`
- `@univerjs-pro/boards-mind-ui`：思维导图 UI 集成与 `UniverBoardsMindUIPlugin`

## 注册插件

下面只展示 Boards 相关的注册代码。完整应用还需要 Univer core、渲染引擎、UI、License、Ink 等通用插件。

```ts
import { UniverBoardsPlugin } from '@univerjs-pro/boards'
import { UniverBoardsMindPlugin } from '@univerjs-pro/boards-mind'
import { UniverBoardsMindUIPlugin } from '@univerjs-pro/boards-mind-ui'
import { UniverBoardsTablePlugin } from '@univerjs-pro/boards-table'
import { UniverBoardsTableUIPlugin } from '@univerjs-pro/boards-table-ui'
import { UniverBoardsUIPlugin } from '@univerjs-pro/boards-ui'
import { UniverInstanceType } from '@univerjs/core'
import '@univerjs-pro/boards/facade'

univer.registerPlugin(UniverBoardsPlugin)
univer.registerPlugin(UniverBoardsUIPlugin)
univer.registerPlugin(UniverBoardsMindPlugin)
univer.registerPlugin(UniverBoardsMindUIPlugin)
univer.registerPlugin(UniverBoardsTablePlugin)
univer.registerPlugin(UniverBoardsTableUIPlugin)

univer.createUnit(UniverInstanceType.UNIVER_BOARD, {})
```

## 继续阅读

- 授权和服务端相关配置请看 [Univer Pro](/guides/pro)。
- 想理解 unit、插件和渲染模块之间的关系，可以先读 [架构实践](/guides/recipes/architecture/univer)。
- 集成时可以先根据上面的包结构决定是否启用表格元素和思维导图元素。
