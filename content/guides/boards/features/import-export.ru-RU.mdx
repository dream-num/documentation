---
title: Импорт и экспорт
icon: '#pro/FolderSync'
---

Доски можно использовать в сценариях импорта и экспорта Univer Pro с помощью соответствующих адаптеров. Поддерживаемые форматы файлов зависят от сервисов обмена Univer Pro, зарегистрированных в вашем приложении.

## Сначала зарегистрируйте среду выполнения досок

```ts
univer.registerPlugin(UniverBoardsPlugin)
univer.registerPlugin(UniverBoardsUIPlugin)
```

Перед импортом данных, которые могут содержать дополнительные типы элементов, зарегистрируйте соответствующие плагины:

```ts
univer.registerPlugin(UniverBoardsTablePlugin)
univer.registerPlugin(UniverBoardsTableUIPlugin)
univer.registerPlugin(UniverBoardsMindPlugin)
univer.registerPlugin(UniverBoardsMindUIPlugin)
```

## Передача снимка

Чтобы сохранять данные на уровне приложения, сохраняйте и восстанавливайте снимок доски:

```ts
const board = univerAPI.getActiveBoard()
const snapshot = board.getData()

univer.createUnit(UniverInstanceType.UNIVER_BOARD, snapshot)
```

## Вставка черновика

Если в результате импорта получен черновик доски, вставьте его через фасад. Это обеспечит согласованное применение порядка элементов, ресурсов и принадлежности к контейнерам.

```ts
await board.insertDraft(draft, {
  pageId: board.getActivePageId(),
  position: { x: 160, y: 120 },
})
```

Требования к лицензированию и развёртыванию приведены на странице [Univer Pro](/guides/pro).
