---
title: Colaboración
icon: 'CloudCheck'
---

<MetaData
  lang="es-ES"
  meta={{
    preset: [],
    plugins: [
      {
        client: '@univerjs-pro/collaboration',
      },
      {
        client: '@univerjs-pro/collaboration-client',
        facade: '@univerjs-pro/collaboration-client/facade',
        locale: '@univerjs-pro/collaboration-client/locale/es-ES',
      },
      {
        client: '@univerjs-pro/collaboration-client-ui',
        facade: '@univerjs-pro/collaboration-client-ui/facade',
        locale: '@univerjs-pro/collaboration-client-ui/locale/es-ES',
        style: '@univerjs-pro/collaboration-client-ui/lib/index.css',
      },
    ],
    server: true,
  }}
/>

Permite que varios usuarios editen el mismo tablero y vean los cambios de los demás. Completa la [integración de colaboración](/server/collaboration/browser-integration) para conectar el backend, la autenticación, los permisos y el almacenamiento; después configura la [licencia](/server/license).

## Configurar y cargar un documento

Añade la configuración del cliente Collaboration a tu editor Boards existente siguiendo el [inicio rápido de Office](/server/collaboration/quick-start). Ese ejemplo usa Sheets; tu backend debe crear y guardar un documento Boards y devolver su ID al navegador.

```ts
import '@univerjs-pro/boards/facade'
import '@univerjs-pro/collaboration-client/facade'

const document = await univerAPI.getCollaboration().loadBoardAsync('your-document-id')
if (!document) throw new Error('Document could not be loaded')
```

Reutiliza el mismo ID al volver a abrir o compartir el documento. No crees un documento local vacío en lugar de cargar el documento del servidor.

## Verificar la sincronización y los permisos

Abre el mismo ID en dos sesiones independientes del navegador. Edita texto o mueve un elemento en una sesión y comprueba la otra. Verifica que el contenido persista tras recargar la página, reconectar y reiniciar el servicio. Usa cuentas de solo lectura y sin autorización para comprobar que el servidor rechaza las lecturas y escrituras no permitidas.

El [historial de edición](/guides/boards/features/edit-history) y la [importación y exportación](/guides/boards/features/import-export) requieren una configuración independiente; la colaboración no los activa automáticamente.
