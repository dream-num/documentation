---
title: Web Workers
icon: CircuitBoard
---

Docs 可透過 `UniverDocsLayoutWorkerPlugin` 將文件排版放到 Web Worker 執行。在 Docs 核心外掛之後註冊它，使用 `workerFactory` 為排版服務建立新的 Worker。

先完成[安裝與基本使用](/guides/docs/getting-started/installation)。以下程式碼用來擴充既有設定。請在建立或載入內容之前註冊外掛；已有的外掛註冊應直接修改，不要重複註冊。

## 主執行緒

```typescript title="main.ts"
import { UniverDocsLayoutWorkerPlugin } from '@univerjs/docs'

univer.registerPlugin(UniverDocsLayoutWorkerPlugin, {
  workerFactory: () => new Worker(new URL('./worker.ts', import.meta.url), { type: 'module' }),
})
```

## Worker 入口

```typescript title="worker.ts"
import { startDocsLayoutWorker } from '@univerjs/docs'

startDocsLayoutWorker()
```

使用模組類型的 Worker 與獨立入口檔案。Worker 與主執行緒的套件版本須一致，渲染與使用者互動仍在主執行緒執行。
