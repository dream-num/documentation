---
title: 快速開始
icon: Rocket
---

如果你想盡快開始體驗 Univer，那麼你來對地方了！這個頁面將帶你在幾分鐘內從零搭建一個可以執行的 Univer 應用。

## 線上體驗

你可以線上體驗 Univer，無需安裝任何東西。只需點選下面的連結，即可立即開始使用 Univer。

<Cards>
  <Card
    title={
      <span>
        📝 Univer Doc<sup>RC</sup>
      </span>
    }
    href="https://stackblitz.com/~/github.com/awesome-univer/docs-vite-demo"
    target="_blank"
  />
</Cards>

## 從模板建立

如果你想在本地搭建 Univer 應用，可以使用以下命令從模板建立一個新的 Univer 應用。

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    執行以下命令需要裝置安裝有 `nodejs >= 18`，`npm >= 8` 和 `git`。

    ```shell
    npx degit dream-num/univer-doc-start-kit univer-doc-start-kit
    cd univer-doc-start-kit
    npm install
    npm run dev
    ```

  </Tab>
  <Tab>
    執行以下命令需要裝置安裝有 `nodejs >= 18`，`pnpm >= 7` 和 `git`。

    ```shell
    pnpm dlx degit dream-num/univer-doc-start-kit univer-doc-start-kit
    cd univer-doc-start-kit
    pnpm install
    pnpm dev
    ```

  </Tab>
</Tabs>

至此，你已經成功建立了屬於你的第一個 Univer 專案，並且體驗了 Univer 的一些功能。

## 基本概念 [#basic-concepts]

在進一步深入瞭解 Univer 之前，請閱讀本篇文件以瞭解 Univer 的基本概念，將有助於你更順利地使用 Univer。

基本概念包括：

- 外掛 Plugin
- 快照 Snapshot
- 命令 Command
- Facade API

### 外掛

Univer 的功能均由外掛提供，外掛是 Univer 的基本組成單元，多個外掛組合在一起構成了 Univer 應用。

外掛的職責包括：

1. 實現功能，例如 `@univerjs/sheets-filter` 提供篩選功能
2. 提供某種基礎能力，例如 `@univerjs/engine-render` 提供渲染能力

你可以按照自己的需要組合 Univer 提供的外掛，客製出符合你的需求的 Univer 應用，避免載入多餘程式碼。你也可以透過編寫外掛來擴充 Univer 的功能，滿足個別化需求。

#### 相關連結

1. [編寫一個 CSV 匯入外掛](/blog/csv-import-plugin) 瞭解如何編寫一個外掛
2. [Univer 架構](/blog/univer) 詳細瞭解 Univer 的架構設計

### 快照

你需要在 Univer 中載入快照來載入文件，每種型別的文件都提供了自己的快照格式，請參考以上 API 文件瞭解如何編寫符合格式要求的快照：

1. 電子表格的快照格式是 [`IWorkbookData`](/guides/sheets/model/workbook-data#iworkbookdata)
2. 文件的快照格式是 [`IDocumentData`](/guides/docs/model/document-data#idocumentdata)

功能外掛可以透過 `resources` 欄位來儲存資源，例如圖片等等，可參考 [外掛自訂模型](/blog/custom-model)。

<Callout type="warning" title="注意事項">
  注意！Univer 的快照僅用於儲存資料，在執行時，它並不會反映最新的文件狀態！如果你想把文件狀態儲存為快照，請使用 Facade
  API 上的儲存快照的方法。
</Callout>

### 命令

在 Univer 中，對於資料的修改都需要透過命令執行，基於命令的方式可以更好地追蹤值的變化，並且能夠實現撤銷、重做和協同編輯等功能，以及處理功能之間複雜的關聯邏輯等等。

如果你想透過命令的方式修改文件資料，請參考 [如何查詢命令 ID](/blog/find-the-command-id)。如果你選擇透過 Facade API 來修改文件資料，實際上 Facade API 在背後會為你準備參數並執行命令。

<Callout type="warning" title="注意事項">
  **請不要直接修改 snapshot！** 這不會生效，不會觸發檢視或者 UI 的更新，請透過命令或者 Facade API 來修改資料。
</Callout>

### Facade API

由於辦公應用的複雜度非常高，Univer 的架構相應地也非常複雜，為了讓開發者更容易地使用 Univer，我們提供了 Facade API，它是對 Univer 第一方外掛的封裝，提供了更簡單的介面，讓開發者可以更容易地使用 Univer。

目前 Facade API 在不斷完善中，如果你有任何建議或者需求，歡迎在 GitHub 上提 issue。

Facade API 是對底層細節的抽象，雖然它簡化了常見任務，但實現複雜功能時可能需要你了解 Univer 的架構並直接使用底層 API。

#### 相關連結

1. [Facade API 介紹](/guides/docs/getting-started/facade)
2. [Facade API 常用方法](/guides/docs/features/core/general-api)
3. [Univer Docs API 常用方法](/guides/docs/features/core/docs-api)

## 預設模式與外掛模式

Univer 採用了外掛化的設計理念，旨在為開發者提供一個靈活、可擴充的電子文件應用框架。透過外掛化設計，Univer 可以輕鬆地整合各種功能模組，從而滿足不同使用者的需求。但外掛化的設計會增加應用的複雜性，尤其是對於首次接觸 Univer 的開發者來說。

為此，Univer 提供了**外掛模式**和**預設模式**兩種方式幫助你快速整合和使用 Univer。所謂**預設**實際上就是一組預先配置好的外掛組合，因此在無需進行復雜擴充的情況下，兩者所提供的能力是相同的。

<Callout type="warning" title="注意事項">
  - **依賴版本需一致**：無論是使用預設模式還是外掛模式的方式，你都必須保證所有依賴的版本號一致。 -
  **謹慎混用外掛和預設**：如果一個外掛已經被包含在某個預設中，那麼在使用預設時就不需要再單獨引入該外掛了。否則可能會導致外掛衝突或功能異常。
</Callout>

預設模式與外掛模式的區別：

| 外掛模式                       | 預設模式                                           |
| ------------------------------ | -------------------------------------------------- |
| 需要手動引入對應的 facade 包   | 無需手動引入任何 facade 包                         |
| 需要注意同型別功能外掛註冊順序 | 同功能被包含在預設中無需關注註冊順序               |
| 支援按需懶載入                 | 設定的預設會在初始化時同步載入             |
