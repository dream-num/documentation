---
title: Démarrage rapide
icon: Rocket
---

Si vous souhaitez commencer à utiliser Univer aussi rapidement que possible, vous êtes au bon endroit ! Cette page vous guidera dans la création d’une application Univer fonctionnelle en quelques minutes seulement.

## Essayer en ligne

Vous pouvez essayer Univer en ligne sans rien installer. Cliquez simplement sur le lien ci-dessous pour commencer immédiatement.

<Cards>
  <Card
    title={<span>📝 Univer Doc<sup>RC</sup></span>}
    href="https://stackblitz.com/~/github.com/awesome-univer/docs-vite-demo"
    target="_blank"
  />
</Cards>

## Kit de démarrage

Si vous préférez travailler dans un environnement de développement local, vous pouvez utiliser l’un des kits de démarrage suivants pour créer une nouvelle application Univer en quelques secondes.

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    Pour exécuter la commande suivante, `nodejs >= 18`, `npm >= 8` et `git` doivent être installés sur votre appareil.

    ```shell
    npx degit dream-num/univer-doc-start-kit univer-doc-start-kit
    cd univer-doc-start-kit
    npm install
    npm run dev
    ```
  </Tab>
  <Tab>
    Pour exécuter la commande suivante, `nodejs >= 18`, `pnpm >= 7` et `git` doivent être installés sur votre appareil.

    ```shell
    pnpm dlx degit dream-num/univer-doc-start-kit univer-doc-start-kit
    cd univer-doc-start-kit
    pnpm install
    pnpm dev
    ```
  </Tab>
</Tabs>

Bravo pour vos premiers pas avec Univer ! Vous avez créé votre premier projet et découvert un aperçu des possibilités offertes par Univer.

## Concepts de base [#basic-concepts]

Avant d’aller plus loin avec Univer, lisez cette page pour en comprendre les concepts de base. Ils vous aideront à utiliser Univer plus facilement.

Ces concepts sont les suivants :

- Plugin
- Snapshot
- Commande
- Facade API

## Plugins

Toutes les fonctionnalités d’Univer sont fournies par des plugins, qui constituent ses éléments de base. Plusieurs plugins combinés forment une application Univer.

Les plugins ont notamment pour rôle :

1. D’implémenter des fonctionnalités. Par exemple, `@univerjs/sheets-filter` fournit la fonction de filtrage.
2. De fournir des capacités fondamentales. Par exemple, `@univerjs/engine-render` fournit les capacités de rendu.

Vous pouvez combiner les plugins d’Univer afin de personnaliser une application selon vos besoins, sans charger de code inutile. Vous pouvez également étendre les fonctionnalités d’Univer en écrivant vos propres plugins.

### Liens connexes

1. Consultez [Écrire un plugin d’importation CSV](/guides/recipes/practices/csv-import-plugin) pour apprendre à écrire un plugin.
2. Consultez [Architecture d’Univer](/guides/recipes/architecture/univer) pour en savoir plus sur la conception de l’architecture d’Univer.

## Snapshot

Pour charger un document dans Univer, vous devez charger un snapshot. Chaque type de document possède son propre format de snapshot. Reportez-vous à la documentation de l’API indiquée ci-dessus pour apprendre à créer un snapshot conforme au format requis :

1. Le format de snapshot d’une feuille de calcul est [`IWorkbookData`](/guides/sheets/model/workbook-data#iworkbookdata).
2. Le format de snapshot d’un document est [`IDocumentData`](/guides/docs/model/document-data#idocumentdata).

Les plugins fonctionnels peuvent stocker des ressources, telles que des images, dans le champ `resources`. Pour en savoir plus, consultez [Modèle personnalisé d’un plugin](/guides/recipes/tutorials/custom-model).

<Callout type="warning" title="Attention">
  Le snapshot d’Univer sert uniquement à stocker des données. Il ne reflète pas le dernier état du document pendant l’exécution ! Pour enregistrer l’état du document sous forme de snapshot, utilisez la méthode d’enregistrement du snapshot de la Facade API.
</Callout>

## Commande

Dans Univer, toute modification des données doit être effectuée au moyen de commandes. Cette approche permet de mieux suivre les changements de valeurs, d’implémenter des fonctions telles que l’annulation, le rétablissement et l’édition collaborative, ainsi que de gérer les interactions complexes entre les fonctionnalités.

Pour modifier les données d’un document au moyen de commandes, consultez [Comment trouver l’identifiant d’une commande](/guides/recipes/practices/find-the-command-id). Si vous choisissez d’utiliser la Facade API, celle-ci prépare en réalité les paramètres et exécute les commandes en arrière-plan.

<Callout type="warning" title="Attention">
  **Ne modifiez pas directement le snapshot !** Vos modifications ne seront pas appliquées et ne déclencheront aucune mise à jour des vues ou de l’interface utilisateur. Modifiez les données au moyen de commandes ou de la Facade API.
</Callout>

## Facade API

Les applications bureautiques étant très complexes, l’architecture d’Univer l’est également. Pour faciliter son utilisation par les développeurs, nous proposons la Facade API, une couche d’abstraction autour des plugins officiels d’Univer qui fournit des interfaces plus simples.

La Facade API est activement améliorée. Si vous avez des questions ou des besoins particuliers, ouvrez une issue sur GitHub.

La Facade API est une API de haut niveau qui masque les détails de l’implémentation sous-jacente. Elle simplifie les tâches courantes, mais les fonctionnalités complexes peuvent nécessiter de comprendre l’architecture d’Univer et d’utiliser directement l’API de bas niveau.

#### Liens connexes

1. [Utiliser la Facade API](/guides/docs/getting-started/facade)
2. [API générale](/guides/docs/features/core/general-api)
4. [API Univer Docs](/guides/docs/features/core/docs-api)
