---
title: Hyperlink
icon: Link
---

<MetaData
  lang="de-DE"
  meta={{
    preset: [
      {
        client: '@univerjs/preset-docs-hyper-link',
        locale: '@univerjs/preset-docs-hyper-link/locales/de-DE',
        style: '@univerjs/preset-docs-hyper-link/lib/index.css',
      },
    ],
    plugins: [
      {
        client: '@univerjs/docs-hyper-link',
      },
      {
        client: '@univerjs/docs-hyper-link-ui',
        locale: '@univerjs/docs-hyper-link-ui/locale/de-DE',
        style: '@univerjs/docs-hyper-link-ui/lib/index.css',
      },
    ],
    server: false,
  }}
/>

Hyperlinks werden verwendet, um schnell zu navigieren und auf externe Webseiten, E-Mail-Adressen und andere Inhalte in Dokumenten zuzugreifen.

## Preset-Modus

### Installation

```package-install
npm install @univerjs/preset-docs-hyper-link
```

### Nutzung

```typescript
import { UniverDocsCorePreset } from '@univerjs/preset-docs-core'
import UniverPresetDocsCoreDeDE from '@univerjs/preset-docs-core/locales/de-DE'
import { UniverDocsHyperLinkPreset } from '@univerjs/preset-docs-hyper-link' // [!code ++]
import UniverPresetDocsHyperLinkDeDE from '@univerjs/preset-docs-hyper-link/locales/de-DE' // [!code ++]
import { createUniver, LocaleType, mergeLocales } from '@univerjs/presets'

import '@univerjs/preset-docs-core/lib/index.css'
import '@univerjs/preset-docs-hyper-link/lib/index.css' // [!code ++]

const { univerAPI } = createUniver({
  locale: LocaleType.DE_DE,
  locales: {
    [LocaleType.DE_DE]: mergeLocales(
      UniverPresetDocsCoreDeDE,
      UniverPresetDocsHyperLinkDeDE, // [!code ++]
    ),
  },
  presets: [
    UniverDocsCorePreset(),
    UniverDocsHyperLinkPreset(), // [!code ++]
  ],
})
```

{/* ### Presets and Configuration */}

## Plugin-Modus

### Installation

```package-install
npm install @univerjs/docs-hyper-link @univerjs/docs-hyper-link-ui
```

### Nutzung

```typescript
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'
import { UniverDocsHyperLinkPlugin } from '@univerjs/docs-hyper-link' // [!code ++]
import { UniverDocsHyperLinkUIPlugin } from '@univerjs/docs-hyper-link-ui' // [!code ++]
import DocsHyperLinkUIDeDE from '@univerjs/docs-hyper-link-ui/locale/de-DE' // [!code ++]

import '@univerjs/docs-hyper-link-ui/lib/index.css' // [!code ++]

const univer = new Univer({
  locale: LocaleType.DE_DE,
  locales: {
    [LocaleType.DE_DE]: mergeLocales(
      DocsHyperLinkUIDeDE, // [!code ++]
    ),
  },
})

univer.registerPlugin(UniverDocsHyperLinkPlugin) // [!code ++]
univer.registerPlugin(UniverDocsHyperLinkUIPlugin) // [!code ++]
```

{/* ### Plugins and Configuration */}

## Links mit der Facade API erstellen und bearbeiten

`link(text, url)` fügt verlinkten Text an und speichert dessen Bereich; Zeichenpositionen müssen Sie nicht selbst berechnen. Mit der von `getLinks()` gelieferten Bereichs-ID können Sie das Ziel später ändern.

```ts
const richText = univerAPI
  .newRichText()
  .text('Read ')
  .link('Univer documentation', 'https://docs.univer.ai')
  .text(' for details.')

const [link] = richText.getLinks()
if (!link) throw new Error('Link was not created')

richText.updateLink(link.rangeId, 'https://docs.univer.ai/guides')
```

`removeLink(link.rangeId)` entfernt das Linkziel, lässt den sichtbaren Text jedoch unverändert.
