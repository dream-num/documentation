---
title: Quick Insert
icon: Plus
---

<MetaData
  lang="en-US"
  meta={{
    plugins: [{
      client: '@univerjs/docs-quick-insert-ui',
      locale: '@univerjs/docs-quick-insert-ui/locale/en-US',
      style: '@univerjs/docs-quick-insert-ui/lib/index.css',
    }],
    server: false,
  }}
/>

Quick Insert bietet ein Menü im Schrägstrich-Befehlsstil zum Einfügen von Inhalten. Geben Sie `/` am Anfang einer leeren Zeile ein, um das Menü zu öffnen und schnell Text, Listen, Trennblätter, Tabellen, Bilder und mehr einzufügen. Diese Funktion unterstützt derzeit nur den Plugin-Modus.

## Interaktionen

- Geben Sie `/` am Anfang einer Zeile ein, um das Menü zu öffnen
- Eine Schaltfläche `+` erscheint links von einem leeren Absatz; Klicken Sie darauf, um alle verfügbaren Menüs zu öffnen.
- Zum Filtern eingeben; Verwenden Sie ↑/↓ zum Navigieren, Enter zum Einfügen, Esc zum Schließen

## Plugin-Modus

### Installation

```package-install
npm install @univerjs/docs-quick-insert-ui
```

### Verwendung

```typescript
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'
import { UniverDocsQuickInsertUIPlugin } from '@univerjs/docs-quick-insert-ui' // [!code ++]
import DocsQuickInsertUIEnUS from '@univerjs/docs-quick-insert-ui/locale/en-US' // [!code ++]

import '@univerjs/docs-quick-insert-ui/lib/index.css' // [!code ++]

const univer = new Univer({
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: mergeLocales(
      DocsQuickInsertUIEnUS, // [!code ++]
    ),
  },
})

univer.registerPlugin(UniverDocsQuickInsertUIPlugin) // [!code ++]
```
