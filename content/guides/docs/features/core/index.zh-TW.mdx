---
title: 核心功能
---

<MetaData
  lang="zh-TW"
  meta={{
    preset: [{
      client: '@univerjs/preset-docs-core',
      locale: '@univerjs/preset-docs-core/locales/zh-CN',
      style: '@univerjs/preset-docs-core/lib/index.css',
    }],
    plugins: [{
      client: '@univerjs/core',
    }, {
      client: '@univerjs/engine-formula',
    }, {
      client: '@univerjs/engine-render',
    }, {
      client: '@univerjs/design',
      locale: '@univerjs/design/locale/zh-CN',
      style: '@univerjs/design/lib/index.css',
    }, {
      client: '@univerjs/ui',
      locale: '@univerjs/ui/locale/zh-CN',
      style: '@univerjs/ui/lib/index.css',
    }, {
      client: '@univerjs/docs',
    }, {
      client: '@univerjs/docs-ui',
      locale: '@univerjs/docs-ui/locale/zh-CN',
      style: '@univerjs/docs-ui/lib/index.css',
      facade: '@univerjs/docs-ui/facade',
    }],
    server: false,
  }}
/>

Univer Docs 的核心功能包含了文件編輯器、公式引擎、渲染引擎、設計系統和 UI 組件庫等。它們共同構成了一個強大的文件處理平台，支持多種文件格式和複雜的文件操作。

## 特性

Univer Docs 的核心功能包括：

- **文件渲染**：支持 Univer Docs 的渲染和游標、選區的繪製。
- **文件排版**：支持全文排版，包括段落設置，段前後間距、段落對齊、縮進和懸掛等，也包括段落中的排版能力，標點擠壓、標點懸掛、字偶間距、西文斷詞等。
- **公式計算**：支持 Univer 公式的添加和計算（開發中）。
- **表格能力**：支持插入基本表格和表格的編輯，新增、刪除行類操作，編輯儲存格等。
- **文件風格**：Univer Docs 不僅支持類似 Microsoft Word 的*傳統文件風格*（包含分頁、頁眉頁腳等），也支持類似 Notion 的*現代文件風格*。在現代文件風格中，無需分頁，內容可以無限擴展，支持多種官方或第三方塊級元素（如文本、圖片、表格等）的混合排版（計劃中）。
- **頁眉頁腳**：支持插入頁眉頁腳和編輯頁眉頁腳內容，可以區分首頁及奇偶頁等，僅傳統文件支持。
- **列表功能**：支持有序、無序列表和任務列表的插入和編輯，支持列表的縮進和反縮進等。
- **插件系統**：支持插件系統，可以自定義插件，如公式插件、圖片插件、圖表插件等，這也是 Univer 核心架構能力之一。

## 預設模式

### 安裝

```package-install
npm install @univerjs/presets @univerjs/preset-docs-core
```

### 使用

```typescript
import { UniverDocsCorePreset } from '@univerjs/preset-docs-core'
import UniverPresetDocsCoreZhCN from '@univerjs/preset-docs-core/locales/zh-CN'
import { createUniver, LocaleType, mergeLocales } from '@univerjs/presets'

import '@univerjs/preset-docs-core/lib/index.css'

const { univerAPI } = createUniver({
  locale: LocaleType.ZH_CN,
  locales: {
    [LocaleType.ZH_CN]: mergeLocales(
      UniverPresetDocsCoreZhCN,
    ),
  },
  presets: [
    UniverDocsCorePreset(),
  ],
})

univerAPI.createUniverDoc({})
```

### 預設與配置

#### @univerjs/presets

本預設提供了創建 Univer 應用所需的 `createUniver` 方法，以及暴露了 **@univerjs/core** 和 **@univerjs/design** 的所有方法和對象。

`createUniver` 方法接受一個配置對象，包含主題、語言、預設等選項，返回一個 `Univer` 實例 `univer` 和用於調用 Facade API 的 `FUniver` 實例 `univerAPI`。

```typescript
interface CreateUniverParameter {
  // Univer 實例的主題，默認使用 defaultTheme
  theme: IStyleSheet
  // 是否使用暗黑模式，默認為 false。
  darkMode?: boolean
  // Univer 實例的語言環境
  locale: LocaleType
  // 要使用的語言環境對象，鍵為語言環境類型，值為對應的語言環境對象
  locales: ILocales
  // Univer 實例的日誌級別
  logLevel?: LogLevel
  // 要使用的預設列表。
  presets: IPresets[]
  // 要使用的插件列表。
  plugins: IPlugins[]
}
```

參考：[`createUniver`](/reference/methods/create-univer)

#### @univerjs/preset-docs-core

本預設是 Univer Docs 的核心預設，包含了文件編輯器、公式引擎、渲染引擎等電子文件必要的核心功能。

```typescript
interface IUniverDocsCorePresetConfig {
  // 容器元素，可以是字串或者 DOM 元素
  container?: string | HTMLElement
  // 是否顯示頭部
  header?: boolean
  // 是否顯示頭部工具欄
  toolbar?: boolean
  // 頭部工具欄的類型。default: 默認緊湊模式；classic: 經典模式；simple：不分組模式
  ribbonType?: RibbonType
  // 是否顯示底部。
  footer?: boolean
  // 是否顯示右鍵菜單
  contextMenu?: boolean
  // 是否禁用自動聚焦
  disableAutoFocus?: true
  // 依賴注入的覆蓋配置
  override?: DependencyOverride
  // 菜單配置
  menu?: MenuConfig

  // ... 更多配置項
}
```

其中，`menu` 配置項可以用於簡單地設置某些特定菜單項的顯示或隱藏等狀態：

```typescript
UniverDocsCorePreset({
  menu: {
    'sheet.command.set-range-bold': {
      hidden: true,
    },
    'sheet.command.set-range-italic': {
      hidden: true,
      disabled: true,
    },
  },
})
```

這裡的 `'sheet.command.set-range-bold'` 和 `'sheet.command.set-range-italic'` 是命令的名稱，表示對應的菜單項。

參考：[@univerjs/preset-docs-core](/reference/packages/presets/univerjs/preset-docs-core)

## 插件模式

### 安裝

```package-install
npm install @univerjs/core @univerjs/engine-render @univerjs/engine-formula @univerjs/design @univerjs/ui @univerjs/docs @univerjs/docs-ui
```

### 使用

```typescript
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'
import DesignZhCN from '@univerjs/design/locale/zh-CN'
import { UniverDocsPlugin } from '@univerjs/docs'
import { UniverDocsUIPlugin } from '@univerjs/docs-ui'
import DocsUIZhCN from '@univerjs/docs-ui/lib/locale/zh-CN'
import { UniverFormulaEnginePlugin } from '@univerjs/engine-formula'
import { UniverRenderEnginePlugin } from '@univerjs/engine-render'
import { UniverUIPlugin } from '@univerjs/ui'
import UIZhCN from '@univerjs/ui/locale/zh-CN'

import '@univerjs/docs-ui/facade'

import '@univerjs/design/lib/index.css'
import '@univerjs/ui/lib/index.css'
import '@univerjs/docs-ui/lib/index.css'

const univer = new Univer({
  locale: LocaleType.ZH_CN,
  locales: {
    [LocaleType.ZH_CN]: mergeLocales(
      DesignZhCN,
      UIZhCN,
      DocsUIZhCN,
    ),
  },
})

univer.registerPlugin(UniverRenderEnginePlugin)
univer.registerPlugin(UniverFormulaEnginePlugin)
univer.registerPlugin(UniverUIPlugin, {
  container: 'app',
})
univer.registerPlugin(UniverDocsPlugin)
univer.registerPlugin(UniverDocsUIPlugin)

univer.createUnit(UniverInstanceType.UNIVER_DOC, {})
```

### 插件與配置

#### @univerjs/core

本插件是 Univer 的核心庫，提供了創建 Univer 實例、主題、語言等基礎功能。

```typescript
interface UniverParameter {
  // Univer 實例的主題，默認使用 defaultTheme
  theme: IStyleSheet
  // 是否使用暗黑模式，默認為 false
  darkMode?: boolean
  // Univer 實例的語言環境
  locale: LocaleType
  // 要使用的語言環境對象，鍵為語言環境類型，值為對應的語言環境對象
  locales: ILocales
  // Univer 實例的日誌級別
  logLevel?: LogLevel
}
```

參考：[`Univer`](/reference/classes/univer)

#### @univerjs/engine-render

本插件是 Univer 的渲染引擎，負責渲染、滾動條、游標和選區等的繪製。其提供了 `UniverRenderEnginePlugin` 插件。

參考：[@univerjs/engine-render](/reference/packages/plugins/univerjs/engine-render)

#### @univerjs/engine-formula

本插件是 Univer 的公式引擎，負責公式的計算和處理。其提供了 `UniverFormulaEnginePlugin` 插件。

參考：[@univerjs/engine-formula](/reference/packages/plugins/univerjs/engine-formula)

#### @univerjs/design

本插件是 Univer 的設計系統，提供了所有的基礎組件和樣式。

詳見： [UI 章節](/guides/docs/ui/overview)

#### @univerjs/ui

本插件是 Univer 的 UI 基座。其提供了 `UniverUIPlugin` 插件。

`UniverUIPlugin` 提供了一些配置項，可用於基礎佈局的配置。

```typescript
interface IUniverUIConfig {
  // 容器元素，可以是字串或者 DOM 元素。
  container?: string | HTMLElement
  // 是否顯示頭部
  header?: boolean
  // 是否顯示頭部工具欄
  toolbar?: boolean
  // 頭部工具欄的類型。default: 默認緊湊模式；classic: 經典模式；simple：不分組模式
  ribbonType?: RibbonType
  // 是否顯示右鍵菜單
  contextMenu?: boolean
  // 是否禁用自動聚焦
  disableAutoFocus?: true
  // 依賴注入的覆蓋配置
  override?: DependencyOverride
  // 菜單配置
  menu?: MenuConfig
}
```

其中，`menu` 配置項可以用於簡單地設置某些特定菜單項的顯示或隱藏等狀態：

```typescript
univer.registerPlugin(UniverSheetsUIPlugin, {
  menu: {
    'sheet.command.set-range-bold': {
      hidden: true,
    },
    'sheet.command.set-range-italic': {
      hidden: true,
      disabled: true,
    },
  },
})
```

這裡的 `'sheet.command.set-range-bold'` 和 `'sheet.command.set-range-italic'` 是命令的名稱，表示對應的菜單項。

參考：[@univerjs/ui](/reference/packages/plugins/univerjs/ui)

#### @univerjs/docs

本插件是 Univer Docs 的核心插件。其提供了 `UniverDocsPlugin` 插件。

參考：[@univerjs/docs](/reference/packages/plugins/univerjs/docs)

#### @univerjs/docs-ui

本插件是 Univer Docs 的 UI 插件，包含了文件編輯器的 UI 組件和交互。其提供了 `UniverDocsUIPlugin` 插件。

參考：[@univerjs/docs-ui](/reference/packages/plugins/univerjs/docs-ui)
