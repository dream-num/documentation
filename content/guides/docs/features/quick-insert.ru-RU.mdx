---
title: Быстрая вставка
icon: Plus
---

<MetaData
  lang="ru-RU"
  meta={{
    plugins: [{
      client: '@univerjs/docs-quick-insert-ui',
      locale: '@univerjs/docs-quick-insert-ui/locale/ru-RU',
      style: '@univerjs/docs-quick-insert-ui/lib/index.css',
    }],
    server: false,
  }}
/>

Быстрая вставка — это меню в стиле слеш-команд для добавления содержимого. Введите `/` в начале пустой строки, чтобы открыть меню и быстро вставить текст, списки, разделители, таблицы, изображения и другие элементы. Сейчас эта функция поддерживается только в режиме плагинов.

## Управление

- Введите `/` в начале строки, чтобы открыть меню
- Слева от пустого абзаца появится кнопка `+`; нажмите её, чтобы открыть все доступные меню
- Вводите текст для фильтрации; используйте ↑/↓ для навигации, Enter для вставки и Esc для закрытия

## Режим плагинов

### Установка

```package-install
npm install @univerjs/docs-quick-insert-ui
```

### Использование

```typescript
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'
import { UniverDocsQuickInsertUIPlugin } from '@univerjs/docs-quick-insert-ui' // [!code ++]
import DocsQuickInsertUIEnUS from '@univerjs/docs-quick-insert-ui/locale/en-US' // [!code ++]

import '@univerjs/docs-quick-insert-ui/lib/index.css' // [!code ++]

const univer = new Univer({
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: mergeLocales(
      DocsQuickInsertUIEnUS, // [!code ++]
    ),
  },
})

univer.registerPlugin(UniverDocsQuickInsertUIPlugin) // [!code ++]
```
