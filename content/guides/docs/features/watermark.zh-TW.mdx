---
title: 水印
icon: Waves
---

<MetaData
  lang="zh-TW"
  meta={{
    plugins: [{
      client: '@univerjs/watermark',
      facade: '@univerjs/watermark/facade',
    }],
    server: false,
  }}
/>

水印功能旨在為網頁或應用提供靈活且安全的水印方案。透過該功能，開發者可以在頁面上動態生成文字或圖片水印，以防止內容未經授權的使用或傳播。並支援多種配置。

<PlaygroundFrame lang="zh-TW" slug="docs/watermark" clickToShow />

## 預設模式

水印並未提供預設模式，但支援與其外掛混用。

### 安裝

```package-install
npm install @univerjs/watermark
```

### 使用

```typescript
import { UniverWatermarkPlugin } from '@univerjs/watermark' // [!code ++]

import '@univerjs/watermark/facade' // [!code ++]

const { univerAPI } = createUniver({
  // ...
  presets: [
    UniverDocsCorePreset(),
  ],
  plugins: [
    [UniverWatermarkPlugin, { // [!code ++]
      textWatermarkSettings: { // [!code ++]
        content: 'Hello, Univer!', // [!code ++]
        fontSize: 36, // [!code ++]
      }, // [!code ++]
    }], // [!code ++]
  ],
})
```

## 外掛模式

### 安裝

```package-install
npm install @univerjs/watermark
```

### 使用

```typescript
import { UniverWatermarkPlugin } from '@univerjs/watermark' // [!code ++]

import '@univerjs/watermark/facade' // [!code ++]

const univer = new Univer({
  // ...
})

univer.registerPlugin(UniverWatermarkPlugin, { // [!code ++]
  textWatermarkSettings: { // [!code ++]
    content: 'Hello, Univer!', // [!code ++]
    fontSize: 36, // [!code ++]
  }, // [!code ++]
}) // [!code ++]
```

### 外掛與配置

#### @univerjs/watermark

本外掛提供了水印功能的核心實現。它允許開發者在應用中新增文字或圖片水印，並提供多種配置選項來客製化水印的外觀和行為。其提供了 `UniverWatermarkPlugin` 外掛。

參考：[@univerjs/watermark](/reference/packages/plugins/univerjs/watermark)

## Facade API

### 引入

<Callout type="info" title="外掛模式提示">
  僅外掛模式需要手動引入 Facade 套件。預設模式已內建對應的 Facade 套件，無需額外引入。
</Callout>

```typescript
import '@univerjs/watermark/facade'
```

### 設定水印

```typescript
univerAPI.addWatermark('text', { content: 'hello', repeat: true })
```

### 移除水印

```typescript
univerAPI.deleteWatermark()
```
