---
title: ライブシェア
icon: '#pro/ScreenShare'
---

<MetaData
  lang="ja-JP"
  isPro
  meta={{
    plugins: [{
      client: '@univerjs-pro/live-share',
      facade: '@univerjs-pro/live-share/facade',
      style: '@univerjs-pro/live-share/lib/index.css',
    }],
    server: true,
  }}
/>

<Callout type="warning" title="注意事項">
  ライブシェア機能を使用するには、Univer サーバーのサポートが必要です。Univer サーバーが正しくインストールおよび設定されていることを確認してください。詳細は、[Pro へのアップグレード](/guides/sheets/getting-started/pro)を参照してください。
</Callout>

ライブシェア（Live Share）機能を使用すると、複数人での共同作業中に、スクロールや選択範囲の変更などのビュー操作を他のフォロワーとリアルタイムで同期したり、他のユーザーの視点をフォローしたりできます。これは、会議のプレゼンテーションや教育的な説明などのシナリオで非常に役立ちます。

## プラグインモード

### インストール

```package-install
npm install @univerjs-pro/live-share
```

### 使用方法

```typescript
import { UniverLiveSharePlugin } from '@univerjs-pro/live-share' // [!code ++]
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'

import '@univerjs-pro/live-share/lib/index.css' // [!code ++]

import '@univerjs-pro/live-share/facade' // [!code ++]

const univer = new Univer({
  // ...
})

univer.registerPlugin(UniverLiveSharePlugin)
```

Univer の商用ライセンスをお持ちの場合は、[クライアントでのライセンスの使用](/guides/pro/license#プラグインモードでの使用)を参照して設定を行ってください。

## Facade API

ライブシェア機能は Facade API に関連するインターフェースを拡張しており、`univerAPI` を介して直接呼び出すことができます。

### インポート

<Callout type="info" title="プラグインモード注意">
  Facade パッケージの手動 import が必要なのはプラグインモードのみです。プリセットモードには対応する Facade パッケージが内蔵されているため、追加の import は不要です。
</Callout>

```typescript
import '@univerjs-pro/live-share/facade'
```

### ステータス列挙型

```typescript
export enum LiveShareStatus {
  IDLE = 'idle',
  FOLLOWING = 'following',
  PRESENTING = 'presenting',
}
```

### 使用例

```typescript
// 現在のステータスを取得
const status = univerAPI.getLiveShareStatus()

if (status === LiveShareStatus.IDLE) {
  // アイドル状態の場合、プレゼンテーションを開始
  univerAPI.startPresenting()
} else if (status === LiveShareStatus.PRESENTING) {
  // プレゼンテーション中の場合、プレゼンテーションを停止
  univerAPI.stopPresenting()
}

// フォローを開始
univerAPI.startFollowing()

// フォローを停止
univerAPI.stopFollowing()
```
