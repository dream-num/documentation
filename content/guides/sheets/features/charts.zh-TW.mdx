---
title: 圖表
icon: '#pro/ChartPie'
---

<MetaData
  lang="zh-TW"
  isPro
  meta={{
    preset: [{
      client: '@univerjs/preset-sheets-advanced',
      locale: '@univerjs/preset-sheets-advanced/locales/zh-TW',
      style: '@univerjs/preset-sheets-advanced/lib/index.css',
    }],
    plugins: [{
      client: '@univerjs-pro/chart-ui',
      locale: '@univerjs-pro/chart-ui/locale/zh-TW',
      style: '@univerjs-pro/chart-ui/lib/index.css',
    }, {
      client: '@univerjs-pro/sheets-chart',
      locale: '@univerjs-pro/sheets-chart/locale/zh-TW',
    }, {
      client: '@univerjs-pro/sheets-chart-ui',
      locale: '@univerjs-pro/sheets-chart-ui/locale/zh-TW',
      style: '@univerjs-pro/sheets-chart-ui/lib/index.css',
    }],
    server: false,
  }}
/>

圖表是資料視覺化的重要工具，可以幫助使用者更直觀地理解和分析資料。在 Univer Sheets 中，圖表功能提供了多種類型的圖表，包括柱狀圖、折線圖、圓餅圖等，使用者可以根據需要選擇合適的圖表類型來展示資料。

<PlaygroundFrame lang="zh-TW" slug="sheets/charts" clickToShow />

## 預設模式

圖表功能被包含在 `@univerjs/preset-sheets-advanced` 預設中。

### 安裝

<Callout>
  @univerjs/preset-sheets-advanced 的 `UniverSheetsAdvancedPreset` 預設在執行時依賴 `UniverSheetsDrawingPreset` 預設，請先安裝 @univerjs/preset-sheets-drawing。
</Callout>

```package-install
npm install @univerjs/preset-sheets-drawing @univerjs/preset-sheets-advanced
```

### 使用

```typescript
import { UniverSheetsAdvancedPreset } from '@univerjs/preset-sheets-advanced' // [!code ++]
import UniverPresetSheetsAdvancedZhTW from '@univerjs/preset-sheets-advanced/locales/zh-TW' // [!code ++]
import { UniverSheetsCorePreset } from '@univerjs/preset-sheets-core'
import UniverPresetSheetsCoreZhTW from '@univerjs/preset-sheets-core/locales/zh-TW'
import { UniverSheetsDrawingPreset } from '@univerjs/preset-sheets-drawing' // [!code ++]
import UniverPresetSheetsDrawingZhTW from '@univerjs/preset-sheets-drawing/locales/zh-TW' // [!code ++]
import { createUniver, LocaleType, mergeLocales } from '@univerjs/presets'

import '@univerjs/preset-sheets-core/lib/index.css'
import '@univerjs/preset-sheets-drawing/lib/index.css' // [!code ++]
import '@univerjs/preset-sheets-advanced/lib/index.css' // [!code ++]

const { univerAPI } = createUniver({
  locale: LocaleType.ZH_TW,
  locales: {
    [LocaleType.ZH_TW]: mergeLocales(
      UniverPresetSheetsCoreZhTW,
      UniverPresetSheetsDrawingZhTW, // [!code ++]
      UniverPresetSheetsAdvancedZhTW, // [!code ++]
    ),
  },
  presets: [
    UniverSheetsCorePreset(),
    UniverSheetsDrawingPreset(), // [!code ++]
    UniverSheetsAdvancedPreset(), // [!code ++]
  ],
})
```

如果你擁有 Univer 的商業授權，請參考[在客戶端使用授權](/guides/pro/license#在預設模式中使用)進行配置。

{/* ### 預設與配置 */}

## 外掛模式

### 安裝

```package-install
npm install @univerjs-pro/chart-ui @univerjs-pro/sheets-chart @univerjs-pro/sheets-chart-ui
```

### 使用

```typescript
import ChartUIZhTW from '@univerjs-pro/chart-ui/locale/zh-TW' // [!code ++]
import { UniverSheetsChartPlugin } from '@univerjs-pro/sheets-chart' // [!code ++]
import { UniverSheetsChartUIPlugin } from '@univerjs-pro/sheets-chart-ui' // [!code ++]
import SheetsChartUIZhTW from '@univerjs-pro/sheets-chart-ui/locale/zh-TW' // [!code ++]
import SheetsChartZhTW from '@univerjs-pro/sheets-chart/locale/zh-TW' // [!code ++]
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'

import '@univerjs-pro/sheets-chart/facade' // [!code ++]

import '@univerjs-pro/chart-ui/lib/index.css' // [!code ++]
import '@univerjs-pro/sheets-chart-ui/lib/index.css' // [!code ++]

const univer = new Univer({
  locale: LocaleType.ZH_TW,
  locales: {
    [LocaleType.ZH_TW]: mergeLocales(
      ChartUIZhTW, // [!code ++]
      SheetsChartZhTW, // [!code ++]
      SheetsChartUIZhTW, // [!code ++]
    ),
  },
})

univer.registerPlugin(UniverSheetsChartPlugin)
univer.registerPlugin(UniverSheetsChartUIPlugin)
```

如果你擁有 Univer 的商業授權，請參考[在客戶端使用授權](/guides/pro/license#在外掛模式中使用)進行配置。

## 支援的圖表類型

Univer Sheets 支援以下圖表類型：

| 圖表類型 | 說明 |
| --- | --- |
| Column | 柱狀圖 |
| ColumnStacked | 堆積柱狀圖 |
| ColumnPercentStacked | 百分比堆積柱狀圖 |
| Bar | 條形圖 |
| BarStacked | 堆積條形圖 |
| BarPercentStacked | 百分比堆積條形圖 |
| Line | 折線圖 |
| Area | 面積圖 |
| AreaStacked | 堆積面積圖 |
| AreaPercentStacked | 百分比堆積面積圖 |
| Pie | 圓餅圖 |
| Doughnut | 環形圖 |
| Radar | 雷達圖 |
| Scatter | 散點圖 |
| Bubble | 氣泡圖 |
| Combination | 組合圖 |
| WordCloud | 詞雲圖 |
| Funnel | 漏斗圖 |
| Relation | 關係圖 |
| Waterfall | 瀑布圖 |
| Pareto | 帕累托圖 |
| Sankey | 桑基圖 |
| Heatmap | 熱力圖 |
| Boxplot | 箱線圖 |

## Facade API

完整 Facade API 類型定義，請查看 [FacadeAPI](/reference/facade/univer)。

### 導入

<Callout type="info" title="外掛模式提示">
  僅外掛模式需要手動引入 Facade 套件。預設模式已內建對應的 Facade 套件，無需額外引入。
</Callout>

```typescript
import '@univerjs-pro/sheets-chart/facade'
```

### 插入圖表

`FWorksheet.newChart()` 方法用於建立一個圖表建構器，回傳一個 `FChartBuilderBase` 實例，你可以透過鏈式呼叫方法來設定圖表的各種屬性。

然後呼叫 `build()` 產生 `IChartBuilderInfo` 物件，透過 `FWorksheet.insertChart(chartBuildInfo: IChartBuilderInfo)` 方法將圖表插入到表格中。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()

// 建立一個柱狀圖，資料來源為 A1:D6。
// 開始位置為儲存格 B2 的左上角。
// 圖表的寬度為 600，高度為 400。
const chartInfo = fWorksheet.newChart()
  .setChartType(univerAPI.Enum.ChartType.Column)
  .addRange('A1:D6')
  .setPosition(1, 1, 0, 0)
  .setWidth(600)
  .setHeight(400)
  .build()
await fWorksheet.insertChart(chartInfo)
```

以下是 `FChartBuilderBase` 上的一些成員方法：

| 方法 | 描述 |
| --- | --- |
| addRange | 指定圖表資料來源範圍 |
| setPosition | 以 row，col 錨點方式設定圖表位置 |
| setAbsolutePosition | 以像素方式設定圖表位置 |
| setChartType | 設定圖表類型 |
| setWidth | 設定圖表寬度 |
| setHeight | 設定圖表高度 |
| setOptions | 設定圖表配置項 |
| setTransposeRowsAndColumns | 行列轉換 |
| setTheme | 設定主題 |
| setXAxisTitle | 設定X軸標題 |
| setYAxisTitle | 設定Y軸標題 |
| setRightYAxisTitle | 設定右軸標題 |
| setXAxisTextStyle | 設定X軸標題樣式 |
| setYAxisTextStyle | 設定Y軸標題樣式 |
| setRightYAxisTextStyle | 設定右軸標題樣式 |
| setInvalidValueStrategy | 設定空儲存格顯示方式 |
| setAxisPointerStyle | 設定指示線樣式 |
| setAllSeriesStyle | 設定所有系列樣式 |
| setSeriesStyle | 設定系列樣式 |
| build | 產生 builder info，用於插入/更新圖表 |

目前我們支援以下的配置項，你可以透過查閱 IChartBuildOptions 來獲取對應的配置：

<details>
  <summary>顯示配置</summary>
  ```typescript
  export interface IChartBuildOptions {
    /**
     * @property {string} [title] The title of the chart.
     */
    title?: {
      /**
       * @property {string} [titlePosition] The position of the chart title.
       */
      position?: TitlePositionEnum
      /**
       * @property {string} [titleAlignment] The alignment of the chart title.The possible values are 'left', 'center' & 'right'.
       */
      titleAlignment: LabelAlignEnum
    } & IChartTextStyle
    /**
     * @property {string} [legend] The legend of the chart.
     */
    legend?: {
      /**
       * @property {string} [legendPosition] The position of the legend.The possible values are 'top', 'bottom', 'left', 'right' & 'hide'.
       */
      position?: LegendPositionEnum
      /**
       * @property {number} [fontSize] The font size of the legend.
       */
      fontSize?: number
      /**
       * @property {string} [color] The font color of the legend.
       */
      color?: string
      /**
       * @property {string} [bold] The font style of the legend.
       */
      bold?: boolean
      /**
       * @property {string} [italic] The font weight of the legend.
       */
      italic?: boolean
      /**
       * @property {SelectModeEnum} [selectMode] The select mode of the legend.The possible values are 'single', 'multiple' & 'close'.
       */
      selectMode?: SelectModeEnum
    }
    /**
     * @property {string} [xAxisTitle] The x-axis title of the chart.
     * @example
     * ```typescript
     * chartBuilder.setOptions('xAxisTitle.content', 'xAxis Title text')
     * .setOptions('xAxisTitle.font', 1)
     * .setOptions('xAxisTitle.fontSize', 12)
     * .setOptions('xAxisTitle.fontColor', '#ff0000')
     * .build();
     * ```
     */
    xAxisTitle?: IChartTextStyle
    /**
     * @property {string} [yAxisTitle] The y-axis title of the chart.
     */
    yAxisTitle?: IChartTextStyle
    /**
     * @property {string} [rightYAxisTitle] The right y-axis title of the chart.
     */
    rightYAxisTitle?: IChartTextStyle
    xAxis?: IAxisOptions
    yAxis?: IAxisOptions
    yRightAxis?: IAxisOptions
    axisPointer?: {
      /**
       * @property {string} [indicatorLineType] The line type of the axis pointer.
       */
      indicatorLineType?: string
      /**
       * @property {ChartBorderDashType} [indicatorLineColor] The line color of the axis pointer.The maybe values are 'solid', 'dotted', 'dashed'.
       */
      indicatorLineColor?: ChartBorderDashType
      /**
       * @property {string} [indicatorLabelColor] The line width of the axis pointer.
       */
      indicatorLabelColor?: string
      /**
       * @property {string} [indicatorLabelTextColor] The font color of the axis pointer.
       */
      indicatorLabelTextColor?: string
    }
    allSeriesStyle?: Partial<IAllSeriesStyle>
    seriesStyleMap?: {
      [id: string]: ISeriesStyle
    }
    /**
     * @property {string} [area] The area of line/area chart.
     */
    area?: {
      lineStyle: AreaLineStyle
    }
    /**
     * @property {RadarShape} [radar] The radar of the chart.
     */
    radar?: {
      /**
       * @property {RadarShape} [shape] The shape of the radar chart.
       */
      shape?: RadarShape
      /**
       * @property {boolean} [fill] True if the radar chart is filled; false otherwise.
       */
      fill?: boolean
    }
    /**
     * @property {string} [pie] The pie configuration of the chart.
     */
    pie?: {
      /**
       * @property {number} [doughnutHole] The size of the hole in the center of the pie chart.
       */
      doughnutHole: number
      /**
       * @property {string} [borderColor] The color of the border around the pie chart.
       */
      borderColor: string
      /**
       * @property {boolean} [hasPaddingAngle] True if the pie chart has a padding angle.
       */
      hasPaddingAngle: boolean
      /**
       * @property {boolean} [isHalfPie] True if the pie chart is a half pie chart.
       */
      isHalfPie: boolean
      /**
       * @property {boolean} [rosePie] True if the pie chart is a rose pie chart.
       */
      rosePie: boolean
      /**
       * @property {boolean} [showLabelLine] True if the pie chart shows label lines.
       */
      showLabelLine: boolean
    }
    /**
     * @property {string} [backgroundColor] The background color of the chart.
     */
    backgroundColor?: string
    /**
     * @property {string} [borderColor] The border color of the chart.
     */
    borderColor?: string
    /**
     * @property {boolean} [gradientFill] Whether to use gradient fill.This property does not work in line charts.
     */
    gradientFill?: boolean
    /**
     * @property {string} [theme] The theme of the chart.
     */
    theme?: string
    /**
     * @property {InvalidValueType} [invalidValueType] The display mode for empty cells.
     */
    invalidValueType?: InvalidValueType
  }
  ```
</details>

配置項透過 setOptions 方法來設定，有以下兩種方式：
  - `setOptions(optionPath, optionVal)`: `setOptions('legend.color', '#ff0000')`
  - `setOptions('', IChartBuildOptions)`: `setOptions('', { legend: { color: '#ff0000', bold: true } })`

<details>
  <summary>建立一個折線圖範例</summary>
  ```typescript
  const fWorkbook = univerAPI.getActiveWorkbook()
  const fWorksheet = fWorkbook.getActiveSheet()

  // 建立一個折線圖
  const chartInfo = fWorksheet.newChart()
    // 折線圖
    .asLineChart()
    // 設定資料來源 A1:D6
    .addRange('A1:D6')
    // 設定起始位置 B2 儲存格左上角
    .setPosition(1, 1, 0, 0)
    // 設定指示線樣式
    .setAxisPointerStyle({
      indicatorLabelColor: '#ff0000',
      indicatorLineType: univerAPI.Enum.ChartBorderDashType.Solid,
      indicatorLineColor: '#00ff00',
      indicatorLabelTextColor: '#0000ff',
    })
    // 設定資料點大小 10
    .setDataPointSize(10)
    // 設定資料點形狀為圓形
    .setDataPointShape(univerAPI.Enum.LinePointShape.Circle)
    // 產生 builder info
    .build()
  const fChart = await fWorksheet.insertChart(chartInfo)

  // 3 秒後更新圖表
  setTimeout(() => {
    if (fChart) {
      // 獲取第一個系列資料
      const first = fChart.getSeriesData()[0]
      // 設定第一個系列顏色為紅色
      const newChartInfo = fWorksheet.newChart(fChart)
        .setSeriesStyle(first.index, {
          color: '#ff0000',
        })
        .build()
      // 更新圖表
      fWorksheet.updateChart(newChartInfo)
    }
  }, 3000)
  ```
</details>

目前我們還提供了以下的 builder 來建立對應的圖表，這些 builder 都可以透過類似 `fWorkSheet.newChart().asLineChart()` 來建立，它們派生自 `FChartBuilderBase` 並且具有一些特有的配置：

```typescript
const lineChartBuilder = fWorkSheet.newChart().asLineChart()
const pieChartBuilder = fWorkSheet.newChart().asPieChart()
const radarChartBuilder = fWorkSheet.newChart().asRadarChart()

// 也可以透過下面的方式來建立
const chartBuilder = fWorkSheet.newChart()
  .setChartType(univerAPI.Enum.ChartType.Column)
```

- `LineChartBuilder`

| 方法 | 描述 |
| --- | --- |
| setLineStyle | 設定折線的平滑度，'line'- 折線 , 'smooth'- 平滑， 'step' -階梯 |
| setDataPointShape | 設定資料點形狀，可以透過 LinePointShape 來查看支援的形狀類型 |
| setDataPointColor | 設定資料點顏色 |
| setDataPointSize | 設定資料點大小 |
| build | 產生 builder info，用於插入/更新圖表 |

- `PieChartBuilder`

| 方法 | 描述 |
| --- | --- |
| setDoughnutHole | 設定圓餅圖空心大小，該參數範圍為 0-1 |
| setBorderColor | 設定圓餅圖邊框顏色 |
| setHasPaddingAngle | 設定圓餅圖扇區的是否有夾角（分離） |
| setIsHalfPie | 是不是半圓餅圖 |
| setRosePie | 是不是玫瑰圖 |
| setShowLabelLine | 顯示標籤線 |
| build | 產生 builder info，用於插入/更新圖表 |

- `RadarChartBuilder`

| 方法 | 描述 |
| --- | --- |
| setShape | 設定雷達圖預設形狀，目前支援的值為：'polygon'-多邊形，'circle'-圓形 |
| setFill | 是否填充雷達圖 |
| build | 產生 builder info，用於插入/更新圖表 |

### 獲取圖表

`FWorksheet.getCharts()` 方法用於獲取目前工作表中的所有圖表，回傳一個 `FChart[]` 陣列。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()

// 獲取目前工作表中的所有圖表
const charts = fWorksheet.getCharts()
```

以下是 `FChart` 上的一些成員方法：

| 方法 | 描述 |
| --- | --- |
| getRange | 獲取圖表資料來源範圍 |
| getSeriesData | 獲取圖表系列資料 |
| getCategoryData | 獲取圖表類別資料 |
| updateRange | 更新圖表資料來源範圍 |

### 更新圖表

`FWorksheet.updateChart(chartBuildInfo: IChartBuilderInfo)` 方法用於更新圖表，傳入一個 `IChartBuilderInfo` 物件，用於更新圖表的配置。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()

// 建立一個柱狀圖，資料來源為 A1:D6.
// 起始位置為儲存格 B2 的左上角.
const chartInfo = fWorksheet.newChart()
  .setChartType(univerAPI.Enum.ChartType.Column)
  .addRange('A1:D6')
  .setPosition(1, 1, 0, 0)
  .build()
await fWorksheet.insertChart(chartInfo)

// 獲取目前工作表中的所有圖表
const charts = fWorksheet.getCharts()

// 3 秒後更新第一個圖表
setTimeout(() => {
  const newChartInfo = fWorksheet.newChart(charts[0])
    .asLineChart()
    .setOptions('legend.position', univerAPI.Enum.LegendPositionEnum.Right)
    .build()
  fWorksheet.updateChart(newChartInfo)
}, 3000)
```

### 刪除圖表

`FWorksheet.removeChart(chart: FChart)` 方法用於刪除圖表，傳入一個 `FChart` 物件，用於刪除指定的圖表。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()

// 建立一個柱狀圖，資料來源為 A1:D6.
// 起始位置為儲存格 B2 的左上角.
const chartInfo = fWorksheet.newChart()
  .setChartType(univerAPI.Enum.ChartType.Column)
  .addRange('A1:D6')
  .setPosition(1, 1, 0, 0)
  .build()
await fWorksheet.insertChart(chartInfo)

// 獲取目前工作表中的所有圖表
const charts = fWorksheet.getCharts()

// 3 秒後刪除第一個圖表
setTimeout(async () => {
  await fWorksheet.removeChart(charts[0])
  console.log(fWorksheet.getCharts())
}, 3000)
```

### 圖表主題

`FWorksheet.registerChartTheme(themeName: string, theme: IEchartTheme)` 方法用於註冊圖表主題，傳入一個主題名稱和主題物件，用於註冊自訂主題。

Univer chart 是基於 echarts 的圖表庫實作的，因此可以借助 echart 的主題建構工具來建構你的[自訂主題](https://echarts.apache.org/zh/theme-builder.html)。
你可以在該網站建構你自己的主題並下載成為配置檔案，使用以下 API 來使用你的自訂主題：

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()

// 註冊你的主題
const theme = { // 你的主題配置
  version: 1,
  themeName: 'myTheme',
  theme: {
    // ... 為了簡潔，省略了一些程式碼
    color: [
      '#893448',
      '#d95850',
      '#eb8146',
      '#ffb248',
      '#f2d643',
      '#ebdba4',
    ],
    // ... 為了簡潔，省略了一些程式碼
    visualMapColor: [
      '#893448',
      '#d95850',
      '#eb8146',
      '#ffb248',
      '#f2d643',
      'rgb(247,238,173)',
    ],
    // ... 為了簡潔，省略了一些程式碼
    axes: [],
    // ... 為了簡潔，省略了一些程式碼
  },
}
fWorksheet.registerChartTheme('myTheme', theme)

// 使用你的主題建立一個折線圖
const chartInfo = fWorksheet.newChart()
  .asLineChart()
  .addRange('A1:D6')
  .setPosition(1, 1, 0, 0)
  .setTheme('myTheme')
  .build()
await fWorksheet.insertChart(chartInfo)
```
