---
title: 列印
icon: '#pro/Printer'
---

<MetaData
  lang="zh-TW"
  isPro
  meta={{
    preset: [{
      client: '@univerjs/preset-sheets-advanced',
      locale: '@univerjs/preset-sheets-advanced/locales/zh-TW',
      style: '@univerjs/preset-sheets-advanced/lib/index.css',
    }],
    plugins: [{
      client: '@univerjs-pro/sheets-print',
      facade: '@univerjs-pro/sheets-print/facade',
      locale: '@univerjs-pro/sheets-print/locale/zh-TW',
      style: '@univerjs-pro/sheets-print/lib/index.css',
    }],
    server: false,
  }}
/>

列印功能允許使用者將表格內容列印為紙質文件或匯出為 PDF 格式，便於離線檢視和分享。

<PlaygroundFrame lang="zh-TW" slug="sheets/print" clickToShow />

## 預設模式

列印功能被包含在 `@univerjs/preset-sheets-advanced` 預設中。

### 安裝

<Callout>
  @univerjs/preset-sheets-advanced 的 `UniverSheetsAdvancedPreset` 預設在執行時依賴 `UniverSheetsDrawingPreset` 預設，請先安裝 @univerjs/preset-sheets-drawing。
</Callout>

```package-install
npm install @univerjs/preset-sheets-drawing @univerjs/preset-sheets-advanced
```

### 使用

```typescript
import { UniverSheetsAdvancedPreset } from '@univerjs/preset-sheets-advanced' // [!code ++]
import UniverPresetSheetsAdvancedZhTW from '@univerjs/preset-sheets-advanced/locales/zh-TW' // [!code ++]
import { UniverSheetsCorePreset } from '@univerjs/preset-sheets-core'
import UniverPresetSheetsCoreZhTW from '@univerjs/preset-sheets-core/locales/zh-TW'
import { UniverSheetsDrawingPreset } from '@univerjs/preset-sheets-drawing' // [!code ++]
import UniverPresetSheetsDrawingZhTW from '@univerjs/preset-sheets-drawing/locales/zh-TW' // [!code ++]
import { createUniver, LocaleType, mergeLocales } from '@univerjs/presets'

import '@univerjs/preset-sheets-core/lib/index.css'
import '@univerjs/preset-sheets-drawing/lib/index.css' // [!code ++]
import '@univerjs/preset-sheets-advanced/lib/index.css' // [!code ++]

const { univerAPI } = createUniver({
  locale: LocaleType.ZH_TW,
  locales: {
    [LocaleType.ZH_TW]: mergeLocales(
      UniverPresetSheetsCoreZhTW,
      UniverPresetSheetsDrawingZhTW, // [!code ++]
      UniverPresetSheetsAdvancedZhTW, // [!code ++]
    ),
  },
  presets: [
    UniverSheetsCorePreset(),
    UniverSheetsDrawingPreset(), // [!code ++]
    UniverSheetsAdvancedPreset(), // [!code ++]
  ],
})
```

如果你擁有 Univer 的商業許可證，請參考[在客戶端使用許可證](/guides/pro/license#在預設模式中使用)進行配置。

### 預設與配置

```typescript
interface IUniverSheetsAdvancedPresetConfig {
  print?: Partial<IUniverSheetsPrintConfig>
}

interface IUniverSheetsPrintConfig {
  /**
   * 是否強制列印時顯示水印
   * @default false
   */
  enforceWatermark?: boolean
}
```

## 外掛模式

### 安裝

```package-install
npm install @univerjs-pro/sheets-print
```

### 使用

```typescript
import { UniverSheetsPrintPlugin } from '@univerjs-pro/sheets-print' // [!code ++]
import SheetsPrintPluginZhTW from '@univerjs-pro/sheets-print/locale/zh-TW' // [!code ++]
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'

import '@univerjs-pro/sheets-print/lib/index.css' // [!code ++]

const univer = new Univer({
  locale: LocaleType.ZH_TW,
  locales: {
    [LocaleType.ZH_TW]: mergeLocales(
      SheetsPrintPluginZhTW, // [!code ++]
    ),
  },
})

univer.registerPlugin(UniverSheetsPrintPlugin) // [!code ++]
```

如果你擁有 Univer 的商業許可證，請參考[在客戶端使用許可證](/guides/pro/license#在外掛模式中使用)進行配置。

### 外掛與配置

```typescript
interface IUniverSheetsPrintConfig {
  /**
   * 是否強制列印時顯示水印
   * @default false
   */
  enforceWatermark?: boolean
}
```

##

### 引入

```typescript
import '@univerjs-pro/sheets-print/facade'
```

### 開啟列印配置對話方塊

使用 `FWorkbook.openPrintDialog` 可以開啟列印配置對話方塊。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()

fWorkbook.openPrintDialog()
```

### 關閉列印配置對話方塊

使用 `FWorkbook.closePrintDialog` 方法可以關閉列印配置對話方塊。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
fWorkbook.openPrintDialog()

// 3 秒後關閉列印配置對話方塊
setTimeout(() => {
  fWorkbook.closePrintDialog()
}, 3000)
```

### 更新列印佈局配置

使用 [`FWorkbook.updatePrintConfig(config: ISheetPrintLayoutConfig)`](https://reference.univer.ai/zh-TW/classes/FWorkbook#updateprintconfig) 方法更新列印佈局配置。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const subUnitId = fWorksheet.getSheetId()

// 更新列印佈局配置
fWorkbook.updatePrintConfig({
  area: univerAPI.Enum.PrintArea.CurrentSheet, // 列印當前工作表
  subUnitIds: [subUnitId],
  paperSize: univerAPI.Enum.PrintPaperSize.A4, // A4 紙張大小
  scale: univerAPI.Enum.PrintScale.FitPage, // 使內容適應頁面
  freeze: [univerAPI.Enum.PrintFreeze.Row], // 凍結行標題
  margin: univerAPI.Enum.PrintPaperMargin.Normal, // 正常邊距
  // ... 其他設定
})

// 開始列印
fWorkbook.print()
```

<details>
  <summary>以下是 `ISheetPrintLayoutConfig` 的完整定義：</summary>
  ```typescript
  /**
   * 列印佈局設定的配置介面
   */
  export interface ISheetPrintLayoutConfig {
    /**
     * 指定要列印的工作表區域（例如，當前工作表，選區）
     */
    area: PrintArea
    /**
     * 要列印的工作簿的工作表 ID 集合，或者包含工作表 ID 和範圍的物件集合
     */
    subUnitIds: (string | { id: string, range: IRange })[]
    /**
     * 列印紙張大小設定（例如，A4，信紙）
     */
    paperSize: PrintPaperSize
    /**
     * 列印頁面方向（縱向或橫向）
     */
    direction: PrintDirection
    /**
     * 列印縮放型別
     */
    scale: PrintScale
    /**
     * 自訂縮放百分比
     */
    customScale: number
    /**
     * 凍結行和列的設定陣列
     */
    freeze: PrintFreeze[]
    /**
     * 列印頁面的邊距預設
     */
    margin: PrintPaperMargin
    /**
     * 使用自訂紙張尺寸時的自訂頁面尺寸
     */
    pageSizeCustom?: { w: number, h: number }
    /**
     * 每頁列印的最大行數
     */
    maxRowsEachPage: number
    /**
     * 每頁列印的最大列數
     */
    maxColumnsEachPage: number
  }

  /**
   * 定義要列印的電子表格區域
   */
  export enum PrintArea {
    /** 只列印當前活動工作表 */
    CurrentSheet = 'CurrentSheet',
    /** 列印整個工作簿 */
    workbook = 'Workbook',
    /** 只列印當前選定的範圍 */
    CurrentSelection = 'CurrentSelection',
    /** 列印所有選定的範圍 */
    AllSelection = 'AllSelection',
  }

  /**
   * 定義列印紙張大小
   */
  export enum PrintPaperSize {
    /** 使用信紙大小 */
    Letter = 'Letter',
    /** 使用小報紙大小 */
    Tabloid = 'Tabloid',
    /** 使用法律紙張大小 */
    Legal = 'Legal',
    /** 使用報表紙張大小 */
    Statement = 'Statement',
    /** 使用行政公文紙張大小 */
    Executive = 'Executive',
    /** 使用對摺紙張大小 */
    Folio = 'Folio',
    /** 使用A3紙張大小 */
    A3 = 'A3',
    /** 使用A4紙張大小 */
    A4 = 'A4',
    /** 使用A5紙張大小 */
    A5 = 'A5',
    /** 使用B4紙張大小 */
    B4 = 'B4',
    /** 使用B5紙張大小 */
    B5 = 'B5',
  }

  /**
   * 定義列印頁面的方向
   */
  export enum PrintDirection {
    /** 縱向方向 */
    Portrait = 'Portrait',
    /** 橫向方向 */
    Landscape = 'Landscape',
  }

  /**
   * 定義列印的縮放型別
   */
  export enum PrintScale {
    /** 正常縮放 */
    Origin = 'Origin',
    /** 適合寬度 */
    FitWidth = 'FitWidth',
    /** 適合高度 */
    FitHeight = 'FitHeight',
    /** 適合頁面大小 */
    FitPage = 'FitPage',
    /** 自訂縮放 */
    Custom = 'Custom',
  }

  /**
   * 定義列印時應保持凍結的元素
   */
  export enum PrintFreeze {
    /** 列印時保持行標題凍結 */
    Row = 'Row',
    /** 列印時保持列標題凍結 */
    Column = 'Column',
  }

  /**
   * 定義列印頁面的邊距預設
   */
  export enum PrintPaperMargin {
    /** 使用正常邊距 */
    Normal = 'Normal',
    /** 使用窄邊距 */
    Narrow = 'Narrow',
    /** 使用寬邊距 */
    Wide = 'Wide',
    /** 沒有邊距 */
    None = 'None',
  }
  ```
</details>

### 更新列印渲染配置

使用 [`FWorkbook.updatePrintRenderConfig(config: ISheetPrintRenderConfig)`](https://reference.univer.ai/zh-TW/classes/FWorkbook#updateprintrenderconfig) 方法更新列印渲染配置。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()

// 更新預設列印佈局配置
fWorkbook.updatePrintConfig({})

// 更新列印渲染配置
fWorkbook.updatePrintRenderConfig({
  gridlines: true, // 顯示網格線
  hAlign: univerAPI.Enum.PrintAlign.Middle, // 水平居中對齊
  vAlign: univerAPI.Enum.PrintAlign.Middle, // 垂直居中對齊
  headerFooter: [ // 要包含的頁首和頁尾元素陣列，這裡是頁碼和工作表名稱
    univerAPI.Enum.PrintHeaderFooter.PageSize,
    univerAPI.Enum.PrintHeaderFooter.WorksheetTitle,
  ],
  // ... 其他設定
})

// 開始列印
fWorkbook.print()
```

<details>
  <summary>以下是 `ISheetPrintRenderConfig` 的完整定義：</summary>
  ```typescript
  /**
   * 列印渲染選項的配置介面
   */
  export interface ISheetPrintRenderConfig {
    /**
     * 是否在列印輸出中顯示網格線
     */
    gridlines: boolean
    /**
     * 水平對齊設定
     */
    hAlign: PrintAlign
    /**
     * 垂直對齊設定
     */
    vAlign: PrintAlign
    /**
     * 包含的頁首和頁尾元素陣列
     */
    headerFooter: PrintHeaderFooter[]
    /**
     * 頁首和頁尾的詳細設定
     */
    headerFooterSetting: IPrintHeaderFooter
    /**
     * 是否使用自訂頁首和頁尾而不是預設
     */
    isCustomHeaderFooter?: boolean

    watermark?: Nullable<IWatermarkConfigWithType>
  }

  /**
   * 定義列印內容的可用對齊選項
   */
  export enum PrintAlign {
    /** 水平方向左對齊，垂直方向頂部對齊 */
    Start = 'Start',
    /** 水平方向右對齊，垂直方向底部對齊 */
    End = 'End',
    /** 水平方向居中對齊，垂直方向居中對齊 */
    Middle = 'Middle',
  }

  /**
   * 定義頁首和頁尾內容的可用佔位符
   */
  export enum PrintHeaderFooter {
    /** 插入當前頁碼資訊 */
    PageSize = 'PageSize',
    /** 插入工作簿名稱 */
    WorkbookTitle = 'WorkbookTitle',
    /** 插入工作表名稱 */
    WorksheetTitle = 'WorksheetTitle',
    /** 插入當前日期 */
    Date = 'Date',
    /** 插入當前時間 */
    Time = 'Time',
  }

  /**
   * 頁首和頁尾內容定位的配置介面
   */
  export interface IPrintHeaderFooter {
    /** 左上角顯示的內容 */
    topLeft: string
    /** 頂部中心顯示的內容 */
    topCenter: string
    /** 右上角顯示的內容 */
    topRight: string
    /** 左下角顯示的內容 */
    bottomLeft: string
    /** 底部中心顯示的內容 */
    bottomCenter: string
    /** 右下角顯示的內容 */
    bottomRight: string
  }
  ```
</details>

### 調起列印

使用 `FWorkbook.print` 方法可以直接調起列印。

```typescript
// 使用預設配置可以傳入空物件
workbook.updatePrintConfig({
  // ... 列印佈局配置
})
workbook.updatePrintRenderConfig({
  // ... 列印渲染配置
})
workbook.print()
```

### 儲存截圖至剪貼簿

使用 `FWorkbook.saveScreenshotToClipboard` 方法可以儲存列印資料圖片到剪下板。

該 API 僅在擁有 [許可證](zh-TW/guides/pro/license) 的情況下可用，未持有許可證時將受到使用限制，儲存操作將返回 `false`。

我們使用 Clipboard API 儲存圖片到剪貼簿，在非安全網路環境或某些不支援的瀏覽器可能會儲存失敗。儲存成功會返回 `true`。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const result = await fWorkbook.saveScreenshotToClipboard()
```

### 獲取選區截圖

使用 `FRange.getScreenshot` 方法可以獲取選區列印資料圖片。

該 API 僅在持有 [許可證](zh-TW/guides/pro/license) 時可用，未持證使用者將受到使用限制，儲存失敗時返回 `false`，成功時返回圖片的 base64 字串。

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:D10')
fRange.getScreenshot()
fRange.getScreenshot({ includeHeaders: true }) // 包含行列標題
```

### 事件監聽

完整事件型別定義，請檢視 [Events](https://reference.univer.ai/zh-TW/classes/FEventName)。

`univerAPI.Event.BeforeSheetPrintOpen` 事件在開啟列印配置對話方塊之前觸發。

```typescript
const disposable = univerAPI.addEvent(univerAPI.Event.BeforeSheetPrintOpen, (params) => {
  const { workbook, worksheet } = params

  // 取消開啟列印配置對話方塊操作
  params.cancel = true
})

// 移除事件監聽器，使用 `disposable.dispose()`
```

`univerAPI.Event.SheetPrintOpen` 事件在開啟列印配置對話方塊之後觸發。

```typescript
const disposable = univerAPI.addEvent(univerAPI.Event.SheetPrintOpen, (params) => {
  const { workbook, worksheet } = params
})

// 移除事件監聽器，使用 `disposable.dispose()`
```

`univerAPI.Event.BeforeSheetPrintConfirm` 事件在確認列印之前觸發。

```typescript
const disposable = univerAPI.addEvent(univerAPI.Event.BeforeSheetPrintConfirm, (params) => {
  const { renderConfig, layoutConfig } = params

  // 取消列印確認操作
  params.cancel = true
})

// 移除事件監聽器，使用 `disposable.dispose()`
```

`univerAPI.Event.SheetPrintConfirmed` 事件在確認列印之後觸發。

```typescript
const disposable = univerAPI.addEvent(univerAPI.Event.SheetPrintConfirmed, (params) => {
  const { renderConfig, layoutConfig } = params
})

// 移除事件監聽器，使用 `disposable.dispose()`
```

`univerAPI.Event.BeforeSheetPrintCanceled` 事件在取消列印之前觸發。

```typescript
const disposable = univerAPI.addEvent(univerAPI.Event.BeforeSheetPrintCanceled, (params) => {
  const { renderConfig, layoutConfig } = params

  // 取消列印取消操作
  params.cancel = true
})

// 移除事件監聽器，使用 `disposable.dispose()`
```

`univerAPI.Event.SheetPrintCanceled` 事件在取消列印之後觸發。

```typescript
const disposable = univerAPI.addEvent(univerAPI.Event.SheetPrintCanceled, (params) => {
  const { renderConfig, layoutConfig } = params
})

// 移除事件監聽器，使用 `disposable.dispose()`
```
