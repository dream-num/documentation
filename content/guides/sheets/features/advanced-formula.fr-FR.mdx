---
title: Moteur de formules avancé
icon: '#pro/Sigma'
---

<MetaData
  lang="fr-FR"
  isPro
  meta={{
    preset: [{
      client: '@univerjs/preset-sheets-advanced',
      locale: '@univerjs/preset-sheets-advanced/locales/fr-FR',
      style: '@univerjs/preset-sheets-advanced/lib/index.css',
    }],
    plugins: [{
      client: '@univerjs-pro/engine-formula',
      facade: '@univerjs-pro/engine-formula/facade',
    }],
    server: false,
  }}
/>

<Callout type="info">
  Cette documentation présente le moteur de formules avancé d’Univer Pro. Pour la version open source, consultez [cette page](/guides/sheets/features/core/formula).
</Callout>

Le moteur de formules avancé optimise le moteur d’origine afin d’améliorer les performances de calcul et d’offrir davantage de fonctionnalités. Il prend en charge de nombreux calculs complexes, peut traiter de grands jeux de données et propose de meilleures capacités de gestion des erreurs et de débogage.

## Comparaison des performances

Le tableau suivant compare le moteur de formules Pro au moteur de formules OSS.

| Fichier de test | Nombre de formules | OSS | Pro | Accélération avec Pro |
| --- | --- | --- | --- | --- |
| Synthetic_500K_MixedFormulas | 500,000 | 137.13 s | **132.18 s** | 1.04× |
| RealWorld_Engineering_68K | 68,576 | 34.54 s | **6.65 s** | **5.19×** |
| RealWorld_Financial_33K | 33,617 | 1.34 s | **1.22 s** | 1.10× |
| Template_1.4M_Summary | 1,463,721 | 248.62 s | **103.62 s** | **2.40×** |

## Mode prédéfini

Le moteur de formules avancé est inclus dans le preset `@univerjs/preset-sheets-advanced`.

### Installation

<Callout>
  Le preset `UniverSheetsAdvancedPreset` du package `@univerjs/preset-sheets-advanced` dépend du preset `UniverSheetsDrawingPreset` lors de l’exécution. Veuillez d’abord installer `@univerjs/preset-sheets-drawing`.
</Callout>

```package-install
npm install @univerjs/preset-sheets-drawing @univerjs/preset-sheets-advanced
```

### Utilisation

```typescript
import { UniverSheetsAdvancedPreset } from '@univerjs/preset-sheets-advanced' // [!code ++]
import UniverPresetSheetsAdvancedEnUS from '@univerjs/preset-sheets-advanced/locales/en-US' // [!code ++]
import { UniverSheetsCorePreset } from '@univerjs/preset-sheets-core'
import UniverPresetSheetsCoreEnUS from '@univerjs/preset-sheets-core/locales/en-US'
import { UniverSheetsDrawingPreset } from '@univerjs/preset-sheets-drawing' // [!code ++]
import UniverPresetSheetsDrawingEnUS from '@univerjs/preset-sheets-drawing/locales/en-US' // [!code ++]
import { createUniver, LocaleType, mergeLocales } from '@univerjs/presets'

import '@univerjs/preset-sheets-core/lib/index.css'
import '@univerjs/preset-sheets-drawing/lib/index.css' // [!code ++]
import '@univerjs/preset-sheets-advanced/lib/index.css' // [!code ++]

const { univerAPI } = createUniver({
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: mergeLocales(
      UniverPresetSheetsCoreEnUS,
      UniverPresetSheetsDrawingEnUS, // [!code ++]
      UniverPresetSheetsAdvancedEnUS, // [!code ++]
    ),
  },
  presets: [
    UniverSheetsCorePreset(),
    UniverSheetsDrawingPreset(), // [!code ++]
    UniverSheetsAdvancedPreset(), // [!code ++]
  ],
})
```

Si vous disposez d’une licence commerciale Univer, consultez [Utiliser une licence côté client](/guides/pro/license#in-preset-mode) pour la configuration.

{/* ### Presets et configuration */}

## Mode plugin

### Installation

```package-install
npm install @univerjs-pro/engine-formula
```

### Utilisation

Pour utiliser le moteur de formules avancé, vous devez remplacer le `UniverFormulaEnginePlugin` d’origine par `UniverProFormulaEnginePlugin`.

```typescript
import { UniverProFormulaEnginePlugin } from '@univerjs-pro/engine-formula' // [!code ++]
import { UniverFormulaEnginePlugin } from '@univerjs/engine-formula' // [!code --]

univer.registerPlugin(UniverFormulaEnginePlugin) // [!code --]
univer.registerPlugin(UniverProFormulaEnginePlugin) // [!code ++]
```

Si vous disposez d’une licence commerciale Univer, consultez [Utiliser une licence côté client](/guides/pro/license#in-plugin-mode) pour la configuration.

{/* ### Plugins et configuration */}
