---
title: Graphiques
icon: '#pro/ChartPie'
---

<MetaData
  lang="fr-FR"
  isPro
  meta={{
    preset: [{
      client: '@univerjs/preset-sheets-advanced',
      locale: '@univerjs/preset-sheets-advanced/locales/fr-FR',
      style: '@univerjs/preset-sheets-advanced/lib/index.css',
    }],
    plugins: [{
      client: '@univerjs-pro/chart-ui',
      locale: '@univerjs-pro/chart-ui/locale/fr-FR',
      style: '@univerjs-pro/chart-ui/lib/index.css',
    }, {
      client: '@univerjs-pro/sheets-chart',
      locale: '@univerjs-pro/sheets-chart/locale/fr-FR',
      facade: '@univerjs-pro/sheets-chart/facade',
    }, {
      client: '@univerjs-pro/sheets-chart-ui',
      locale: '@univerjs-pro/sheets-chart-ui/locale/fr-FR',
      style: '@univerjs-pro/sheets-chart-ui/lib/index.css',
    }],
    server: false,
  }}
/>

Les graphiques visualisent les données de la feuille de calcul grâce aux packages de modèle et d’interface.

<PlaygroundFrame lang="fr-FR" slug="sheets/charts" clickToShow />

## Mode preset

La fonctionnalité est incluse dans `@univerjs/preset-sheets-advanced` et nécessite le preset de dessin à l’exécution.

```package-install
npm install @univerjs/preset-sheets-drawing @univerjs/preset-sheets-advanced
```

## Mode plugin

```package-install
npm install @univerjs-pro/chart-ui @univerjs-pro/sheets-chart @univerjs-pro/sheets-chart-ui
```

```ts
import ChartUIFrFR from '@univerjs-pro/chart-ui/locale/fr-FR'
import { UniverSheetsChartPlugin } from '@univerjs-pro/sheets-chart'
import { UniverSheetsChartUIPlugin } from '@univerjs-pro/sheets-chart-ui'
import SheetsChartUIFrFR from '@univerjs-pro/sheets-chart-ui/locale/fr-FR'
import SheetsChartFrFR from '@univerjs-pro/sheets-chart/locale/fr-FR'
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'

import '@univerjs-pro/sheets-chart/facade'
import '@univerjs-pro/chart-ui/lib/index.css'
import '@univerjs-pro/sheets-chart-ui/lib/index.css'

const univer = new Univer({
  locale: LocaleType.FR_FR,
  locales: {
    [LocaleType.FR_FR]: mergeLocales(ChartUIFrFR, SheetsChartFrFR, SheetsChartUIFrFR),
  },
})

univer.registerPlugin(UniverSheetsChartPlugin)
univer.registerPlugin(UniverSheetsChartUIPlugin)
```

## Créer et mettre à jour un graphique

Gérez les graphiques avec la collection `charts` de la feuille. `create()` renvoie un générateur détaché, puis `insert()` l’insère dans la feuille.

```ts
const worksheet = univerAPI.getActiveWorkbook().getActiveSheet()

const builder = worksheet.charts.create()
  .setType(univerAPI.Enum.ChartTypeString.Column)
  .setSource({
    range: 'A1:D6',
    orientation: univerAPI.Enum.ChartSourceOrientation.Column,
  })
  .setTitle('Revenus trimestriels')
  .setAbsolutePosition(80, 120)
  .setSize(520, 320)

const chart = await worksheet.charts.insert(builder)

chart
  .setType(univerAPI.Enum.ChartTypeString.Line)
  .setLegend({ visible: true })
  .setSeries({ index: 0 }, { color: '#2F80ED' })
  .commit()
```

Utilisez `worksheet.charts.list()`, `worksheet.charts.get(id)` et `await worksheet.charts.remove(chartOrId)` pour gérer les graphiques existants. `worksheet.charts.registerTheme(name, theme)` enregistre un thème ECharts partagé par le runtime Univer.

## Types de graphiques pris en charge

`univerAPI.Enum.ChartTypeString` fournit `Line`, `Column`, `ColumnStacked`, `ColumnPercentStacked`, `Bar`, `BarStacked`, `BarPercentStacked`, `Pie`, `Donut`, `Area`, `AreaStacked`, `AreaPercentStacked`, `Radar`, `Scatter`, `Combination`, `WordCloud`, `Funnel`, `Bubble`, `Relation`, `Waterfall`, `Pareto`, `Sankey`, `Heatmap`, `Boxplot`, `Candlestick`, `Histogram`, `Treemap`, `Sunburst`, `Gauge` et `Chord`.

Consultez la [référence Facade des graphiques](/reference/facade/chart) pour la configuration commune des générateurs.
