---
title: Guía de uso de texto enriquecido
---

Univer proporciona potentes funciones de texto enriquecido que se pueden usar en varios escenarios, incluidos contenido de celda, comentarios e hipervínculos. Esta guía presentará cómo utilizar la funcionalidad de texto enriquecido.

## API de Facade

API de Facade proporciona una serie de métodos para crear objetos relacionados con texto enriquecido:

```typescript
// Create rich text
const richText = univerAPI.newRichText()
const richTextValue = univerAPI.newRichTextValue({ body: { dataStream: 'Hello Univer\r\n' } })

// Create paragraph style
const paragraphStyle = univerAPI.newParagraphStyle()
const paragraphStyleValue = univerAPI.newParagraphStyleValue()

// Create text style
const textStyle = univerAPI.newTextStyle()
const textStyleValue = univerAPI.newTextStyleValue()

// Create text decoration
const decoration = univerAPI.newTextDecoration()
```

Cada método de creación tiene versiones de Builder y Value correspondientes:
- Las versiones de Builder (como `newRichText`) se usan para crear y modificar estilos
- Las versiones de Value (como `newRichTextValue`) se usan para crear objetos de estilo de solo lectura

## Conceptos básicos

El sistema de texto enriquecido consta principalmente de dos componentes principales:

1. **TextStyle (estilo de texto)**: controla la apariencia del texto, como la fuente, el tamaño, el color, etc.
2. **ParagraphStyle (Estilo de párrafo)**: controla el formato a nivel de párrafo, como alineación, sangría, interlineado, etc.

## Configuración de estilo de texto

### Creación de estilos de texto

```typescript
// Create a basic text style
const textStyle = univerAPI.newTextStyle()
  .setFontFamily('Arial')
  .setFontSize(12)
  .setItalic(true)
  .setBold(true)
  .build()

// Create a text style with colors
const coloredStyle = univerAPI.newTextStyle()
  .setColor({ rgb: '#FF0000' }) // Set red text
  .setBackground({ rgb: '#FFFF00' }) // Set yellow background
  .build()
```

### Decoraciones de texto

```typescript
// Add underline
const underlineStyle = univerAPI.newTextStyle()
  .setUnderline(
    univerAPI.newTextDecoration()
      .setShow(true)
      .setColor({ rgb: '#0000FF' })
      .setLineType(univerAPI.Enum.TextDecoration.SINGLE),
  )
  .build()

// Add strikethrough
const strikethroughStyle = univerAPI.newTextStyle()
  .setStrikethrough(
    univerAPI.newTextDecoration()
      .setShow(true),
  )
  .build()
```

## Configuración de estilo de párrafo

### Creación de estilos de párrafo

```typescript
// Create a basic paragraph style
const paragraphStyle = univerAPI.newParagraphStyle()
  .setHorizontalAlign(univerAPI.Enum.HorizontalAlign.CENTER) // Center alignment
  .setLineSpacing(1.5) // 1.5 line spacing
  .build()

// Set paragraph indentation
const indentedStyle = univerAPI.newParagraphStyle()
  .setIndentFirstLine({ value: 2, unit: 'cm' }) // First line indent 2cm
  .setIndentStart({ value: 1, unit: 'cm' }) // Left indent 1cm
  .build()
```

## Casos de uso

### 1. Texto enriquecido de celda

Hay dos formas de configurar texto enriquecido para celdas: [`setRichTextValueForCell`](https://docs.univer.ai/reference/facade/range#setrichtextvalueforcell) y [`setRichTextValues`](https://docs.univer.ai/reference/facade/range#setrichtextvalues).

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:B2')
console.log(fRange.getValue(true))

// Set A1 cell value to rich text
const richText = univerAPI.newRichText()
  .insertText('Hello World')
  .setStyle(0, 1, { bl: 1, cl: { rgb: '#c81e1e' } })
  .setStyle(6, 7, { bl: 1, cl: { rgb: '#c81e1e' } })
fRange.setRichTextValueForCell(richText)
console.log(fRange.getValue(true).toPlainText()) // Hello World

// Set A1:B2 cell value to rich text
fRange.setRichTextValues([
  [richText, richText],
  [null, null],
])
console.log(fRange.getValue(true).toPlainText()) // Hello World
```

### 2. Comentar texto enriquecido

```typescript
// Add formatted comments
const range = univerAPI.getActiveWorkbook()
  .getActiveSheet()
  .getActiveRange()

// Create comment content
const comment = univerAPI.newTheadComment()
  .setContent(
    univerAPI.newRichText()
      .insertText('Please check the data in this cell'),
  )

// Add comment asynchronously
const success = await range.addCommentAsync(comment)
if (success) {
  console.log('Comment added successfully')
}
```

### 3. Texto enriquecido de hipervínculo

```typescript
// Create rich text with hyperlink
const range = univerAPI.getActiveWorkbook()
  .getActiveSheet()
  .getActiveRange()

// Create hyperlink using newRichText().insertLink
const richText = univerAPI.newRichText()
  .insertLink('Visit Univer', '/')

// Set to cell
range.setRichTextValueForCell(richText)
```
