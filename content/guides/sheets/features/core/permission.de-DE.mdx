---
title: Berechtigungskontrolle
---

import { MigrationCell, MigrationRow, MigrationTable } from './components/migration-table'

Univer bietet Berechtigungskontrollfunktionen, um Benutzeroperationen an Arbeitsmappen, Arbeitsblättern und Bereichen einzuschränken. Wenn ein Benutzer einen Vorgang ohne die erforderliche Berechtigung versucht, wird die Ausführung angehalten und der Benutzer benachrichtigt. Sie können beispielsweise einen Bereich schützen, um zu steuern, ob Mitarbeiter innerhalb dieses Bereichs bearbeiten, anzeigen, kopieren oder filtern können.

<Callout type="warning" title="Precautions before use">
Univer bietet erweiterbare Grundfunktionen, keine sofort einsatzbereiten benutzerdefinierten Funktionen.
Wenn Sie Persistenz, Organisationsstrukturen oder andere maßgeschneiderte Anforderungen benötigen, müssen Sie die Speicherung von Berechtigungsregeln implementieren und Organisationsstrukturen selbst integrieren – normalerweise durch benutzerdefinierte Plugins.
Daher ist es normal, dass die Berechtigungsliste nach dem Festlegen leer ist oder Benutzerinformationen fehlen Berechtigungen. Diese Daten müssen über Ihre eigenen API-Anfragen abgerufen werden. Dabei handelt es sich um eine benutzerdefinierte Logik, die Sie implementieren müssen. Weitere Informationen finden Sie unten im Abschnitt zur Integration von Drittanbietern.
</Callout>

<PlaygroundFrame lang="en-US" slug="sheets/permission" clickToShow />

## Kernkonzepte

- Geltungsbereich: Berechtigungen gelten auf drei Ebenen – Arbeitsmappe, Arbeitsblatt und Bereich.
- Berechtigungspunkte: Jede Funktion entspricht einem Berechtigungspunkt. Das Ändern eines Punkts steuert, ob dieses Feature verfügbar ist.
- Schutzregeln: Bevor Sie Berechtigungspunkte für Arbeitsblätter und Bereiche festlegen, müssen Sie eine Schutzregel erstellen.

<Callout type="info" title="Permission Point Explanation">
In einer lokalen, nicht persistenten Umgebung (ohne gemeinsame Bearbeitung) können Sie Berechtigungen direkt steuern, indem Sie Berechtigungspunkte ändern. Informationen zu dauerhaften Berechtigungen finden Sie im Abschnitt „Integration eines Drittanbieter-Autorisierungsdienstes“ weiter unten.
In einem kollaborativen Bearbeitungsszenario synchronisiert die Aktualisierung eines Berechtigungspunkts den Serverstatus:
  - `false`: Der Berechtigungspunkt erfordert mindestens die Rolle `owner`
  - `true`: Der Berechtigungspunkt erfordert mindestens die `reader` Rolle
</Callout>

## Grundlegendes Beispiel

Die gängigsten Berechtigungseinstellungsmethoden werden unten auf drei Ebenen gezeigt: Arbeitsmappe, Arbeitsblatt und Bereich.

### Beispiel für einen Arbeitsmappenberechtigungscode

Berechtigungen auf Arbeitsmappenebene können direkt über [Fassaden-API](/guides/sheets/getting-started/facade) festgelegt werden.

Das folgende Beispiel deaktiviert die Bearbeitung für die gesamte Arbeitsmappe. Für andere Funktionen ersetzen Sie den Berechtigungspunkt entsprechend (siehe [Liste der Arbeitsmappen-Berechtigungspunkte](#workbook-point)):

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

// Set the workbook to be non-editable
await workbookPermission.setPoint(univerAPI.Enum.WorkbookPermissionPoint.Edit, false)
```

Wichtige Punkte:
- Arbeitsmappen-Berechtigungspunkte können direkt festgelegt werden, ohne dass Schutzregeln erstellt werden müssen.

### Beispiel für einen Arbeitsblatt-Berechtigungscode

Arbeitsblatt- und Bereichsberechtigungen können über die Facade-API oder Befehle festgelegt werden. Das folgende Beispiel zeigt, wie Berechtigungen zum Bearbeiten von Arbeitsblättern festgelegt werden. Für andere Funktionen ersetzen Sie den Berechtigungspunkt entsprechend (siehe [Liste der Arbeitsblatt-Berechtigungspunkte](#worksheet-point)):

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// Create worksheet protection, allow specified users to edit
const permissionId = await worksheetPermission.protect({
  allowedUsers: ['user1', 'user2'],
  name: 'My Worksheet Protection',
})

// To set permission points, you must first protect the worksheet
await worksheetPermission.setPoint(univerAPI.Enum.WorksheetPermissionPoint.Edit, false)
```

#### Arbeitsblattberechtigungen löschen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// Remove worksheet protection
await worksheetPermission.unprotect()
```

### Codebeispiel für benutzerdefinierte Bereichsberechtigungen

Bereichsberechtigungen können auch über API- und Befehlsmodi festgelegt werden. Hier verwenden wir als Beispiel Bereichsbearbeitungsberechtigungen. Andere Bereichsfunktionen erfordern das Ersetzen der Berechtigungspunkte. Weitere Informationen finden Sie unter [Liste der Berechtigungspunkte – Bereich](#range-point).

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:B2')
const rangePermission = fRange.getRangePermission()

// Set range protection permission, allow specified users to edit but not allow others to view
const rule = await rangePermission.protect({
  name: 'My protected range',
  allowViewByOthers: false,
  allowedUsers: ['user1', 'user2'],
})
console.log(rule)

// To set permission points, you must first create a range protection rule.
await rule.setPoint(univerAPI.Enum.RangePermissionPoint.Edit, false)
await rule.setPoint(univerAPI.Enum.RangePermissionPoint.View, true)
```

#### Bereichsschutzberechtigungen löschen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:B2')
const rangePermission = fRange.getRangePermission()

// delete all rules within the range
await rangePermission.unprotect()

// Or delete specific rules within the range
const rules = await rangePermission.listRules()
if (rules.length > 0) {
  await rules[0].remove()
}
```

## Erweiterte Verwendung

Wenn Sie Berechtigungen in Ihrem eigenen Plugin überprüfen müssen, können Sie die Berechtigungspunkte direkt bearbeiten.

Das folgende Beispiel verwendet `WorkbookEditablePermission`, um zu demonstrieren, wie Sie Berechtigungen in Ihrem eigenen Plugin überprüfen. Andere Berechtigungspunkte funktionieren ähnlich.

```typescript
import { IPermissionService } from '@univerjs/core'
import { WorkbookEditablePermission } from '@univerjs/sheets'

class YourService {
  constructor(@IPermissionService private _permissionService: IPermissionService) {
  }

  setWorkbookNotEditable() {
    this._permissionService.updatePermissionPoint(new WorkbookEditablePermission('unitId').id, false)
  }

  setWorkbookEditable() {
    this._permissionService.updatePermissionPoint(new WorkbookEditablePermission('unitId').id, true)
  }
}
```

Sie können auch andere Berechtigungspunkte erweitern und ändern, um verschiedene Funktionen zu steuern. Die vollständige Liste finden Sie unten auf dieser Seite.

### So erweitern Sie Berechtigungspunkte

Um Berechtigungspunkte anzupassen, implementieren Sie `IPermissionPoint` und registrieren Sie es beim Berechtigungsdienst:

```typescript
import type { IPermissionPoint } from '@univerjs/core'
import { IPermissionService, PermissionStatus } from '@univerjs/core'
import { UnitAction, UnitObject } from '@univerjs/protocol'

export class CustomPermissionPoint implements IPermissionPoint {
  type = UnitObject.Unkonwn // your type
  subType = UnitAction.View // your subType
  status = PermissionStatus.INIT
  value = true // Default value
  id: string
  constructor(unitId: string, subUnitId: string, customId: string) {
    // The id must be unique within `IPermissionService`.
    this.id = `${unitId}.${subUnitId}.${customId}`
  }
}

class YourService {
  constructor(@IPermissionService private _permissionService: IPermissionService) {
    this._init()
  }

  _init() {
    this._permissionService.addPermissionPoint(new CustomPermissionPoint('unitId', 'subUnitId', 'my-id'))
  }
}

// How to use it elsewhere
class ConsumeService {
  constructor(@IPermissionService private _permissionService: IPermissionService) {
  }

  doSomething() {
    const point = this._permissionService.getPermissionPoint(new CustomPermissionPoint('unitId', 'subUnitId', 'my-id').id)
    console.log(point.value)
  }

  bindEvent() {
    // This returns an RxJS observable that lets you listen for permission changes
    const point$ = this._permissionService.getPermissionPoint$(new CustomPermissionPoint('unitId', 'subUnitId', 'my-id').id)
    console.log(point$)
  }
}
```

### Integration eines Drittanbieter-Autorisierungsdienstes (erweitert)

<Callout type="warning" title="Caution">
Es wird empfohlen, den benutzerdefinierten Berechtigungszugriff nicht mit der Permission Facade API zu vermischen, da dies eine detailliertere Steuerung der Berechtigungen ermöglicht Punkte.
</Callout>

Die Logik zur Bestimmung von Berechtigungen wird normalerweise von einem externen Dienst verwaltet, der einen Kommunikationsprozess umfasst. In der Frontend-SDK-Implementierung verwenden wir [AuthzIoLocalService](https://github.com/dream-num/univer/blob/dev/packages/core/src/services/authz-io/authz-io-local.service.ts), um diese Logik zu verarbeiten.

In einer Produktionsumgebung müssen wir diese Implementierung durch einen Backend-Dienst ersetzen. Das Frontend muss die entsprechenden Anforderungsfunktionen basierend auf der Schnittstelle [IAuthzIoService](https://github.com/dream-num/univer/blob/dev/packages/core/src/services/authz-io/type.ts) für den Laufzeitaustausch implementieren.

![Berechtigung](./permission/permission-en.png)

Hier ist ein einfaches Beispiel, das das Hinzufügen und Löschen von Berechtigungen für geschützte Bereiche für zwei vordefinierte Rollen (Besitzer/Leser) demonstriert. Der Eigentümer verfügt über Bearbeitungs-/Anzeigeberechtigungen für geschützte Bereiche, während der Leser den Inhalt von Zellen innerhalb geschützter Bereiche nicht bearbeiten oder anzeigen kann.

```typescript
import type { Injector } from '@univerjs/core'
import type { IActionInfo, IAllowedRequest, IBatchAllowedResponse, ICollaborator, ICreateRequest, ICreateRequest_SelectRangeObject, IListPermPointRequest, IPermissionPoint, IPutCollaboratorsRequest, IUnitRoleKV, IUpdatePermPointRequest } from '@univerjs/protocol'
import { createDefaultUser, generateRandomId, IAuthzIoService, Inject, IResourceManagerService, isDevRole, Univer, UserManagerService } from '@univerjs/core'
import { ObjectScope, UnitAction, UnitObject, UnitRole, UniverType } from '@univerjs/protocol'

class YourAuthzService implements IAuthzIoService {
  private _permissionMap: Map<string, ICreateRequest_SelectRangeObject & { objectType: UnitObject }> = new Map([])

  constructor(
    @IResourceManagerService private _resourceManagerService: IResourceManagerService,
    @Inject(UserManagerService) private _userManagerService: UserManagerService,
  ) {
    this._initSnapshot()
    this._initDefaultUser()
  }

  private _initDefaultUser() {
    const currentUser = this._userManagerService.getCurrentUser()
    const currentUserIsValid = currentUser && currentUser.userID
    if (!currentUserIsValid) {
      this._userManagerService.setCurrentUser(createDefaultUser(UnitRole.Owner))
    }
  }

  private _getRole(type: UnitRole) {
    const user = this._userManagerService.getCurrentUser()
    if (!user) {
      return false
    }
    return isDevRole(user.userID, type)
  }

  private _initSnapshot() {
    this._resourceManagerService.registerPluginResource({
      toJson: (_unitId: string) => {
        const obj = [...this._permissionMap.keys()].reduce((r, k) => {
          const v = this._permissionMap.get(k)
          r[k] = v!
          return r
        }, {} as Record<string, ICreateRequest_SelectRangeObject & { objectType: UnitObject }>)
        return JSON.stringify(obj)
      },
      parseJson: (json: string) => {
        return JSON.parse(json)
      },
      pluginName: 'SHEET_AuthzIoMockService_PLUGIN',
      businesses: [UniverType.UNIVER_SHEET, UniverType.UNIVER_DOC, UniverType.UNIVER_SLIDE],
      onLoad: (_unitId, resource) => {
        for (const key in resource) {
          this._permissionMap.set(key, resource[key])
        }
      },
      onUnLoad: () => {
        this._permissionMap.clear()
      },
    })
  }

  async create(config: ICreateRequest): Promise<string> {
    const permissionId = generateRandomId(8)
    if (config.objectType === UnitObject.SelectRange && config.selectRangeObject) {
      this._permissionMap.set(permissionId, { ...config.selectRangeObject, objectType: config.objectType })
    }
    return permissionId
  }

  async batchAllowed(config: IAllowedRequest[]): Promise<IBatchAllowedResponse['objectActions']> {
    const selectionRangeConfig = config.filter(c => c.objectType === UnitObject.SelectRange)
    if (selectionRangeConfig.length) {
      const currentUser = this._userManagerService.getCurrentUser()
      const res = [] as IBatchAllowedResponse['objectActions']
      selectionRangeConfig.forEach((c) => {
        res.push({
          unitID: c.unitID,
          objectID: c.objectID,
          actions: c.actions.map((action) => {
            if (isDevRole(currentUser.userID, UnitRole.Owner)) {
              return { action, allowed: true }
            }
            return { action, allowed: false }
          }),
        })
      })
      return res
    }
    return Promise.resolve([])
  }

  async list(config: IListPermPointRequest): Promise <IPermissionPoint[]> {
    const result: IPermissionPoint[] = []
    config.objectIDs.forEach((objectID) => {
      const rule = this._permissionMap.get(objectID)
      if (rule) {
        const item = {
          objectID,
          unitID: config.unitID,
          objectType: rule!.objectType,
          name: rule!.name,
          shareOn: false,
          shareRole: UnitRole.Owner,
          shareScope: -1,
          scope: {
            read: ObjectScope.AllCollaborator,
            edit: ObjectScope.AllCollaborator,
          },
          creator: createDefaultUser(UnitRole.Owner),
          strategies: [
            {
              action: UnitAction.View,
              role: UnitRole.Owner,
            },
            {
              action: UnitAction.Edit,
              role: UnitRole.Owner,
            },
          ],
          actions: config.actions.map((a) => {
            return { action: a, allowed: this._getRole(UnitRole.Owner) }
          }),
        }
        result.push(item)
      }
    })
    return result
  }

  async listCollaborators(): Promise<ICollaborator[]> {
    // List the existing collaborators
    return []
  }

  async allowed(_config: IAllowedRequest): Promise<IActionInfo[]> {
    // Because this is a mockService for handling permissions, we will not write real logic in it. We will only return an empty array to ensure that the permissions originally set by the user are not modified.
    // If you want to achieve persistence of permissions, you can modify the logic here.
    return Promise.resolve([])
  }

  async listRoles(): Promise<{ roles: IUnitRoleKV[], actions: UnitAction[] }> {
    return {
      roles: [],
      actions: [],
    }
  }

  async update(config: IUpdatePermPointRequest): Promise<void> {
    // Update bit information
  }

  async updateCollaborator(): Promise<void> {
    // Update collaborator information
    return undefined
  }

  async createCollaborator(): Promise<void> {
    // Create new collaborator information
    return undefined
  }

  async deleteCollaborator(): Promise<void> {
    return undefined
  }

  async putCollaborators(config: IPutCollaboratorsRequest): Promise<void> {
    return undefined
  }
}

export class YourPlugin extends Plugin {
  constructor(
    _config: unknown,
    @Inject(Injector) protected override _injector: Injector,
  ) {
  }

  override onStarting(): void {
    this._injector.add([IAuthzIoService, { useClass: YourAuthzService }])
  }
}

// By setting the override option to [[IAuthzIoService, null]], you can instruct Univer not to register the built-in IAuthzIoService.
// This way, Univer will use the service provided by YourAuthzService as the implementation of the authorization service.
const univer = new Univer({
  override: [[IAuthzIoService, null]],
})

univer.registerPlugin(YourPlugin)
```

## Liste der Berechtigungspunkte [#point-list]

Sie können den Quellcode für alle Berechtigungspunkte anzeigen [Hier](https://github.com/dream-num/univer/tree/dev/packages/sheets/src/services/permission/permission-point).

Wenn sich Arbeitsmappenberechtigungen mit Arbeitsblatt- oder Bereichsberechtigungen überschneiden, müssen **alle** relevanten Berechtigungen auf festgelegt werden `true`, damit die Funktion nutzbar ist.

### Arbeitsmappenberechtigungen [#workbook-point]

| API-Aufzählung  | Entsprechende Berechtigungspunktklasse | Beschreibung |
| --- | --- | --- |
| univerAPI.Enum.WorkbookPermissionPoint.Edit | WorkbookEditablePermission | Kann bearbeiten |
| univerAPI.Enum.WorkbookPermissionPoint.View | WorkbookViewPermission | Kann anzeigen |
| univerAPI.Enum.WorkbookPermissionPoint.Print | WorkbookPrintPermission | Kann drucken |
| univerAPI.Enum.WorkbookPermissionPoint.Export | WorkbookExportPermission | Kann exportieren |
| univerAPI.Enum.WorkbookPermissionPoint.Share | WorkbookSharePermission | Kann teilen |
| univerAPI.Enum.WorkbookPermissionPoint.CopyContent | WorkbookCopyPermission | Kann kopieren |
| univerAPI.Enum.WorkbookPermissionPoint.DuplicateFile | WorkbookDuplicatePermission | Kann duplizieren |
| univerAPI.Enum.WorkbookPermissionPoint.Comment | WorkbookCommentPermission | Kann kommentieren |
| univerAPI.Enum.WorkbookPermissionPoint.ManageCollaborator | WorkbookManageCollaboratorPermission | Kann Mitarbeiter verwalten |
| univerAPI.Enum.WorkbookPermissionPoint.CreateSheet | WorkbookCreateSheetPermission | Kann Arbeitsblätter erstellen |
| univerAPI.Enum.WorkbookPermissionPoint.DeleteSheet | WorkbookDeleteSheetPermission | Kann Arbeitsblätter löschen |
| univerAPI.Enum.WorkbookPermissionPoint.RenameSheet | WorkbookRenameSheetPermission | Kann Arbeitsblätter umbenennen |
| univerAPI.Enum.WorkbookPermissionPoint.MoveSheet | WorkbookMoveSheetPermission | Kann Arbeitsblätter verschieben |
| univerAPI.Enum.WorkbookPermissionPoint.HideSheet | WorkbookHideSheetPermission | Kann Arbeitsblätter ausblenden |
| univerAPI.Enum.WorkbookPermissionPoint.CopySheet | WorkbookCopySheetPermission | Kann Arbeitsblätter kopieren |
| univerAPI.Enum.WorkbookPermissionPoint.ViewHistory | WorkbookViewHistoryPermission | Kann den Verlauf anzeigen |
| univerAPI.Enum.WorkbookPermissionPoint.RecoverHistory | WorkbookRecoverHistoryPermission | Kann den Verlauf wiederherstellen |
| univerAPI.Enum.WorkbookPermissionPoint.CreateProtection | WorkbookCreateProtectPermission | Kann Schutz erstellen |
| univerAPI.Enum.WorkbookPermissionPoint.InsertRow | WorkbookInsertRowPermission | Kann Zeilen einfügen |
| univerAPI.Enum.WorkbookPermissionPoint.InsertColumn | WorkbookInsertColumnPermission | Kann Spalten einfügen |
| univerAPI.Enum.WorkbookPermissionPoint.DeleteRow | WorkbookDeleteRowPermission | Kann Zeilen löschen |
| univerAPI.Enum.WorkbookPermissionPoint.DeleteColumn | WorkbookDeleteColumnPermission | Kann Spalten löschen |

### Arbeitsblattberechtigungen [#worksheet-point]

| API Enum  | Entsprechende Berechtigungspunktklasse | Beschreibung |
| --- | --- | --- |
| univerAPI.Enum.WorksheetPermissionPoint.Edit | WorksheetEditPermission | Kann bearbeiten |
| univerAPI.Enum.WorksheetPermissionPoint.View | WorksheetViewPermission | Kann Ansicht |
| univerAPI.Enum.WorksheetPermissionPoint.Copy | WorksheetCopyPermission | Kann kopieren |
| univerAPI.Enum.WorksheetPermissionPoint.SetCellValue | WorksheetSetCellValuePermission | Kann Zellwerte bearbeiten |
| univerAPI.Enum.WorksheetPermissionPoint.SetCellStyle | WorksheetSetCellStylePermission | Kann Zellstile bearbeiten |
| univerAPI.Enum.WorksheetPermissionPoint.SetRowStyle | WorksheetSetRowStylePermission | Kann Zeilenstile festlegen |
| univerAPI.Enum.WorksheetPermissionPoint.SetColumnStyle | WorksheetSetColumnStylePermission | Kann Spaltenstile festlegen |
| univerAPI.Enum.WorksheetPermissionPoint.InsertRow | WorksheetInsertRowPermission | Kann Zeilen einfügen |
| univerAPI.Enum.WorksheetPermissionPoint.InsertColumn | WorksheetInsertColumnPermission | Kann Spalten einfügen |
| univerAPI.Enum.WorksheetPermissionPoint.DeleteRow | WorksheetDeleteRowPermission | Kann Zeilen löschen |
| univerAPI.Enum.WorksheetPermissionPoint.DeleteColumn | WorksheetDeleteColumnPermission | Kann Spalten löschen |
| univerAPI.Enum.WorksheetPermissionPoint.Sort | WorksheetSortPermission | Kann sortieren |
| univerAPI.Enum.WorksheetPermissionPoint.Filter | WorksheetFilterPermission | Kann filtern |
| univerAPI.Enum.WorksheetPermissionPoint.PivotTable | WorksheetPivotTablePermission | Kann Pivot-Tabellen verwenden |
| univerAPI.Enum.WorksheetPermissionPoint.InsertHyperlink | WorksheetInsertHyperlinkPermission | Kann Hyperlinks verwenden |
| univerAPI.Enum.WorksheetPermissionPoint.ManageCollaborator | WorksheetManageCollaboratorPermission | Kann verwalten Mitarbeiter |
| univerAPI.Enum.WorksheetPermissionPoint.DeleteProtection | WorksheetDeleteProtectionPermission | Kann den Schutz löschen |
| univerAPI.Enum.WorksheetPermissionPoint.EditExtraObject | WorksheetEditExtraObjectPermission | Kann zusätzliche Objekte bearbeiten |
| univerAPI.Enum.WorksheetPermissionPoint.SelectProtectedCells | WorksheetSelectProtectedCellsPermission | Kann geschützte Zellen auswählen |
| univerAPI.Enum.WorksheetPermissionPoint.SelectUnProtectedCells | WorksheetSelectUnProtectedCellsPermission | Kann ungeschützte Zellen auswählen |

### Bereichsschutz [#range-point]

| API-Aufzählung  | Entsprechende Berechtigungspunktklasse | Beschreibung |
| --- | --- | --- |
| univerAPI.Enum.RangePermissionPoint.Edit | RangeProtectionPermissionEditPoint | Kann geschützte Bereiche bearbeiten |
| univerAPI.Enum.RangePermissionPoint.View | RangeProtectionPermissionViewPoint | Kann Inhalte geschützter Bereiche anzeigen |
| univerAPI.Enum.RangePermissionPoint.Delete | RangeProtectionPermissionDeleteProtectionPoint | Kann geschützte Bereiche löschen |
| univerAPI.Enum.RangePermissionPoint.ManageCollaborator | RangeProtectionPermissionManageCollaPoint | Kann Mitarbeiter für geschützte Bereiche verwalten Bereiche |

## Konfiguration

### Schutzbereichsschattenstrategie

Der geschützte Bereichsschatten wird standardmäßig angezeigt. Sie können es mit der folgenden Konfiguration ausblenden:

<Tabs items={['Preset Mode', 'Plugin Mode']}>
  <Tab>
    ```typescript
    createUniver({
      presets: [
        UniverSheetsCorePreset({
          sheets: {
            /**
             * Strategy for showing the protected range shadow.
             * - true or 'always': Show shadow for all protected ranges (default behavior)
             * - 'non-editable': Only show shadow for ranges that cannot be edited (Edit permission is false)
             * - 'non-viewable': Only show shadow for ranges that cannot be viewed (View permission is false)
             * - false or 'none': Never show shadow for protected ranges
             * @default true
             */
            protectedRangeShadow: false,
          },
        }),
      ],
    })
    ```
  </Tab>
  <Tab>
    ```typescript
    univer.registerPlugin(UniverSheetsUIPlugin, {
      /**
       * Strategy for showing the protected range shadow.
       * - true or 'always': Show shadow for all protected ranges (default behavior)
       * - 'non-editable': Only show shadow for ranges that cannot be edited (Edit permission is false)
       * - 'non-viewable': Only show shadow for ranges that cannot be viewed (View permission is false)
       * - false or 'none': Never show shadow for protected ranges
       * @default true
       */
      protectedRangeShadow: false,
    })
    ```
  </Tab>
</Tabs>

Sie können es auch dynamisch über die API ändern:

```typescript
// Set to only show shadows for non-editable ranges
univerAPI.setProtectedRangeShadowStrategy('non-editable')

// Get the current protected range shadow display strategy
console.log(univerAPI.getProtectedRangeShadowStrategy())

// Subscribe to changes in the protected range shadow strategy
const subscription = univerAPI.getProtectedRangeShadowStrategy$().subscribe((strategy) => {
  console.log('Global strategy changed to:', strategy)
  // Update UI or perform other actions
})

// Later, unsubscribe to clean up
subscription.unsubscribe()
```

### Benutzerdefinierte Benutzerkomponente

Der integrierte Benutzerselektor von Univer bietet nur begrenzte Flexibilität. Für komplexere Szenarien können Sie wie unten gezeigt eine benutzerdefinierte Komponente bereitstellen.

<Callout>
Sie können darauf verweisen [Benutzerdefinierte Komponenten](/guides/sheets/ui/components) für die Erstellung benutzerdefinierter Komponenten.
</Callout>

<Tabs items={['Preset Mode', 'Plugin Mode']}>
  <Tab>
    ```typescript
    const { univer } = createUniver({
      presets: [
        UniverSheetsCorePreset({
          sheets: {
            protectedRangeUserSelector: {
            /**
             * Custom component; must implement the `IPermissionDetailUserPartProps` interface.
             */
              component: CustomPermissionDetailUserPart,
              /**
               * The framework of the component. Must be passed correctly.
               */
              framework: 'react',
            },
          },
        }),
      ],
    })
    ```
  </Tab>
  <Tab>
    ```typescript
    univer.registerPlugin(UniverSheetsUIPlugin, {
      protectedRangeUserSelector: {
        /**
         * Custom component; must implement the `IPermissionDetailUserPartProps` interface.
         */
        component: CustomPermissionDetailUserPart,
        /**
         * The framework of the component. Must be passed correctly.
         */
        framework: 'react',
      },
    })
    ```
  </Tab>
</Tabs>

Nachdem Sie Ihre benutzerdefinierte Benutzerkomponente konfiguriert haben, synchronisieren Sie die ausgewählte Benutzerliste zur späteren Verwendung über `setSelectUserList()` mit `SheetPermissionUserManagerService`.

<Tabs items={['Preset Mode', 'Plugin Mode']}>
  <Tab>
    ```typescript
    import { SheetPermissionUserManagerService, useDependency } from '@univerjs/preset-sheets-core'

    const sheetPermissionUserManagerService = useDependency(SheetPermissionUserManagerService)
    // For the specific data structure, refer to: https://github.com/dream-num/univer/blob/b4d4cfa063c9e6d5a82d1fc6b05edc206a415252/packages/sheets-ui/src/services/permission/sheet-permission-user-list.service.ts#L57
    sheetPermissionUserManagerService.setSelectUserList([])
    ```
  </Tab>
  <Tab>
    ```typescript
    import { SheetPermissionUserManagerService } from '@univerjs/sheets-ui'
    import { useDependency } from '@univerjs/ui'

    const sheetPermissionUserManagerService = useDependency(SheetPermissionUserManagerService)
    // For the specific data structure, refer to: https://github.com/dream-num/univer/blob/b4d4cfa063c9e6d5a82d1fc6b05edc206a415252/packages/sheets-ui/src/services/permission/sheet-permission-user-list.service.ts#L57
    sheetPermissionUserManagerService.setSelectUserList([])
    ```
  </Tab>
</Tabs>

## Facade API

Die Facade API bietet einen einheitlichen Einstiegspunkt für Berechtigungen. Im Folgenden gruppieren wir gängige Methoden nach Arbeitsmappe, Arbeitsblatt und Bereich.

### Arbeitsmappenberechtigungen

#### Arbeitsmappenberechtigungsinstanz abrufen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()
```

#### Arbeitsmappenberechtigungspunkt festlegen

Die Enumerationswerte `WorkbookPermissionPoint` finden Sie in der [Liste der Arbeitsmappen-Berechtigungspunkte](#workbook-point).

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

// Disable the workbook print permission
await workbookPermission.setPoint(univerAPI.Enum.WorkbookPermissionPoint.Print, false)
```

Vordefinierte Modi:

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

// Set to owner mode
await workbookPermission.setMode('owner')

// Set to editor mode
await workbookPermission.setMode('editor')

// Set to viewer mode
await workbookPermission.setMode('viewer')

// Set to commenter mode
await workbookPermission.setMode('commenter')
```

Verknüpfung Methoden:

- `FWorkbookPermission.setReadOnly()`: Legt den schreibgeschützten Modus fest (entspricht `setMode('viewer')`)
- `FWorkbookPermission.setEditable()`: Legt den bearbeitbaren Modus fest (entspricht `setMode('editor')`)

#### Status des Arbeitsmappen-Berechtigungspunkts abrufen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

// Check whether the workbook can be printed
const canPrint = workbookPermission.getPoint(univerAPI.Enum.WorkbookPermissionPoint.Print)
console.log('Can print:', canPrint)

// Get the status of all workbook permission points
const snapshot = workbookPermission.getSnapshot()
console.log('Workbook permission snapshot:', snapshot)
```

Verknüpfung Methoden:

- `FWorkbookPermission.canEdit()`: Gibt `true` zurück, wenn die Arbeitsmappe bearbeitbar ist

#### Mitarbeiterverwaltung

<Callout type="warning" title="Caution">
Mitarbeiterverwaltungs-APIs sind nur in Szenarios zur kollaborativen Bearbeitung mit integriertem USIP-Dienst verfügbar.
Wenn Sie einen benutzerdefinierten Berechtigungsdienst verwenden, implementieren Sie die Mitarbeiterverwaltungslogik selbst, wie im Abschnitt „Integration eines Drittanbieters“ beschrieben Abschnitt „Autorisierungsdienst“ oben.
</Callout>

- Liste der Mitarbeiter abrufen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

const collaborators = await workbookPermission.listCollaborators()
console.log(collaborators)
```

- Mitarbeiter hinzufügen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

// Add multiple collaborators at once
await workbookPermission.setCollaborators([
  {
    user: { userID: 'user1', name: 'John Doe', avatar: 'https://...' },
    role: univerAPI.Enum.UnitRole.Editor,
  },
  {
    user: { userID: 'user2', name: 'Jane Smith', avatar: '' },
    role: univerAPI.Enum.UnitRole.Reader,
  },
])

// Add a single collaborator
await workbookPermission.addCollaborator(
  { userID: 'user1', name: 'John Doe', avatar: 'https://...' },
  univerAPI.Enum.UnitRole.Editor,
)
```

- Mitarbeiterrolle aktualisieren

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

await workbookPermission.updateCollaborator(
  { userID: 'user1', name: 'John Doe Updated', avatar: 'https://...' },
  univerAPI.Enum.UnitRole.Reader,
)
```

- Mitarbeiter entfernen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const workbookPermission = fWorkbook.getWorkbookPermission()

// Remove multiple collaborators
await workbookPermission.removeCollaborators(['user1', 'user2'])

// Remove a single collaborator
await workbookPermission.removeCollaborator('user1')
```

### Arbeitsblattberechtigungen

#### Arbeitsblattberechtigungsinstanz abrufen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()
```

#### Arbeitsblattschutz

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// Protect the worksheet, allowing only specified users to edit
const permissionId = await worksheetPermission.protect({
  allowedUsers: ['user1', 'user2'],
  name: 'My Worksheet Protection',
})

// Check if the worksheet is protected
const isProtected = worksheetPermission.isProtected()
console.log('Is worksheet protected:', isProtected)

if (isProtected) {
  // Unprotect the worksheet
  await worksheetPermission.unprotect()
}
```

#### Arbeitsblattberechtigungspunkt festlegen

Die Enumerationswerte `WorksheetPermissionPoint` finden Sie in der [Liste der Arbeitsblatt-Berechtigungspunkte](#worksheet-point).

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// To set permission points, you must first protect the worksheet
await worksheetPermission.protect()

// Disable the worksheet insert-row permission
await worksheetPermission.setPoint(univerAPI.Enum.WorksheetPermissionPoint.InsertRow, false)
```

Verwendung vordefinierter Modi:

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// To set permission points, you must first protect the worksheet
await worksheetPermission.protect()

// Set to editable mode
await worksheetPermission.setMode('editable')

// Set to read-only mode
await worksheetPermission.setMode('readOnly')

// Set to filter/sort only mode
await worksheetPermission.setMode('filterOnly')

// Apply a custom configuration
await worksheetPermission.applyConfig({
  mode: 'readOnly',
  points: {
    [univerAPI.Enum.WorksheetPermissionPoint.InsertRow]: true,
    [univerAPI.Enum.WorksheetPermissionPoint.InsertColumn]: true,
  },
})
```

Verknüpfungsmethoden:

- `FWorksheetPermission.setReadOnly()`: Legt den schreibgeschützten Modus fest (entspricht `setMode('readOnly')`)
- `FWorksheetPermission.setEditable()`: Legt den bearbeitbaren Modus fest (entspricht `setMode('editable')`)

#### Status des Arbeitsblatt-Berechtigungspunkts abrufen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// Check whether the worksheet can insert rows
const canInsertRow = worksheetPermission.getPoint(univerAPI.Enum.WorksheetPermissionPoint.InsertRow)

// Get the status of all worksheet permission points
const snapshot = worksheetPermission.getSnapshot()
```

Verwenden von Verknüpfungsmethoden:

- `FWorksheetPermission.canEdit(): boolean`: Überprüfen Sie, ob das Arbeitsblatt vorhanden ist bearbeitbar
- `FWorksheetPermission.canEditCell(row: number, col: number): boolean`: Überprüfen Sie, ob eine bestimmte Zelle bearbeitet werden kann
- `FWorksheetPermission.canView(): boolean`: Überprüfen Sie, ob das Arbeitsblatt sichtbar ist
- `FWorksheetPermission.canViewCell(row: number, col: number): boolean`: Überprüfen Sie, ob eine bestimmte Zelle sichtbar ist
- `FWorksheetPermission.debugCellPermission(row: number, col: number): ICellPermissionDebugInfo`: Berechtigungs-Debug-Informationen für eine bestimmte abrufen Zelle

### Bereichsberechtigungen

#### Bereichsberechtigungsinstanz abrufen

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:B2')
const rangePermission = fRange.getRangePermission()
```

#### Bereichsschutz festlegen

- Bereiche über die API auf Arbeitsblattebene schützen:

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()

// Protect two ranges: A1:B2 is viewable by everyone but not editable,
// while C3:D4 is editable only by specified users and hidden from others
const rules = await worksheetPermission.protectRanges([
  {
    ranges: [fWorksheet.getRange('A1:B2')],
    options: { name: 'Protected Area 1', allowViewByOthers: true },
  },
  {
    ranges: [fWorksheet.getRange('C3:D4')],
    options: { name: 'Protected Area 2', allowViewByOthers: false, allowedUsers: ['user1'] },
  },
])
console.log(rules)

// List all protection rules on the worksheet
const rules = await worksheetPermission.listRangeProtectionRules()
console.log(rules)

// Delete the first rule
await worksheetPermission.unprotectRules([rules[0].id])

// Or delete a rule via `FRangeProtectionRule.remove()`
await ruleList[0].remove()
```

- Bereich über die API auf Bereichsebene schützen:

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:B2')
const rangePermission = fRange.getRangePermission()

// Protect the range so only specified users can edit it, while others cannot view it
const rule = await rangePermission.protect({
  name: 'My protected range',
  allowViewByOthers: false,
  allowedUsers: ['user1', 'user2'],
})
console.log(rule)

// List all protection rules covering the range
const rules = await rangePermission.listRules()
console.log(rules)

// Delete all protection rules covering the range
await rangePermission.unprotect()

// Or delete a rule via `FRangeProtectionRule.remove()`
await rules[0].remove()
```

#### Bereichsberechtigungspunkt festlegen

Die Enumerationswerte `RangePermissionPoint` finden Sie in den [Liste der Bereichsschutzpunkte](#range-point).

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const fRange = fWorksheet.getRange('A1:B2')
const rangePermission = fRange.getRangePermission()

// To set permission points, you must first create a range protection rule
if (rangePermission.isProtected()) {
  const rules = await rangePermission.listRules()

  // Disable editing but enable viewing for the first protection rule
  await rules[0].setPoint(univerAPI.Enum.RangePermissionPoint.Edit, false)
  await rules[0].setPoint(univerAPI.Enum.RangePermissionPoint.View, true)
} else {
  const rule = await rangePermission.protect({
    name: 'My protected range',
    allowViewByOthers: false,
    allowedUsers: ['user1', 'user2'],
  })

  // Disable editing but enable viewing for the A1:B2 range
  await rule.setPoint(univerAPI.Enum.RangePermissionPoint.Edit, false)
  await rule.setPoint(univerAPI.Enum.RangePermissionPoint.View, true)
}
```

### Bereichsberechtigungsregel-API

`FRangeProtectionRule` stellt die folgenden Mitglieder bereit:

| API | Beschreibung |
| --- | --- |
| `id: string` | Die eindeutige Kennung der Schutzregel |
| `permissionId: string` | Die mit der Schutzregel verknüpfte Berechtigungs-ID |
| `ranges: FRange[]` | Die von der Schutzregel abgedeckten Bereiche |
| `options: IRangeProtectionOptions` | Die Optionen der Schutzregel, einschließlich des Namens der Regel, ob andere sie anzeigen dürfen, und der Liste der Benutzer, die zum Bearbeiten berechtigt sind |
| `updateRanges(ranges: FRange[]): Promise<boolean>` | Aktualisieren Sie die von der Schutzregel abgedeckten Bereiche |
| `remove(): Promise<boolean>` | Entfernen Sie die Schutzregel |
| `setPoint(point: RangePermissionPoint, value: boolean): Promise<void>` | Einstellen der Status eines bestimmten Berechtigungspunkts für die Schutzregel (z. B. Bearbeiten/Anzeigen/Löschen) |
| `getPoint(point: RangePermissionPoint): boolean` | Rufen Sie den Status eines bestimmten Berechtigungspunkts für die Schutzregel ab (z. B. Bearbeiten/Anzeigen/Löschen) |
| `canEdit(): boolean` | Überprüfen Sie, ob die Schutzregel das Bearbeiten zulässt |
| `canView(): boolean` | Überprüfen Sie, ob die Schutzregel das Anzeigen zulässt |
| `canDelete(): boolean` | Überprüfen Sie, ob die Schutzregel das Löschen zulässt |
| `canManageCollaborator(): boolean` | Überprüfen Sie, ob die Schutzregel das Verwalten von Mitarbeitern zulässt |
| `getSnapshot(): RangePermissionSnapshot` | Erhalten Sie eine Momentaufnahme des aktuellen Berechtigungsstatus der Schutzregel, einschließlich aller Berechtigungspunkte |

```typescript
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getActiveSheet()
const worksheetPermission = fWorksheet.getWorksheetPermission()
const rules = await worksheetPermission.listRangeProtectionRules()
const rule = rules?.[0]

// Get the ID of the first protection rule
const ruleId = rule?.id
console.log(ruleId)

// Get the ranges of the first protection rule
const ranges = rule?.ranges
console.log(ranges)

// Update the ranges of the first protection rule to A1:C3
await rule?.updateRanges([fWorksheet.getRange('A1:C3')])

// Remove the first protection rule
await rule?.remove()
```

### Berechtigungsdialog ausblenden

```typescript
univerAPI.setPermissionDialogVisible(false)
```

### Migration von der alten API

<MigrationTable headers={['API Method Description', 'Migration Guide']}>
  <MigrationRow method="Get Permission Instance">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FWorkbook.getPermission()</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<div>Workbook: <code>FWorkbook.getWorkbookPermission()</code></div>
<div>Worksheet: <code>FWorksheet.getWorksheetPermission()</code></div>
<div>Range: <code>FRange.getRangePermission()</code></div>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Set Workbook Permission Point">
    <MigrationCell className="mb-2.5">
<strong>Old API:</strong>
      <br />
<code>FPermission.setWorkbookPermissionPoint(unitId: string, FPointClass: WorkbookPermissionPointConstructor, Wert: boolean)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorkbookPermission.setPoint(point: WorkbookPermissionPoint, value: boolean)</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Get Workbook Permission Point Status">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.checkWorkbookPermissionPoint(unitId: string, FPointClass: WorkbookPermissionPointConstructor): boolean</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neu API:</strong>
      <br />
<code>FWorkbookPermission.getPoint(point: WorkbookPermissionPoint): boolean</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Set Workbook Edit Permission">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.setWorkbookEditPermission(unitId: string, value: boolean)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorkbookPermission.setPoint(univerAPI.Enum.WorkbookPermissionPoint.Edit, value: boolean)</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Add Worksheet Protection">
    <MigrationCell className="mb-2.5">
<strong>Old API:</strong>
      <br />
<code>FPermission.addWorksheetBasePermission(unitId: string, subUnitId: string)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorksheetPermission.protect(options?: IWorksheetProtectionOptions)</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Remove Worksheet Protection">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.removeWorksheetPermission(unitId: string, subUnitId: string)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorksheetPermission.unprotect()</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Set Worksheet Permission Point">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.setWorksheetPermissionPoint(unitId: string, subUnitId: string, FPointClass: WorkSheetPermissionPointConstructor, value: boolean)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorksheetPermission.setPoint(point: WorksheetPermissionPoint, value: boolean)</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Get Worksheet Permission Point Status">
    <MigrationCell className="mb-2.5">
<strong>Old API:</strong>
      <br />
<code>FPermission.checkWorksheetPermissionPoint(unitId: string, subUnitId: string, FPointClass: WorkSheetPermissionPointConstructor): boolean</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorksheetPermission.getPoint(point: WorksheetPermissionPoint): boolean</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Get Permission Info of Specific Cell">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.getPermissionInfoWithCell(unitId: string, subUnitId: string, row: number, columns: number)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FWorksheetPermission.debugCellPermission(row: number, col: number): ICellPermissionDebugInfo</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Set Range Protection Permission">
    <MigrationCell className="mb-2.5">
<strong>Old API:</strong>
      <br />
<code>FPermission.addRangeBaseProtection(unitId: string, subUnitId: string, ranges: FRange[])</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<div>Arbeitsblatt: <code>FWorksheetPermission.protectRanges(configs)</code></div>
<div>Range: <code>FRangePermission.protect(options?: IRangeProtectionOptions)</code></div>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Remove Range Protection Permission">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.removeRangeProtection(unitId: string, subUnitId: string, RuleIds: string[])</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<div>Worksheet: <code>FWorksheetPermission.unprotectRules(ruleIds: string[])</code></div>
<div>Range: <code>FRangePermission.unprotect()</code></div>
<div>Bereichsschutzregel: <code>FRangeProtectionRule.remove()</code></div>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Set Range Protection Permission Point">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.setRangeProtectionPermissionPoint(unitId: string, subUnitId: string,missionId: string, FPointClass: RangePermissionPointConstructor, value: boolean)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FRangeProtectionRule.setPoint(point: RangePermissionPoint, value: boolean)</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Update Range Protection Permission Ranges">
    <MigrationCell className="mb-2.5">
<strong>Old API:</strong>
      <br />
<code>FPermission.setRangeProtectionRanges(unitId: string, subUnitId: string, RuleId: string, ranges: FRange[])</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>FRangeProtectionRule.updateRanges(ranges: FRange[])</code>
    </MigrationCell>
  </MigrationRow>
  <MigrationRow method="Hide Permission Dialog">
    <MigrationCell className="mb-2.5">
<strong>Alte API:</strong>
      <br />
<code>FPermission.setPermissionDialogVisible(false)</code>
    </MigrationCell>
    <MigrationCell>
<strong>Neue API:</strong>
      <br />
<code>univerAPI.setPermissionDialogVisible(false)</code>
    </MigrationCell>
  </MigrationRow>
</MigrationTable>
