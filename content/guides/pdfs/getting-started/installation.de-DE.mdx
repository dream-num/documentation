---
title: Installation und grundlegende Verwendung
icon: '#pro/Blocks'
---

Univer PDFs ist ein Produkt von Univer Pro und stellt derzeit kein Preset bereit. Erstellen Sie PDF-Anwendungen im Plugin-Modus.

## Browser-Editor installieren

<Tabs items={['npm', 'pnpm', 'yarn']}>
  <Tab>

    ```shell
    npm install @univerjs/core @univerjs/design @univerjs/ui @univerjs-pro/license @univerjs-pro/pdfs @univerjs-pro/pdfs-editor @univerjs-pro/pdfs-ui
    ```

  </Tab>
  <Tab>

    ```shell
    pnpm add @univerjs/core @univerjs/design @univerjs/ui @univerjs-pro/license @univerjs-pro/pdfs @univerjs-pro/pdfs-editor @univerjs-pro/pdfs-ui
    ```

  </Tab>
  <Tab>

    ```shell
    yarn add @univerjs/core @univerjs/design @univerjs/ui @univerjs-pro/license @univerjs-pro/pdfs @univerjs-pro/pdfs-editor @univerjs-pro/pdfs-ui react react-dom rxjs
    ```

  </Tab>
</Tabs>

Verwenden Sie für alle Pakete unter `@univerjs/*` und `@univerjs-pro/*` dieselbe Version.

## Plugins registrieren

<Steps>
  <Step>
    Importieren Sie die gemeinsamen UI-Stile vor dem PDF-UI-Stil und führen Sie die in Ihrer Anwendung aktivierten Sprachpakete zusammen:

    ```ts
    import { UniverLicensePlugin } from '@univerjs-pro/license'
    import { UniverPdfsPlugin } from '@univerjs-pro/pdfs'
    import { UniverPdfEditorPlugin } from '@univerjs-pro/pdfs-editor'
    import { UniverPdfsUIPlugin } from '@univerjs-pro/pdfs-ui'
    import PdfsUIEnUS from '@univerjs-pro/pdfs-ui/locale/en-US'
    import { LocaleType, mergeLocales, Univer } from '@univerjs/core'
    import { FUniver } from '@univerjs/core/facade'
    import DesignEnUS from '@univerjs/design/locale/en-US'
    import { UniverUIPlugin } from '@univerjs/ui'
    import UIEnUS from '@univerjs/ui/locale/en-US'

    import '@univerjs/design/lib/index.css'
    import '@univerjs/ui/lib/index.css'
    import '@univerjs-pro/pdfs-ui/lib/index.css'
    ```

  </Step>
  <Step>
    Importieren Sie den PDF-Facade-Einstieg. Dieser Side-Effect-Import fügt `FUniver` PDF-Methoden und -Enumerationen hinzu:

    ```ts
    import '@univerjs-pro/pdfs/facade'
    ```

  </Step>
  <Step>
    Erstellen Sie die Laufzeit, registrieren Sie den ausdrücklichen Plugin-Stack und erstellen Sie ein PDF:

    ```ts
    const univer = new Univer({
      locale: LocaleType.EN_US,
      locales: {
        [LocaleType.EN_US]: mergeLocales(
          DesignEnUS,
          UIEnUS,
          PdfsUIEnUS,
        ),
      },
    })

    univer.registerPlugin(UniverUIPlugin, { container: 'app' })
    univer.registerPlugin(UniverLicensePlugin, {
      license: process.env.CLIENT_LICENSE_TEXT,
    })
    univer.registerPlugin(UniverPdfsPlugin)
    univer.registerPlugin(UniverPdfEditorPlugin)
    univer.registerPlugin(UniverPdfsUIPlugin)

    const univerAPI = FUniver.newAPI(univer)
    univerAPI.createPdf({ name: 'Untitled PDF' })
    ```

  </Step>
</Steps>

## Optionaler Thumbnail-Worker

Seitenminiaturen können ohne zusätzliche Konfiguration im Hauptthread gerendert werden. Stellen Sie für größere Dokumente über `thumbnailWorkerURL` einen dedizierten Modul-Worker bereit.

Erstellen Sie einen Worker-Einstieg wie `pdf-thumbnail.worker.ts`:

```ts
import { startPdfPageThumbnailWorker } from '@univerjs-pro/pdfs-ui'

startPdfPageThumbnailWorker()
```

Übergeben Sie anschließend beim Registrieren des PDF-UI-Plugins dessen URL:

```ts
univer.registerPlugin(UniverPdfsUIPlugin, {
  thumbnailWorkerURL: new Worker(new URL('./pdf-thumbnail.worker.ts', import.meta.url), { type: 'module' }),
})
```

Die Option akzeptiert auch eine URL als Zeichenfolge oder einen vorhandenen `Worker`. Wenn Worker oder `OffscreenCanvas` nicht verfügbar sind, fällt das Rendern der Miniaturen auf den Hauptthread zurück.

## Verwendung mit Node.js

Node.js-Dienste können `UniverPdfsPlugin` und `UniverPdfEditorPlugin` ohne Browser-UI-Pakete registrieren. Fügen Sie den Collaboration-Client nur hinzu, wenn der Dienst dauerhafte PDF-Einheiten laden oder synchronisieren muss.
