---
title: Web Workers
icon: CircuitBoard
---

PDFs 支援在 Worker 中渲染頁面縮圖。向 `UniverPdfsUIPlugin` 傳入 `thumbnailWorkerURL`，Worker 使用 `OffscreenCanvas`。此設定僅適用於縮圖，不會將整個 PDF 編輯器移入 Worker。

先完成[安裝與基本使用](/guides/pdfs/getting-started/installation)。以下程式碼用來擴充既有設定。請在建立或載入內容之前註冊外掛；已有的外掛註冊應直接修改，不要重複註冊。

## 主執行緒

```typescript title="main.ts"
import { UniverPdfsUIPlugin } from '@univerjs-pro/pdfs-ui'

univer.registerPlugin(UniverPdfsUIPlugin, {
  thumbnailWorkerURL: new Worker(new URL('./worker.ts', import.meta.url), { type: 'module' }),
})
```

## Worker 入口

```typescript title="worker.ts"
import { startPdfPageThumbnailWorker } from '@univerjs-pro/pdfs-ui'

startPdfPageThumbnailWorker()
```

使用模組類型的 Worker 與獨立入口檔案。Worker 與主執行緒的套件版本須一致，渲染與使用者互動仍在主執行緒執行。
