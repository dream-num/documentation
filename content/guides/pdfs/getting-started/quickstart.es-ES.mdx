---
title: Inicio rápido
icon: '#pro/Rocket'
---

Univer PDFs está disponible actualmente en modo de plugins. Registre primero el entorno de interfaz compartido y la licencia Pro; después, añada los plugins del modelo PDF, el editor y la interfaz.

```ts
import { UniverLicensePlugin } from '@univerjs-pro/license'
import { UniverPdfsPlugin } from '@univerjs-pro/pdfs'
import { UniverPdfEditorPlugin } from '@univerjs-pro/pdfs-editor'
import { UniverPdfsUIPlugin } from '@univerjs-pro/pdfs-ui'
import PdfsUIEnUS from '@univerjs-pro/pdfs-ui/locale/en-US'
import { LocaleType, mergeLocales, Univer } from '@univerjs/core'
import { FUniver } from '@univerjs/core/facade'
import DesignEnUS from '@univerjs/design/locale/en-US'
import { UniverUIPlugin } from '@univerjs/ui'
import UIEnUS from '@univerjs/ui/locale/en-US'

import '@univerjs/design/lib/index.css'
import '@univerjs/ui/lib/index.css'
import '@univerjs-pro/pdfs-ui/lib/index.css'

import '@univerjs-pro/pdfs/facade'

const univer = new Univer({
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: mergeLocales(DesignEnUS, UIEnUS, PdfsUIEnUS),
  },
})

univer.registerPlugin(UniverUIPlugin, { container: 'app' })
univer.registerPlugin(UniverLicensePlugin, {
  license: process.env.CLIENT_LICENSE_TEXT,
})
univer.registerPlugin(UniverPdfsPlugin)
univer.registerPlugin(UniverPdfEditorPlugin)
univer.registerPlugin(UniverPdfsUIPlugin)

const univerAPI = FUniver.newAPI(univer)
const pdf = univerAPI.createPdf({ name: 'Project brief' })

console.log(pdf.getId(), pdf.getPages().length)
```

`createPdf` crea una unidad PDF y devuelve su Facade `FPdf`. Cuando no se proporciona ningún documento, la unidad comienza con una página A4 en blanco.

## Modo de visor

La edición de PDF está habilitada de forma predeterminada. Deshabilite los controladores del editor mientras mantiene el canvas PDF y la interfaz de navegación cuando solo necesite un visor:

```ts
univer.registerPlugin(UniverPdfsUIPlugin, {
  editor: {
    enabled: false,
  },
})
```

## Añadir funciones según sea necesario

- [Importación y exportación](/guides/pdfs/features/import-export) añade la conversión de PDF respaldada por servidor.
- [Colaboración](/guides/pdfs/features/collaboration) carga y sincroniza unidades PDF persistentes.
- [Impresión](/guides/pdfs/features/print) añade la impresión en el navegador para el PDF activo.
