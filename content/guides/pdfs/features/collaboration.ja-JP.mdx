---
title: 共同編集
icon: 'CloudCheck'
---


[共同編集の統合](/server/collaboration/browser-integration)に沿って、サンプルの実行、アプリへの組み込み、永続化と権限の検証を進めてください。実装と Web/Server サンプルは [Univer Office](/server/collaboration/quick-start) を参照してください。

認証済みの業務 API から Collaboration Service を呼び出して Unit を作成し、返された ID をブラウザーで読み込みます。[Quick Start](/server/collaboration/quick-start) で HTTP、Session Ticket、WebSocket を設定し、Database Adapter で永続化します。読み取り、JOIN、編集は[サーバー側の権限](/server/collaboration/identity-and-authorization)で個別に保護してください。

<Callout>
  使用前に、対象のバックエンドが PDF Unit をサポートしていることを確認してください。Sheets の共同編集サンプルだけでは PDF の対応を確認できません。
</Callout>

## 共同編集 PDF を読み込む

サーバー API または `importPdfToUnitIdAsync` を通じて PDF ユニットを作成した後、共同編集 Facade で読み込みます。

```ts
const pdf = await univerAPI.getCollaboration().loadPdfAsync(unitId)

if (!pdf) {
  throw new Error(`PDF unit not found: ${unitId}`)
}

console.log(pdf.getId(), pdf.getName())
```

`loadPdfAsync` は、サーバースナップショットを読み込み、共同編集セッションを準備して `FPdf` を返します。ユニットを PDF に関連付けられない場合は `null` を返します。
