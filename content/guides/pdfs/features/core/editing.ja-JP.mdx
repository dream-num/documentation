---
title: PDF の編集
description: ページ、テキスト、テーブル、画像、区切り線、注釈、ネイティブテキストに対応する PDF Facade ワークフローを選びます。
---

PDF 編集 API は Univer コマンドを通じて実行され、逆操作がある場合は元に戻す／やり直しの対象になります。変更する内容に合うトピックから始めてください。

## 編集トピック

- [ページと要素](/guides/pdfs/features/core/pages-and-elements)：ページの挿入、編集可能なオーバーレイの列挙、変換、重なり順、ロック、削除。
- [テキストとリスト](/guides/pdfs/features/core/text-and-lists)：テキストボックス、段落ブロック、セマンティックリストの編集。
- [テーブル](/guides/pdfs/features/core/tables)：グリッドの挿入とサイズ変更、セルの更新、テーマの適用。
- [画像と区切り線](/guides/pdfs/features/core/images-and-dividers)：管理対象画像とベクター区切り線の挿入。
- [注釈とネイティブテキスト](/guides/pdfs/features/core/annotations-and-native-text)：対応する注釈の追加とインポートテキストの昇格。

## 操作を組み合わせる

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf?.getPageByIndex(0)

if (!page) {
  throw new Error('No active PDF page')
}

const image = await page.insertImageAsync(
  'https://example.com/assets/signature.png',
  { left: 36, top: 36, width: 144, height: 72 }
)

image.setRotation(-4).bringToFront()
```

Facade の配置値は PDF ポイントです。URL 画像には絶対 `http` / `https` URL を使用してください。相対 URL パスは自動判定で受け付けられません。

ユニットの作成、取得、保存、ラッパーの全体像は [Univer PDFs API](/guides/pdfs/features/core/pdfs-api)を参照してください。
