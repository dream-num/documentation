---
title: 공동 편집
icon: 'CloudCheck'
---


[공동 편집 통합](/server/collaboration/browser-integration)에 따라 예제 실행, 앱 연결, 영속성과 권한 검증을 진행하세요. 구현 세부 정보와 Web/Server 예제는 [Univer Office](/server/collaboration/quick-start)를 참고하세요.

인증된 비즈니스 API에서 Collaboration Service로 Unit을 생성한 뒤 반환된 ID를 브라우저에서 로드하세요. [Quick Start](/server/collaboration/quick-start)에 따라 HTTP, Session Ticket, WebSocket을 설정하고 Database Adapter로 저장하세요. 읽기, JOIN, 수정은 [서버 권한](/server/collaboration/identity-and-authorization)으로 각각 보호해야 합니다.

<Callout>
  이 API를 사용하기 전에 대상 백엔드가 PDF Unit을 지원하는지 확인하세요. Sheets 협업 예제의 성공이 PDF 지원을 의미하지는 않습니다.
</Callout>

## 협업 PDF 불러오기

서버 API 또는 `importPdfToUnitIdAsync`를 통해 PDF 유닛을 만든 후 협업 퍼사드로 불러오세요.

```ts
const pdf = await univerAPI.getCollaboration().loadPdfAsync(unitId)

if (!pdf) {
  throw new Error(`PDF unit not found: ${unitId}`)
}

console.log(pdf.getId(), pdf.getName())
```

`loadPdfAsync`는 서버 스냅샷을 불러오고 협업 세션을 준비한 다음 `FPdf`를 반환합니다. 유닛을 PDF와 연결할 수 없으면 `null`을 반환합니다.
