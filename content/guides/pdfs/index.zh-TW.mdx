---
title: Univer PDFs 簡介
description: Univer PDFs 提供瀏覽器端 PDF 檢視器與編輯器，支援持久化文件資料、Facade API、匯入匯出、協作和列印。
icon: 'PdfMultiIcon'
---

## Univer PDFs 是什麼

Univer PDFs 是 Univer SDK Pro 中的 PDF 產品。它將 PDF 表示為正式的 `UNIVER_PDF` 單元，保留匯入的來源文件，並將已接受的編輯儲存在獨立的持久化編輯層中。瀏覽器 UI 會將這兩層呈現為一份連續文件。

當應用程式需要建立或開啟 PDF、加入可編輯內容、保持匯入 PDF 的保真度、協同修改、匯出結果，或將目前文件交給瀏覽器列印流程時，可以使用 Univer PDFs。

<Callout type="warning" title="僅支援外掛模式">
  PDF 目前沒有預設套件。請明確註冊 PDF 模型、UI 以及選用的功能外掛。
</Callout>


## 瀏覽器相容性

Univer SDK 的編譯目標是 Chrome 88，但這並不意味著 Univer SDK 能夠完全支援所有主流環境。我們會盡力確保 Univer SDK 在以下瀏覽器和 Node.js 環境中能夠正常運行：

| Edge | Firefox | Chrome | Safari | Electron |
| ---- | ------- | ------ | ------ | -------- |
| >=88 | >=90    | >=88   | >=14.1 | >=12     |

<Callout type="warning">
  Univer SDK 依賴 `Intl.Segmenter`，如果你的瀏覽器或執行環境暫不支援它，請先補上對應的 polyfill。我們推薦使用
  `@formatjs/intl-segmenter`。
</Callout>

如果遇到無法相容的情況，請先借助對應的 polyfill 來解決，如果仍然無法解決，請[提交 issue](https://github.com/dream-num/univer/issues/new/choose)。

## Node.js 相容性

<Callout type="warning">
  以下僅適用於在 Node.js 環境中運行 Univer SDK 的情況，並不適用於那些基於 Node.js 使用建構工具在瀏覽器中運行 Univer SDK 的情況。
</Callout>

Univer SDK 可以在 Node.js 環境中運行，支援的版本為 v18.17.0 及之後的版本。

## 主要功能

- **PDF Unit 模型**：在 `IPdfUnitData` 中儲存來源文件、頁面結構、資源和持久化編輯意圖。
- **頁面與元素操作**：支援頁面導覽和插入，以及可編輯的文字、清單、表格、影像、分隔線和註解。
- **瀏覽器工作台**：提供 PDF 頁面算繪、縮圖、選取、編輯控制項、剪貼簿行為和檢查器介面。
- **命令管線**：透過持久化命令和變更批次套用已接受的修改，使儲存、復原和重做共用同一模型。
- **Facade API**：透過 PDF、頁面、元素和原生文字物件建立、尋找、編輯和儲存 PDF Unit。

## 如何使用這些文件

在螢幕的左側是文件導航欄。文件頁面按從基礎到高階的順序組織，你可以按照這個順序循序漸進地瞭解 Univer SDK，或者跳到你感興趣的章節。你還可以在側邊欄的頂部切換 Univer PDFs 的文件。

螢幕的右側是當前頁面的大綱。如果你需要快速找到頁面，可以使用頂部的搜尋欄或搜尋快捷鍵（Ctrl+K 或 Cmd+K）。

我們推薦從 [快速開始](/guides/pdfs/getting-started/quickstart) 出發瞭解 Univer SDK。

## 前置知識

儘管我們已經盡力使得文件對新手也易於理解，但是我們必須假設你在開始閱讀之前已經瞭解了一些基礎知識，以便我們的文件可以專注於 Univer SDK。

為了充分利用我們的文件，建議你對 JavaScript 及其生態系統有基本的瞭解。

如果你有興趣在 Node.js 上執行 Univer SDK，你還應該對 Node.js 有基本的瞭解。

如果你想嘗試高階功能並部署 Univer Server，我們建議你對 Linux、Docker 和 Docker Compose（或 Kubernetes）有基本的瞭解。

## 擴充 Univer PDFs

在設計之初，Univer SDK 就被設計為高度可擴充的，因此你可以根據自己的需求來客製化它。

<Cards>
  <Card title="快速開始" href="/guides/pdfs/getting-started/quickstart" />
  <Card title="整合高階功能" href="/guides/pdfs/getting-started/pro" />
  <Card title="功能一覽" href="/guides/pdfs/features/core" />
  <Card title="API 參考" href="/reference/classes/univer" />
</Cards>
