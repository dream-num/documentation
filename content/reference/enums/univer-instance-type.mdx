---
title: UniverInstanceType
icon: '#ref/E'
---

Unit types used by `univer.createUnit()` and instance APIs. The Facade provides product-specific creation methods such as `createWorkbook()` and `createDocument()`.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Import

```ts
import { UniverInstanceType } from '@univerjs/core'
```

## Definition

```ts
enum UniverInstanceType {
  UNIVER_UNKNOWN = 0,
  UNIVER_DOC = 1,
  UNIVER_SHEET = 2,
  UNIVER_SLIDE = 3,
  UNIVER_PROJECT = 4,
  UNIVER_BASE = 5,
  UNIVER_BOARD = 6,
  UNIVER_PDF = 7,
  UNRECOGNIZED = -1,
}
```

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/protocol@1.0.0-rc.0/lib/types/ts/univer/constants/univer.d.ts)
