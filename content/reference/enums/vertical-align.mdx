---
title: VerticalAlign
icon: '#ref/E'
---

Vertical alignment for cell and text styles. Use these values with `IStyleData.vt`.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Import

```ts
import { VerticalAlign } from '@univerjs/core'
```

## Definition

```ts
enum VerticalAlign {
  UNSPECIFIED = 0,
  TOP = 1, // The text is explicitly aligned to the top of the cell.
  MIDDLE = 2, // The text is explicitly aligned to the middle of the cell.
  BOTTOM = 3,
}
```

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/types/enum/text-style.d.ts)
