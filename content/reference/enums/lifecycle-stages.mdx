---
title: LifecycleStages
icon: '#ref/E'
---

Global initialization stages reported by `univerAPI.getCurrentLifecycleStage()` and the [`LifeCycleChanged` event](/reference/facade/events#lifecyclechanged).

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Import

```ts
import { LifecycleStages } from '@univerjs/core'
```

## Definition

```ts
enum LifecycleStages {
  Starting = 0,

  Ready = 1,

  Rendered = 2,

  Steady = 3,
}
```

## Values

- `Starting` — Register plugins to Univer.
- `Ready` — Univer business instances (UniverDoc / UniverSheet / UniverSlide) are created and services or controllers provided by plugins get initialized. The application is ready to do the first-time rendering.
- `Rendered` — First-time rendering is completed.
- `Steady` — All lazy tasks are completed. The application is fully ready to provide features to users.

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/services/lifecycle/lifecycle.d.ts)
