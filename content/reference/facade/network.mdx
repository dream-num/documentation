---
title: FNetwork
icon: '#ref/F'
---

Make HTTP requests through `univerAPI.getNetwork()`. Register the Network plugin and import its Facade entry before using these methods. The response wraps the body, status, and headers; `getSSE` returns an observable stream[^observable-stream].

The following example assumes your application serves a JSON list at `/api/items`:

```ts
const network = univerAPI.getNetwork()
const response = await network.get<{ id: string }[]>('/api/items')
console.log(response.status, response.body)
```

[^observable-stream]: An RxJS Observable can emit multiple values over time, whereas a Promise resolves once. Subscribe to consume the stream and unsubscribe when it is no longer needed; SSE carries server-to-client events over HTTP.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FUniver.getNetwork()`](/reference/facade/univer#getnetwork)

## Setup

Register [`@univerjs/network`](/reference/packages/plugins/univerjs/network) or a preset that includes it. In plugin mode, import `@univerjs/network/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs/network` [#univerjsnetwork-members]

### `FNetwork.delete` [#delete]

Send DELETE request to the server.

```typescript
delete<T>(url: string, params?: IRequestParams): Promise<HTTPResponse<T>>
```

**Parameters**

- `url` — Required. The requested URL
- `params` — Optional. Request options, including query parameters, headers, and response type

**Returns**

A promise resolving to the HTTP response with a body of type `T`.

**Types:** [`HTTPResponse`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/response.d.ts) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IRequestParams`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/http.service.d.ts)

**Package:** [`@univerjs/network`](/reference/packages/plugins/univerjs/network) · [Type definitions](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/facade/f-network.d.ts)

### `FNetwork.get` [#get]

Send a GET request to the server.

```typescript
get<T>(url: string, params?: IRequestParams): Promise<HTTPResponse<T>>
```

**Parameters**

- `url` — Required. The requested URL.
- `params` — Optional. Request options, including query parameters, headers, and response type.

**Returns**

A promise resolving to the HTTP response with a body of type `T`.

**Types:** [`HTTPResponse`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/response.d.ts) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IRequestParams`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/http.service.d.ts)

**Package:** [`@univerjs/network`](/reference/packages/plugins/univerjs/network) · [Type definitions](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/facade/f-network.d.ts)

### `FNetwork.getSSE` [#getsse]

Request for a stream of server-sent events. Instead of a single response, the server sends a stream of responses,
Univer wraps the stream in an [`Observable`](https://rxjs.dev/guide/observable) which you can call `subscribe` on.

```typescript
getSSE<T>(method: HTTPRequestMethod, url: string, params?: IPostRequestParams): Observable<HTTPEvent<T>>
```

**Parameters**

- `method` — Required. HTTP request method
- `url` — Required. The requested URL
- `params` — Optional. params Request options, including body, query parameters, headers, and response type

**Returns**

An observable of HTTP events with response data of type `T`.

**Types:** [`HTTPEvent`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/response.d.ts) · [`Observable`](https://unpkg.com/rxjs@7.8.2/dist/types/internal/Observable.d.ts) · [`HTTPRequestMethod`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/request.d.ts) · [`IPostRequestParams`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/http.service.d.ts)

**Package:** [`@univerjs/network`](/reference/packages/plugins/univerjs/network) · [Type definitions](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/facade/f-network.d.ts)

### `FNetwork.patch` [#patch]

Send PATCH request to the server.

```typescript
patch<T>(url: string, params?: IPostRequestParams): Promise<HTTPResponse<T>>
```

**Parameters**

- `url` — Required. The requested URL
- `params` — Optional. Request options, including body, query parameters, headers, and response type

**Returns**

A promise resolving to the HTTP response with a body of type `T`.

**Types:** [`HTTPResponse`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/response.d.ts) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IPostRequestParams`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/http.service.d.ts)

**Package:** [`@univerjs/network`](/reference/packages/plugins/univerjs/network) · [Type definitions](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/facade/f-network.d.ts)

### `FNetwork.post` [#post]

Send a POST request to the server.

```typescript
post<T>(url: string, params?: IPostRequestParams): Promise<HTTPResponse<T>>
```

**Parameters**

- `url` — Required. The requested URL.
- `params` — Optional. Request options, including body, query parameters, headers, and response type.

**Returns**

A promise resolving to the HTTP response with a body of type `T`.

**Types:** [`HTTPResponse`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/response.d.ts) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IPostRequestParams`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/http.service.d.ts)

**Package:** [`@univerjs/network`](/reference/packages/plugins/univerjs/network) · [Type definitions](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/facade/f-network.d.ts)

### `FNetwork.put` [#put]

Send a PUT request to the server.

```typescript
put<T>(url: string, params?: IPostRequestParams): Promise<HTTPResponse<T>>
```

**Parameters**

- `url` — Required. The requested URL
- `params` — Optional. Request options, including body, query parameters, headers, and response type

**Returns**

A promise resolving to the HTTP response with a body of type `T`.

**Types:** [`HTTPResponse`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/response.d.ts) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IPostRequestParams`](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/services/http/http.service.d.ts)

**Package:** [`@univerjs/network`](/reference/packages/plugins/univerjs/network) · [Type definitions](https://unpkg.com/@univerjs/network@1.0.0-rc.0/lib/types/facade/f-network.d.ts)
