---
title: FPdfPage
icon: '#ref/F'
---

Facade for one materialized PDF page.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FPdf.insertPage()`](/reference/facade/pdf#insertpage)
- [`FPdf.getPages()`](/reference/facade/pdf#getpages)
- [`FPdf.getPageById()`](/reference/facade/pdf#getpagebyid)
- [`FPdf.getPageByIndex()`](/reference/facade/pdf#getpagebyindex)

## Example

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getElements())
```

## Setup

Register [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) or a preset that includes it. In plugin mode, import `@univerjs-pro/pdfs/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/pdfs/getting-started/facade).

## `@univerjs-pro/pdfs` [#univerjs-propdfs-members]

### `FPdfPage.getAnnotations` [#getannotations]

Return all editable annotations in z-order.

```typescript
getAnnotations(): FPdfAnnotation[]
```

**Returns**

The annotation Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getAnnotations())
```

**Types:** [`FPdfAnnotation`](/reference/facade/pdf-annotation)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getData` [#getdata]

Return a detached snapshot of the current materialized page.

```typescript
getData(): Readonly<IPdfPage>
```

**Returns**

The current page snapshot.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getData())
```

**Types:** [`IPdfPage`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/types.d.ts) · [`Readonly`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getDividers` [#getdividers]

Return all editable dividers in z-order.

```typescript
getDividers(): FPdfDivider[]
```

**Returns**

The divider Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getDividers())
```

**Types:** [`FPdfDivider`](/reference/facade/pdf-divider)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getElementById` [#getelementbyid]

Return one editable page element by ID.

```typescript
getElementById(id: string): FPdfPageElement | null
```

**Parameters**

- `id` — Required. The element ID.

**Returns**

The element Facade, or `null` when it does not exist.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const element = page.getElementById('element-id')
console.log(element)
```

**Types:** [`FPdfPageElement`](/reference/facade/pdf-page-element)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getElements` [#getelements]

Return all editable page elements in z-order.

```typescript
getElements(): FPdfPageElement[]
```

**Returns**

The editable element Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getElements())
```

**Types:** [`FPdfPageElement`](/reference/facade/pdf-page-element)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getId` [#getid]

Return the stable page ID.

```typescript
getId(): string
```

**Returns**

The page ID.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getId())
```

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getImages` [#getimages]

Return all editable managed images in z-order.

```typescript
getImages(): FPdfImage[]
```

**Returns**

The image Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getImages())
```

**Types:** [`FPdfImage`](/reference/facade/pdf-image)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getIndex` [#getindex]

Return the current zero-based page index.

```typescript
getIndex(): number
```

**Returns**

The current page index.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getIndex())
```

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getLists` [#getlists]

Return all semantic lists in z-order.

```typescript
getLists(): FPdfList[]
```

**Returns**

The list Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getLists())
```

**Types:** [`FPdfList`](/reference/facade/pdf-list)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getParagraphs` [#getparagraphs]

Return all story-backed paragraph frames in z-order.

```typescript
getParagraphs(): FPdfParagraph[]
```

**Returns**

The paragraph Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getParagraphs())
```

**Types:** [`FPdfParagraph`](/reference/facade/pdf-paragraph)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getTables` [#gettables]

Return all structured tables in z-order.

```typescript
getTables(): FPdfTable[]
```

**Returns**

The table Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getTables())
```

**Types:** [`FPdfTable`](/reference/facade/pdf-table)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getTextBoxes` [#gettextboxes]

Return all simple editable text boxes in z-order.

```typescript
getTextBoxes(): FPdfTextBox[]
```

**Returns**

The text-box Facades.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getTextBoxes())
```

**Types:** [`FPdfTextBox`](/reference/facade/pdf-text-box)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.getTextSpans` [#gettextspans]

Return snapshot Facades for every currently visible native text operation.

```typescript
getTextSpans(): FPdfTextSpan[]
```

**Returns**

The current native-text span snapshots.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
console.log(page.getTextSpans())
```

**Types:** [`FPdfTextSpan`](/reference/facade/pdf-text-span)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertAnnotation` [#insertannotation]

Insert one durable PDF annotation.

```typescript
insertAnnotation(options: IPdfAnnotationInsertOptions): FPdfAnnotation
```

**Parameters**

- `options` — Required. Annotation type, geometry, and placement.

**Returns**

The inserted annotation Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const annotation = page.insertAnnotation({
  annotationType: univerAPI.Enum.PdfAnnotationType.HIGHLIGHT,
  left: 36,
  top: 72,
  width: 180,
  height: 18,
})
```

**Types:** [`FPdfAnnotation`](/reference/facade/pdf-annotation) · [`IPdfAnnotationInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertDivider` [#insertdivider]

Insert one editable vector divider.

```typescript
insertDivider(options?: IPdfDividerInsertOptions): FPdfDivider
```

**Parameters**

- `options` — Optional. Default: `{}`. Placement and stroke options.

**Returns**

The inserted divider Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const divider = page.insertDivider({
  left: 36,
  top: 144,
  width: 200,
  strokeWidth: 1,
})
```

**Types:** [`FPdfDivider`](/reference/facade/pdf-divider) · [`IPdfDividerInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertImage` [#insertimage]

Insert an image from detached builder information.

```typescript
insertImage(info: IPdfImageBuilderInfo, index?: number): FPdfImage
```

**Parameters**

- `info` — Required. The image builder result.
- `index` — Optional. The optional z-order index.

**Returns**

The inserted image Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const imageInfo = page
  .newImage()
  .setSource('https://avatars.githubusercontent.com/u/61444807?s=48&v=4')
  .setSize(240, 135)
  .build()
page.insertImage(imageInfo)
```

**Types:** [`FPdfImage`](/reference/facade/pdf-image) · [`IPdfImageBuilderInfo`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertImageAsync` [#insertimageasync]

Insert an image directly from a string or Blob source.

```typescript
insertImageAsync(source: string | IFBlobSource, options?: IPdfImageInsertOptions): Promise<FPdfImage>
```

**Parameters**

- `source` — Required. A URL, UUID, base64 value, or Blob source.
- `options` — Optional. Default: `{}`. Optional placement, crop, opacity, and source type.

**Returns**

The inserted image Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const image = await page.insertImageAsync(
  'https://avatars.githubusercontent.com/u/61444807?s=48&v=4',
  {
    left: 36,
    top: 36,
    width: 240,
    height: 135,
  },
)
```

**Types:** [`FPdfImage`](/reference/facade/pdf-image) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IFBlobSource`](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts) · [`IPdfImageInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertList` [#insertlist]

Insert one story-backed semantic list.

```typescript
insertList(options: IPdfListInsertOptions): FPdfList
```

**Parameters**

- `options` — Required. List text, kind, preset, and placement.

**Returns**

The inserted list Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const list = page.insertList({
  text: 'First item',
  kind: univerAPI.Enum.PdfListKind.UNORDERED,
  preset: univerAPI.Enum.PdfListPresetId.UNORDERED_DISC,
})

list
  .insertItem(1, { text: '第二项' })
  .insertItem(2, { text: '子项', level: 1 })
  .insertItem(3, { text: '第四项' })
```

**Types:** [`FPdfList`](/reference/facade/pdf-list) · [`IPdfListInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertParagraph` [#insertparagraph]

Insert one story-backed paragraph frame.

```typescript
insertParagraph(options: IPdfParagraphInsertOptions): FPdfParagraph
```

**Parameters**

- `options` — Required. Paragraph text, placement, and initial style.

**Returns**

The inserted paragraph Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const paragraph = page.insertParagraph({
  text: 'First paragraph',
  fontSize: 24,
})
```

**Types:** [`FPdfParagraph`](/reference/facade/pdf-paragraph) · [`IPdfParagraphInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertTable` [#inserttable]

Insert one structured editable table.

```typescript
insertTable(options: IPdfTableInsertOptions): FPdfTable
```

**Parameters**

- `options` — Required. Grid dimensions, values, placement, and theme.

**Returns**

The inserted table Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const table = page.insertTable({
  rowCount: 2,
  columnCount: 3,
  cellTexts: ['A', 'B', 'C'],
})
```

**Types:** [`FPdfTable`](/reference/facade/pdf-table) · [`IPdfTableInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.insertTextBox` [#inserttextbox]

Insert a simple editable text box using PDF-point geometry.

```typescript
insertTextBox(options: IPdfTextBoxInsertOptions): FPdfTextBox
```

**Parameters**

- `options` — Required. Text, placement, and initial text style.

**Returns**

The inserted text-box Facade.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const textBox = page.insertTextBox({
  text: 'Hello PDF',
  left: 36,
  top: 36,
})
```

**Types:** [`FPdfTextBox`](/reference/facade/pdf-text-box) · [`IPdfTextBoxInsertOptions`](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/types.d.ts)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)

### `FPdfPage.newImage` [#newimage]

Return a detached image builder, optionally initialized from an image or ID.

```typescript
newImage(existing?: FPdfImage | string): FPdfImageBuilder
```

**Parameters**

- `existing` — Optional. An image Facade or explicit image ID to initialize.

**Returns**

A detached image builder.

**Examples**

```ts
const pdf = univerAPI.getActivePdf()
const page = pdf.getPageByIndex(0)
const imageInfo = page
  .newImage()
  .setSource('https://avatars.githubusercontent.com/u/61444807?s=48&v=4')
  .setSize(240, 135)
  .build()
page.insertImage(imageInfo)
```

**Types:** [`FPdfImageBuilder`](/reference/facade/pdf-image-builder) · [`FPdfImage`](/reference/facade/pdf-image)

**Package:** [`@univerjs-pro/pdfs`](/reference/packages/plugins/univerjs-pro/pdfs) · [Type definitions](https://unpkg.com/@univerjs-pro/pdfs@1.0.0-rc.0/lib/types/facade/f-pdf-page.d.ts)
