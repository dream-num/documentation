---
title: FBaseObjectPermission
icon: '#ref/F'
---

Command-backed Edit permission for one stable Base object.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FBaseDashboard.getPermission()`](/reference/facade/base-dashboard#getpermission)
- [`FBasePivotView.getPermission()`](/reference/facade/base-pivot-view#getpermission)
- [`FBaseTableRecord.getPermission()`](/reference/facade/base-table-record#getpermission)
- [`FBaseTableField.getPermission()`](/reference/facade/base-table-field#getpermission)
- [`FBaseTable.getPermission()`](/reference/facade/base-table#getpermission)
- [`FBaseTableView.getPermission()`](/reference/facade/base-table-view#getpermission)

## Setup

Register [`@univerjs-pro/bases`](/reference/packages/plugins/univerjs-pro/bases) or a preset that includes it. In plugin mode, import `@univerjs-pro/bases/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/bases/getting-started/facade).

## `@univerjs-pro/bases` [#univerjs-probases-members]

### `FBaseObjectPermission.canEdit` [#canedit]

Returns the effective Edit result after applying the Base unit, parent objects, and this object.

```typescript
canEdit(): boolean
```

**Returns**

Whether the object is currently editable.

**Package:** [`@univerjs-pro/bases`](/reference/packages/plugins/univerjs-pro/bases) · [Type definitions](https://unpkg.com/@univerjs-pro/bases@1.0.0-rc.0/lib/types/facade/f-base-permission.d.ts)

### `FBaseObjectPermission.setEditable` [#seteditable]

Enables or disables editing for this stable Base object.

This changes only the object's Edit point. `canEdit()` also applies the Base unit and parent
object ceilings, so enabling a child does not override a read-only Base or parent Table.

```typescript
setEditable(editable?: boolean): Promise<void>
```

**Parameters**

- `editable` — Optional. Default: `true`. Whether object editing is allowed. Defaults to true.

**Returns**

Resolves after the permission command finishes.

**Examples**

Restore editing for a table
```ts
const base = univerAPI.getActiveBase()
const table = base?.getTables()[0]
if (!table) throw new Error('Tasks table not found.')
await table.getPermission().setEditable()
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/bases`](/reference/packages/plugins/univerjs-pro/bases) · [Type definitions](https://unpkg.com/@univerjs-pro/bases@1.0.0-rc.0/lib/types/facade/f-base-permission.d.ts)

### `FBaseObjectPermission.setReadOnly` [#setreadonly]

Makes this stable Base object read-only.

```typescript
setReadOnly(): Promise<void>
```

**Returns**

Resolves after the permission command finishes.

**Examples**

```ts
const table = univerAPI.getActiveBase()?.getTables()[0]
if (!table) throw new Error('Table not found.')
await table.getPermission().setReadOnly()
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/bases`](/reference/packages/plugins/univerjs-pro/bases) · [Type definitions](https://unpkg.com/@univerjs-pro/bases@1.0.0-rc.0/lib/types/facade/f-base-permission.d.ts)
