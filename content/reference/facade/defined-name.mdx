---
title: FDefinedName
icon: '#ref/F'
---

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FWorkbook.getDefinedName()`](/reference/facade/workbook#getdefinedname)
- [`FWorkbook.getDefinedNames()`](/reference/facade/workbook#getdefinednames)
- [`FWorksheet.getDefinedNames()`](/reference/facade/worksheet#getdefinednames)

## Setup

Register [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) or a preset that includes it. In plugin mode, import `@univerjs/sheets/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs/sheets` [#univerjssheets-members]

### `FDefinedName.delete` [#delete]

Deletes the defined name.

```typescript
delete(): void
```

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.delete()
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.getComment` [#getcomment]

Gets the comment of the defined name.

```typescript
getComment(): string | undefined
```

**Returns**

The comment of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
console.log(definedName?.getComment())
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.getFormulaOrRefString` [#getformulaorrefstring]

Gets the formula or reference string of the defined name.

```typescript
getFormulaOrRefString(): string
```

**Returns**

The formula or reference string of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
console.log(definedName?.getFormulaOrRefString())
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.getLocalSheetId` [#getlocalsheetid]

Gets the local sheet id of the defined name.

```typescript
getLocalSheetId(): string | undefined
```

**Returns**

The local sheet id of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
console.log(definedName?.getLocalSheetId())
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.getName` [#getname]

Gets the name of the defined name.

```typescript
getName(): string
```

**Returns**

The name of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
console.log(definedName?.getName())
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.isWorkbookScope` [#isworkbookscope]

Checks if the defined name is in the workbook scope.

```typescript
isWorkbookScope(): boolean
```

**Returns**

True if the defined name is in the workbook scope, false otherwise.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
console.log(definedName?.isWorkbookScope())
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setComment` [#setcomment]

Sets the comment of the defined name.

```typescript
setComment(comment: string): void
```

**Parameters**

- `comment` — Required. The comment of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setComment('This is a comment')
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setFormula` [#setformula]

Sets the formula of the defined name.

```typescript
setFormula(formula: string): void
```

**Parameters**

- `formula` — Required. The formula of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setFormula('SUM(Sheet1!$A$1)')
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setHidden` [#sethidden]

Sets the hidden status of the defined name.

```typescript
setHidden(hidden: boolean): void
```

**Parameters**

- `hidden` — Required. The hidden status of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setHidden(true)
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setName` [#setname]

Sets the name of the defined name.

```typescript
setName(name: string): void
```

**Parameters**

- `name` — Required. The name of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setName('NewDefinedName')
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setRef` [#setref]

Sets the reference of the defined name.

```typescript
setRef(refString: string): void
```

**Parameters**

- `refString` — Required. The reference of the defined name.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setRef('Sheet1!$A$1')
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setRefByRange` [#setrefbyrange]

Sets the reference of the defined name by range.

```typescript
setRefByRange(row: number, column: number, numRows: number, numColumns: number): void
```

**Parameters**

- `row` — Required. The start row of the range.
- `column` — Required. The start column of the range.
- `numRows` — Required. The number of rows in the range.
- `numColumns` — Required. The number of columns in the range.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setRefByRange(1, 3, 2, 5) // D2:H3
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setScopeToWorkbook` [#setscopetoworkbook]

Sets the scope of the defined name to the workbook.

```typescript
setScopeToWorkbook(): void
```

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setScopeToWorkbook()
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.setScopeToWorksheet` [#setscopetoworksheet]

Sets the scope of the defined name to the worksheet.

```typescript
setScopeToWorksheet(worksheet: FWorksheet): void
```

**Parameters**

- `worksheet` — Required. The worksheet to set the scope to.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const sheets = fWorkbook.getSheets()

// Get the first defined name and make it available only in the second worksheet
const definedName = fWorkbook.getDefinedNames()[0]
definedName?.setScopeToWorksheet(sheets[1])
```

**Types:** [`FWorksheet`](/reference/facade/worksheet)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)

### `FDefinedName.toBuilder` [#tobuilder]

Converts the defined name to a defined name builder.

```typescript
toBuilder(): FDefinedNameBuilder
```

**Returns**

The defined name builder.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const definedName = fWorkbook.getDefinedNames()[0]
if (!definedName) throw new Error('definedName is not available')
const definedNameParam = definedName
  .toBuilder()
  .setName('NewDefinedName')
  .setFormula('SUM(Sheet1!$A$1)')
  .build()
fWorkbook.updateDefinedNameBuilder(definedNameParam)
```

**Types:** [`FDefinedNameBuilder`](/reference/facade/defined-name-builder)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/f-defined-name.d.ts)
