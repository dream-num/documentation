---
title: FSparkline
icon: '#ref/F'
---

The facade class for the sparkline.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FWorksheet.addSparkline()`](/reference/facade/worksheet#addsparkline)
- [`FWorksheet.getSparklineByCell()`](/reference/facade/worksheet#getsparklinebycell)

## Setup

Register [`@univerjs-pro/sheets-sparkline`](/reference/packages/plugins/univerjs-pro/sheets-sparkline) or a preset that includes it. In plugin mode, import `@univerjs-pro/sheets-sparkline/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs-pro/sheets-sparkline` [#univerjs-prosheets-sparkline-members]

### `FSparkline.changeDataSource` [#changedatasource]

```typescript
changeDataSource(sourceRange: IRange, targetRange: IRange): FSparkline | undefined
```

**Parameters**

- `sourceRange` — Required. Location of new data source
- `targetRange` — Required. New placement

**Returns**

Return this, for chaining

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')

// Create a sparkline in the range A10, with the data source in the range A1:A7.
const sparkline = fWorksheet.addSparkline(
  [fWorksheet.getRange('A1:A7').getRange()],
  [fWorksheet.getRange('A10').getRange()],
)

// Modify the data source of the sparkline to the range B1:B7, and move it to the range B10 after 3 seconds.
setTimeout(() => {
  sparkline.changeDataSource(
    fWorksheet.getRange('B1:B7').getRange(),
    fWorksheet.getRange('B10').getRange(),
  )
}, 3000)
```

**Types:** [`FSparkline`](/reference/facade/sparkline) · [`IRange`](/reference/types/range)

**Package:** [`@univerjs-pro/sheets-sparkline`](/reference/packages/plugins/univerjs-pro/sheets-sparkline) · [Type definitions](https://unpkg.com/@univerjs-pro/sheets-sparkline@1.0.0-rc.0/lib/types/facade/f-sparkline.d.ts)

### `FSparkline.removeSparkline` [#removesparkline]

```typescript
removeSparkline(): void
```

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')

// Create a sparkline in the range A10, with the data source in the range A1:A7.
const sparkline = fWorksheet.addSparkline(
  [fWorksheet.getRange('A1:A7').getRange()],
  [fWorksheet.getRange('A10').getRange()],
)

// Remove the sparkline after 3 seconds.
setTimeout(() => {
  sparkline.removeSparkline()
}, 3000)
```

**Package:** [`@univerjs-pro/sheets-sparkline`](/reference/packages/plugins/univerjs-pro/sheets-sparkline) · [Type definitions](https://unpkg.com/@univerjs-pro/sheets-sparkline@1.0.0-rc.0/lib/types/facade/f-sparkline.d.ts)
