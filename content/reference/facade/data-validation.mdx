---
title: FDataValidation
icon: '#ref/F'
---

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FDataValidationBuilder.build()`](/reference/facade/data-validation-builder#build)
- [`FRange.getDataValidation()`](/reference/facade/range#getdatavalidation)
- [`FRange.getDataValidations()`](/reference/facade/range#getdatavalidations)
- [`FWorksheet.getDataValidations()`](/reference/facade/worksheet#getdatavalidations)
- [`FWorksheet.getDataValidation()`](/reference/facade/worksheet#getdatavalidation)

## Setup

Register [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) or a preset that includes it. In plugin mode, import `@univerjs/sheets-data-validation/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs/sheets-data-validation` [#univerjssheets-data-validation-members]

### `FDataValidation.copy` [#copy]

Creates a new instance of FDataValidationBuilder using the current rule object

```typescript
copy(): FDataValidationBuilder
```

**Returns**

A new FDataValidationBuilder instance with the same rule configuration

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const fRange = fWorksheet.getRange('A1:B10')
const rule = univerAPI
  .newDataValidation()
  .requireNumberBetween(1, 10)
  .setOptions({
    allowBlank: true,
    showErrorMessage: true,
    error: 'Please enter a number between 1 and 10',
  })
  .build()
fRange.setDataValidation(rule)

const builder = fRange.getDataValidation().copy()
const newRule = builder
  .requireNumberBetween(1, 5)
  .setOptions({
    error: 'Please enter a number between 1 and 5',
  })
  .build()
fRange.setDataValidation(newRule)
```

**Types:** [`FDataValidationBuilder`](/reference/facade/data-validation-builder)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.delete` [#delete]

Delete the data validation rule from the worksheet

```typescript
delete(): boolean
```

**Returns**

true if the rule is deleted successfully, false otherwise

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')

// Create a new data validation rule that requires a number equal to 20 for the range A1:B10
const fRange = fWorksheet.getRange('A1:B10')
const rule = univerAPI.newDataValidation().requireNumberEqualTo(20).build()
fRange.setDataValidation(rule)

// Delete the data validation rule
fRange.getDataValidation().delete()
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getAllowInvalid` [#getallowinvalid]

Gets whether invalid data is allowed based on the error style value

```typescript
getAllowInvalid(): boolean
```

**Returns**

true if invalid data is allowed, false otherwise

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = fWorksheet.getDataValidations()
rules.forEach((rule) => {
  console.log(rule, rule.getAllowInvalid())
})
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getApplied` [#getapplied]

Gets whether the data validation rule is applied to the worksheet

```typescript
getApplied(): boolean
```

**Returns**

true if the rule is applied, false otherwise

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = fWorksheet.getDataValidations()
rules.forEach((rule) => {
  console.log(rule, rule.getApplied())
})

const fRange = fWorksheet.getRange('A1:B10')
console.log(fRange.getDataValidation()?.getApplied())
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getCriteriaType` [#getcriteriatype]

Gets the data validation type of the rule

```typescript
getCriteriaType(): DataValidationType | string
```

**Returns**

The data validation type

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = fWorksheet.getDataValidations()
rules.forEach((rule) => {
  console.log(rule, rule.getCriteriaType())
})
```

**Types:** [`DataValidationType`](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/types/enum/data-validation-type.d.ts)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getCriteriaValues` [#getcriteriavalues]

Gets the values used for criteria evaluation

```typescript
getCriteriaValues(): [string | undefined, string | undefined, string | undefined]
```

**Returns**

An array containing the operator, formula1, and formula2 values

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = fWorksheet.getDataValidations()
rules.forEach((rule) => {
  console.log(rule)
  const criteriaValues = rule.getCriteriaValues()
  const [operator, formula1, formula2] = criteriaValues
  console.log(operator, formula1, formula2)
})
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getHelpText` [#gethelptext]

Gets the help text information, which is used to provide users with guidance and support

```typescript
getHelpText(): string | undefined
```

**Returns**

Returns the help text information. If there is no error message, it returns an undefined value

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const fRange = fWorksheet.getRange('A1:B10')
const rule = univerAPI
  .newDataValidation()
  .requireNumberBetween(1, 10)
  .setOptions({
    allowBlank: true,
    showErrorMessage: true,
    error: 'Please enter a number between 1 and 10',
  })
  .build()
fRange.setDataValidation(rule)
console.log(fRange.getDataValidation().getHelpText()) // 'Please enter a number between 1 and 10'
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getRanges` [#getranges]

Gets the ranges to which the data validation rule is applied

```typescript
getRanges(): FRange[]
```

**Returns**

An array of FRange objects representing the ranges to which the data validation rule is applied

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = fWorksheet.getDataValidations()
rules.forEach((rule) => {
  console.log(rule)
  const ranges = rule.getRanges()
  ranges.forEach((range) => {
    console.log(range.getA1Notation())
  })
})
```

**Types:** [`FRange`](/reference/facade/range)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getSheetId` [#getsheetid]

Gets the sheet ID of the worksheet

```typescript
getSheetId(): string | undefined
```

**Returns**

The sheet ID of the worksheet

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const fRange = fWorksheet.getRange('A1:B10')
console.log(fRange.getDataValidation().getSheetId())
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.getUnitId` [#getunitid]

Gets the unit ID of the workbook

```typescript
getUnitId(): string | undefined
```

**Returns**

The unit ID of the workbook

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const fRange = fWorksheet.getRange('A1:B10')
console.log(fRange.getDataValidation().getUnitId())
```

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.rule` [#rule]

The underlying validation rule data. Use the Facade setters to update a rule attached to a worksheet.

```ts
rule: IDataValidationRule
```

**Types:** [`IDataValidationRule`](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/types/interfaces/i-data-validation.d.ts)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.setCriteria` [#setcriteria]

Set Criteria for the data validation rule

```typescript
setCriteria(type: DataValidationType, values: [DataValidationOperator, string, string], allowBlank?: boolean): FDataValidation
```

**Parameters**

- `type` — Required. The type of data validation criteria
- `values` — Required. An array containing the operator, formula1, and formula2 values
- `allowBlank` — Optional. Default: `true`. Whether to allow blank values

**Returns**

The current instance for method chaining

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')

// Create a new data validation rule that requires a number equal to 20 for the range A1:B10
const fRange = fWorksheet.getRange('A1:B10')
const rule = univerAPI.newDataValidation().requireNumberEqualTo(20).build()
fRange.setDataValidation(rule)

// Change the rule criteria to require a number between 1 and 10
fRange
  .getDataValidation()
  .setCriteria(univerAPI.Enum.DataValidationType.DECIMAL, [
    univerAPI.Enum.DataValidationOperator.BETWEEN,
    '1',
    '10',
  ])
```

**Types:** [`FDataValidation`](/reference/facade/data-validation) · [`DataValidationType`](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/types/enum/data-validation-type.d.ts) · [`DataValidationOperator`](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/types/enum/data-validation-operator.d.ts)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.setOptions` [#setoptions]

Set the options for the data validation rule

```typescript
setOptions(options: Partial<IDataValidationRuleOptions>): FDataValidation
```

**Parameters**

- `options` — Required. The options to set for the data validation rule

**Returns**

The current instance for method chaining

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')

// Create a new data validation rule that requires a number equal to 20 for the range A1:B10
const fRange = fWorksheet.getRange('A1:B10')
const rule = univerAPI.newDataValidation().requireNumberEqualTo(20).build()
fRange.setDataValidation(rule)

// Supplement the rule with additional options
fRange.getDataValidation().setOptions({
  allowBlank: true,
  showErrorMessage: true,
  error: 'Please enter a valid value',
})
```

**Types:** [`FDataValidation`](/reference/facade/data-validation) · [`Partial`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`IDataValidationRuleOptions`](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/types/interfaces/i-data-validation.d.ts)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)

### `FDataValidation.setRanges` [#setranges]

Set the ranges to the data validation rule

```typescript
setRanges(ranges: FRange[]): FDataValidation
```

**Parameters**

- `ranges` — Required. New ranges array

**Returns**

The current instance for method chaining

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')

// Create a new data validation rule that requires a number equal to 20 for the range A1:B10
const fRange = fWorksheet.getRange('A1:B10')
const rule = univerAPI.newDataValidation().requireNumberEqualTo(20).build()
fRange.setDataValidation(rule)

// Change the range to C1:D10
const newRuleRange = fWorksheet.getRange('C1:D10')
fRange.getDataValidation().setRanges([newRuleRange])
```

**Types:** [`FDataValidation`](/reference/facade/data-validation) · [`FRange`](/reference/facade/range)

**Package:** [`@univerjs/sheets-data-validation`](/reference/packages/plugins/univerjs/sheets-data-validation) · [Type definitions](https://unpkg.com/@univerjs/sheets-data-validation@1.0.0-rc.0/lib/types/facade/f-data-validation.d.ts)
