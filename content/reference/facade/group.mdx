---
title: FGroup
icon: '#ref/F'
---

The facade class for a slide group element.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FSlide.getGroups()`](/reference/facade/slide#getgroups)
- [`FSlide.group()`](/reference/facade/slide#group)

## Inheritance

Extends [`FPageElement`](/reference/facade/page-element). Its inherited members are available on this object.

## Setup

Register [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) or a preset that includes it. In plugin mode, import `@univerjs-pro/slides/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/slides/getting-started/facade).

## `@univerjs-pro/slides` [#univerjs-proslides-members]

### `FGroup.getChildren` [#getchildren]

Get the direct children of this group.

```typescript
getChildren(): FSlidePageElement[]
```

**Returns**

The direct child elements in group order.

**Examples**

```ts
const fPresentation = univerAPI.getActivePresentation()
const fSlide = fPresentation.getSlideByIndex(0)
const groups = fSlide.getGroups()
console.log(groups[0]?.getChildren())
```

**Types:** [`FSlidePageElement`](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-slide.d.ts)

**Package:** [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) · [Type definitions](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-group.d.ts)

### `FGroup.ungroup` [#ungroup]

Ungroup this group and return the released child element facades.

```typescript
ungroup(): FSlidePageElement[]
```

**Returns**

The direct child elements that were released.

**Examples**

```ts
const fPresentation = univerAPI.getActivePresentation()
const fSlide = fPresentation.getSlideByIndex(0)

const group = fSlide.getGroups()[0]
if (group) {
  group.ungroup()
}
```

**Types:** [`FSlidePageElement`](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-slide.d.ts)

**Package:** [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) · [Type definitions](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-group.d.ts)
