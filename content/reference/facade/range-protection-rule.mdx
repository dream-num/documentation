---
title: FRangeProtectionRule
icon: '#ref/F'
---

Implementation class for range protection rules
Encapsulates operations on a single protection rule

> This class should not be instantiated directly. Use factory methods on `univerAPI` instead.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FRangePermission.protect()`](/reference/facade/range-permission#protect)
- [`FRangePermission.listRules()`](/reference/facade/range-permission#listrules)
- [`FWorksheetPermission.protectRanges()`](/reference/facade/worksheet-permission#protectranges)
- [`FWorksheetPermission.listRangeProtectionRules()`](/reference/facade/worksheet-permission#listrangeprotectionrules)
- [`FWorksheetPermission.debugCellPermission()`](/reference/facade/worksheet-permission#debugcellpermission)

## Setup

Register [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) or a preset that includes it. In plugin mode, import `@univerjs/sheets/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs/sheets` [#univerjssheets-members]

### `FRangeProtectionRule.canDelete` [#candelete]

Check if the current user can delete this protection rule.

```typescript
canDelete(): boolean
```

**Returns**

true if can delete rule, false otherwise.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Check if the first rule allows deleting the rule
const rule = rules[0]
if (rule?.canDelete()) {
  console.log(
    `You can delete this protection rule for this range ${rule.ranges.map((r) => r.getA1Notation()).join(', ')}`,
  )
}
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.canEdit` [#canedit]

Check if the current user can edit this range.

```typescript
canEdit(): boolean
```

**Returns**

true if editable, false otherwise.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Check if the first rule allows editing
const rule = rules[0]
if (rule?.canEdit()) {
  console.log(`You can edit this range ${rule.ranges.map((r) => r.getA1Notation()).join(', ')}`)
}
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.canManageCollaborator` [#canmanagecollaborator]

Check if the current user can manage collaborators for this range.

```typescript
canManageCollaborator(): boolean
```

**Returns**

true if can manage collaborators, false otherwise.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Check if the first rule allows managing collaborators
const rule = rules[0]
if (rule?.canManageCollaborator()) {
  console.log(
    `You can manage collaborators for this range ${rule.ranges.map((r) => r.getA1Notation()).join(', ')}`,
  )
}
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.canView` [#canview]

Check if the current user can view this range.

```typescript
canView(): boolean
```

**Returns**

true if viewable, false otherwise.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Check if the first rule allows viewing
const rule = rules[0]
if (rule?.canView()) {
  console.log(`You can view this range ${rule.ranges.map((r) => r.getA1Notation()).join(', ')}`)
}
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.getPoint` [#getpoint]

Get the value of a specific permission point.

```typescript
getPoint(point: RangePermissionPoint): boolean
```

**Parameters**

- `point` — Required. The range permission point to query.

**Returns**

true if allowed, false if denied.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Check if the first rule allows editing
if (rules.length > 0) {
  const rule = rules[0]
  const canEdit = rule.getPoint(univerAPI.Enum.RangePermissionPoint.Edit)
  console.log(canEdit)
}
```

**Types:** [`RangePermissionPoint`](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/permission-types.d.ts)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.getSnapshot` [#getsnapshot]

Get the current permission snapshot.

```typescript
getSnapshot(): RangePermissionSnapshot
```

**Returns**

Snapshot of all permission points.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Get the permission snapshot of the first rule
if (rules.length > 0) {
  const rule = rules[0]
  const snapshot = rule.getSnapshot()
  console.log(snapshot)
}
```

**Types:** [`RangePermissionSnapshot`](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/permission-types.d.ts)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.id` [#id]

Get the rule ID.

```typescript
readonly id: string
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.options` [#options]

Get the protection options.

```typescript
readonly options: IRangeProtectionOptions
```

**Types:** [`IRangeProtectionOptions`](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/permission-types.d.ts)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.permissionId` [#permissionid]

Get the permission ID associated with this rule.

```typescript
readonly permissionId: string
```

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.ranges` [#ranges]

Get the protected ranges.

```typescript
readonly ranges: FRange[]
```

**Types:** [`FRange`](/reference/facade/range)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.remove` [#remove]

Delete the current protection rule.

```typescript
remove(): Promise<boolean>
```

**Returns**

A promise resolving to whether the rule was removed.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Remove the first protection rule
if (rules.length > 0) {
  const rule = rules[0]
  const result = await rule.remove()
  console.log(result)
}
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.setPoint` [#setpoint]

Set a specific permission point for the range rule (low-level API).

**Important:** This method only updates the permission point value for an existing protection rule.
It does NOT create permission checks that will block actual editing operations.
You must call `protect()` first to create a protection rule before using this method.

This method is useful for:
- Fine-tuning permissions after creating a protection rule with `protect()`
- Dynamically adjusting permissions based on runtime conditions
- Advanced permission management scenarios

```typescript
setPoint(point: RangePermissionPoint, value: boolean): Promise<void>
```

**Parameters**

- `point` — Required. The permission point to set.
- `value` — Required. The value to set (true = allowed, false = denied).

**Returns**

A promise that resolves when the point is set.

**Throws**

If no protection rule exists for this range.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const fRange = fWorksheet.getRange('A1:B2')
// First, create a protection rule
const rule = await fRange.getRangePermission().protect({ name: 'My Range', allowEdit: true })
// Then you can dynamically update permission points
await rule.setPoint(univerAPI.Enum.RangePermissionPoint.Edit, false) // Now disable edit
await rule.setPoint(univerAPI.Enum.RangePermissionPoint.View, true) // Ensure view is enabled
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`RangePermissionPoint`](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/permission-types.d.ts)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)

### `FRangeProtectionRule.updateRanges` [#updateranges]

Update the protected ranges.

```typescript
updateRanges(ranges: FRange[]): Promise<boolean>
```

**Parameters**

- `ranges` — Required. New ranges to protect.

**Returns**

A promise resolving to whether the protected ranges were updated.

**Examples**

```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
if (!fWorksheet) throw new Error('fWorksheet is not available')
const rules = await fWorksheet.getWorksheetPermission().listRangeProtectionRules()
// Update the ranges to A1:C3 for the first rule
if (rules.length > 0) {
  const rule = rules[0]
  const result = await rule.updateRanges([fWorksheet.getRange('A1:C3')])
  console.log(result)
}
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`FRange`](/reference/facade/range)

**Package:** [`@univerjs/sheets`](/reference/packages/plugins/univerjs/sheets) · [Type definitions](https://unpkg.com/@univerjs/sheets@1.0.0-rc.0/lib/types/facade/permission/f-range-protection-rule.d.ts)
