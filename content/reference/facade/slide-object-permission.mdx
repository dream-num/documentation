---
title: FSlideObjectPermission
icon: '#ref/F'
---

Command-backed Edit permission for one stable Slide, Element, or Master object.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FPageElement.getPermission()`](/reference/facade/page-element#getpermission)
- [`FSlide.getPermission()`](/reference/facade/slide#getpermission)
- [`FSlide.getElementPermission()`](/reference/facade/slide#getelementpermission)
- [`FPresentation.getMasterViewPermission()`](/reference/facade/presentation#getmasterviewpermission)
- [`FPresentation.getMasterElementPermission()`](/reference/facade/presentation#getmasterelementpermission)

## Setup

Register [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) or a preset that includes it. In plugin mode, import `@univerjs-pro/slides/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/slides/getting-started/facade).

## `@univerjs-pro/slides` [#univerjs-proslides-members]

### `FSlideObjectPermission.canEdit` [#canedit]

Returns the effective Edit result after applying the Presentation, parent, and object permissions.

```typescript
canEdit(): boolean
```

**Returns**

Whether the object is currently editable.

**Package:** [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) · [Type definitions](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-slide-permission.d.ts)

### `FSlideObjectPermission.setEditable` [#seteditable]

Enables or disables editing for this Slide, Element, or Master object.

`canEdit()` also applies the Presentation and parent object ceilings.

```typescript
setEditable(editable?: boolean): Promise<void>
```

**Parameters**

- `editable` — Optional. Default: `true`. Whether object editing is allowed. Defaults to true.

**Returns**

Resolves after the permission command finishes.

**Examples**

```ts
const presentation = univerAPI.getActivePresentation()
const slide = presentation?.getSlideByIndex(0)
if (!slide) throw new Error('Slide not found.')
await slide.getPermission().setEditable()
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) · [Type definitions](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-slide-permission.d.ts)

### `FSlideObjectPermission.setReadOnly` [#setreadonly]

Makes this Slide, Element, or Master object read-only.

```typescript
setReadOnly(): Promise<void>
```

**Returns**

Resolves after the permission command finishes.

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/slides`](/reference/packages/plugins/univerjs-pro/slides) · [Type definitions](https://unpkg.com/@univerjs-pro/slides@1.0.0-rc.0/lib/types/facade/f-slide-permission.d.ts)
