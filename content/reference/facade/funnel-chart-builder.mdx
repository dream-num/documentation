---
title: FFunnelChartBuilder
icon: '#ref/F'
---

Fluent, type-specific Builder for Funnel Charts.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Inheritance

Extends [`FChartBuilderBase`](/reference/facade/chart-builder-base). Its inherited members are available on this object.

## Example

Sheet
```ts
const fWorkbook = univerAPI.getActiveWorkbook()
const fWorksheet = fWorkbook.getSheetByName('Sheet1')
const chartInfo = fWorksheet
  .newChart(univerAPI.Enum.ChartTypeString.Funnel)
  .setSource('A1:B8')
  .setGap(4)
  .setUseAbsoluteValue(true)
  .build()
await fWorksheet.insertChart(chartInfo)
```

Doc
```ts
const fDocument = univerAPI.getActiveDocument()
const chartInfo = fDocument
  .newChart(univerAPI.Enum.ChartTypeString.Funnel)
  .setSource([
    ['Stage', 'Users'],
    ['Visited', 1000],
    ['Signed up', 420],
    ['Purchased', 160],
  ])
  .setGap(4)
  .setUseAbsoluteValue(true)
  .build()
await fDocument.insertChart(chartInfo)
```

Slide
```ts
const fPresentation = univerAPI.getActivePresentation()
const fSlide = fPresentation.getSlideByIndex(0)
const chartInfo = fSlide
  .newChart(univerAPI.Enum.ChartTypeString.Funnel)
  .setSource([
    ['Stage', 'Users'],
    ['Visited', 1000],
    ['Signed up', 420],
    ['Purchased', 160],
  ])
  .setGap(4)
  .setUseAbsoluteValue(true)
  .build()
await fSlide.insertChart(chartInfo)
```

Board
```ts
const fBoard = univerAPI.getActiveBoard()
const chartInfo = fBoard
  .newChart(univerAPI.Enum.ChartTypeString.Funnel)
  .setSource([
    ['Stage', 'Users'],
    ['Visited', 1000],
    ['Signed up', 420],
    ['Purchased', 160],
  ])
  .setGap(4)
  .setUseAbsoluteValue(true)
  .build()
await fBoard.insertChart(chartInfo)
```

## Setup

Register [`@univerjs-pro/engine-chart`](/reference/packages/plugins/univerjs-pro/engine-chart) or a preset that includes it. In plugin mode, import `@univerjs-pro/engine-chart/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs-pro/engine-chart` [#univerjs-proengine-chart-members]

### `FFunnelChartBuilder.resetGap` [#resetgap]

Restores the gap to 2 px and returns this builder for chaining.

This method executes synchronously.

```typescript
resetGap(): this
```

**Returns**

This builder for chaining.

**Examples**

```ts
builder.resetGap()
```

**Package:** [`@univerjs-pro/engine-chart`](/reference/packages/plugins/univerjs-pro/engine-chart) · [Type definitions](https://unpkg.com/@univerjs-pro/engine-chart@1.0.0-rc.0/lib/types/facade/builders/f-funnel-chart-builder.d.ts)

### `FFunnelChartBuilder.resetUseAbsoluteValue` [#resetuseabsolutevalue]

Restores the default of preserving signed values and returns this builder for chaining.

This method executes synchronously.

```typescript
resetUseAbsoluteValue(): this
```

**Returns**

This builder for chaining.

**Examples**

```ts
builder.resetUseAbsoluteValue()
```

**Package:** [`@univerjs-pro/engine-chart`](/reference/packages/plugins/univerjs-pro/engine-chart) · [Type definitions](https://unpkg.com/@univerjs-pro/engine-chart@1.0.0-rc.0/lib/types/facade/builders/f-funnel-chart-builder.d.ts)

### `FFunnelChartBuilder.setGap` [#setgap]

Sets the gap configuration and returns this builder for chaining.

This method executes synchronously.

```typescript
setGap(value: number): this
```

**Parameters**

- `value` — Required. The semantic value to record.

**Returns**

This builder for chaining.

**Examples**

```ts
builder.setGap(8)
```

**Package:** [`@univerjs-pro/engine-chart`](/reference/packages/plugins/univerjs-pro/engine-chart) · [Type definitions](https://unpkg.com/@univerjs-pro/engine-chart@1.0.0-rc.0/lib/types/facade/builders/f-funnel-chart-builder.d.ts)

### `FFunnelChartBuilder.setUseAbsoluteValue` [#setuseabsolutevalue]

Sets the use absolute value configuration and returns this builder for chaining.

This method executes synchronously.

```typescript
setUseAbsoluteValue(value: boolean): this
```

**Parameters**

- `value` — Required. The semantic value to record.

**Returns**

This builder for chaining.

**Examples**

```ts
builder.setUseAbsoluteValue(true)
```

**Package:** [`@univerjs-pro/engine-chart`](/reference/packages/plugins/univerjs-pro/engine-chart) · [Type definitions](https://unpkg.com/@univerjs-pro/engine-chart@1.0.0-rc.0/lib/types/facade/builders/f-funnel-chart-builder.d.ts)
