---
title: FBlob
icon: '#ref/F'
---

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FUniver.newBlob()`](/reference/facade/univer#newblob)

## Setup

Register [`@univerjs/core`](/reference/packages/plugins/univerjs/core) or a preset that includes it. In plugin mode, import `@univerjs/core/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/sheets/getting-started/facade).

## `@univerjs/core` [#univerjscore-members]

### `FBlob.copyBlob` [#copyblob]

Returns a copy of this blob.

```typescript
copyBlob(): FBlob
```

**Returns**

a new blob by copying the current blob

**Examples**

```ts
const blob = univerAPI.newBlob()
const newBlob = blob.copyBlob()
console.log(newBlob)
```

**Types:** [`FBlob`](/reference/facade/blob)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.getAs` [#getas]

Returns a copy labeled with the specified MIME type. The data bytes are not converted or re-encoded.

```typescript
getAs(contentType: string): FBlob
```

**Parameters**

- `contentType` — Required. the content type refer to https://developer.mozilla.org/en-US/docs/Web/HTTP/MIME_types/Common_types

**Returns**

A new facade containing the same data with the specified MIME type.

**Examples**

```ts
const blob = univerAPI.newBlob()
const newBlob = blob.getAs('text/plain')
console.log(newBlob)
```

**Types:** [`FBlob`](/reference/facade/blob)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.getBytes` [#getbytes]

Gets the data stored in this blob.

```typescript
getBytes(): Promise<Uint8Array>
```

**Returns**

A promise resolving to the bytes; rejects if no blob data is set.

**Examples**

```ts
const blob = univerAPI.newBlob().setDataFromString('Hello, World!')
const bytes = await blob.getBytes()
console.log(bytes)
```

**Types:** [`Uint8Array`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts) · [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.getContentType` [#getcontenttype]

Gets the content type of the data stored in this blob.

```typescript
getContentType(): string | undefined
```

**Returns**

The MIME type, or `undefined` if no blob data is set.

**Examples**

```ts
const blob = univerAPI.newBlob()
const contentType = blob.getContentType()
console.log(contentType)
```

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.getDataAsString` [#getdataasstring]

Get the blob as a string.

```typescript
getDataAsString(): Promise<string>
getDataAsString(charset?: string): Promise<string>
```

**Parameters**

- `charset` — Optional. TextDecoder encoding label. Defaults to UTF-8.

**Returns**

A promise resolving to decoded text; an unset blob returns an empty string.

**Examples**

```ts
const blob = univerAPI.newBlob()
const data = await blob.getDataAsString()
console.log(data)
```

```ts
const blob = univerAPI.newBlob()
const data = await blob.getDataAsString('iso-8859-1')
console.log(data)
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.setBytes` [#setbytes]

Sets the data stored in this blob.

```typescript
setBytes(bytes: Uint8Array): FBlob
```

**Parameters**

- `bytes` — Required. a byte array

**Returns**

the blob object

**Examples**

```ts
const blob = univerAPI.newBlob()
const bytes = new Uint8Array(10)
blob.setBytes(bytes)
```

**Types:** [`FBlob`](/reference/facade/blob) · [`Uint8Array`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.setContentType` [#setcontenttype]

Sets the MIME type without converting or re-encoding the data bytes. Has no effect when no blob data is set.

```typescript
setContentType(contentType: string): FBlob
```

**Parameters**

- `contentType` — Required. the content type refer to https://developer.mozilla.org/en-US/docs/Web/HTTP/MIME_types/Common_types

**Returns**

the blob object

**Examples**

```ts
const blob = univerAPI.newBlob()
blob.setContentType('text/plain')
```

**Types:** [`FBlob`](/reference/facade/blob)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)

### `FBlob.setDataFromString` [#setdatafromstring]

Sets the data stored in this blob.

```typescript
setDataFromString(data: string): FBlob
setDataFromString(data: string, contentType?: string): FBlob
```

**Parameters**

- `data` — Required. blob data string
- `contentType` — Optional. the content type refer to https://developer.mozilla.org/en-US/docs/Web/HTTP/MIME_types/Common_types

**Returns**

the blob object

**Examples**

```ts
const blob = univerAPI.newBlob()
blob.setDataFromString('Hello, World!')
```

```ts
const blob = univerAPI.newBlob()
blob.setDataFromString('Hello, World!', 'text/plain')
```

**Types:** [`FBlob`](/reference/facade/blob)

**Package:** [`@univerjs/core`](/reference/packages/plugins/univerjs/core) · [Type definitions](https://unpkg.com/@univerjs/core@1.0.0-rc.0/lib/types/facade/f-blob.d.ts)
