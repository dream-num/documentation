---
title: FBoardElementPermission
icon: '#ref/F'
---

Command-backed Edit permission for one stable Board element.

{/* API details are generated by scripts/sync-api-reference.mjs. */}

## Access

Access through:

- [`FBoard.getElementPermission()`](/reference/facade/board#getelementpermission)

## Setup

Register [`@univerjs-pro/boards`](/reference/packages/plugins/univerjs-pro/boards) or a preset that includes it. In plugin mode, import `@univerjs-pro/boards/facade`. Additional methods below require their listed plugin packages. See [Facade setup](/guides/boards/getting-started/facade).

## `@univerjs-pro/boards` [#univerjs-proboards-members]

### `FBoardElementPermission.canEdit` [#canedit]

Returns the effective Edit result after applying the Board unit and element permissions.

```typescript
canEdit(): boolean
```

**Returns**

Whether the element is currently editable.

**Package:** [`@univerjs-pro/boards`](/reference/packages/plugins/univerjs-pro/boards) · [Type definitions](https://unpkg.com/@univerjs-pro/boards@1.0.0-rc.0/lib/types/facade/f-board-permission.d.ts)

### `FBoardElementPermission.setEditable` [#seteditable]

Enables or disables editing for one stable Board element.

```typescript
setEditable(editable?: boolean): Promise<void>
```

**Parameters**

- `editable` — Optional. Default: `true`. Whether element editing is allowed. Defaults to true.

**Returns**

Resolves after the permission command finishes.

**Examples**

```ts
const board = univerAPI.getActiveBoard()
if (!board) throw new Error('No active Board.')
const element = Object.values(board.getElements())[0]
if (!element) throw new Error('Board element not found.')
await board.getElementPermission(element.id).setEditable()
```

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/boards`](/reference/packages/plugins/univerjs-pro/boards) · [Type definitions](https://unpkg.com/@univerjs-pro/boards@1.0.0-rc.0/lib/types/facade/f-board-permission.d.ts)

### `FBoardElementPermission.setReadOnly` [#setreadonly]

Makes this Board element read-only.

```typescript
setReadOnly(): Promise<void>
```

**Returns**

Resolves after the permission command finishes.

**Types:** [`Promise`](https://unpkg.com/@typescript/typescript-darwin-arm64@7.0.2/lib/lib.es5.d.ts)

**Package:** [`@univerjs-pro/boards`](/reference/packages/plugins/univerjs-pro/boards) · [Type definitions](https://unpkg.com/@univerjs-pro/boards@1.0.0-rc.0/lib/types/facade/f-board-permission.d.ts)
