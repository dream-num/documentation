---
title: API overview
description: Find APIs, configure packages, and start using Univer Office SDK.
---

Use the Facade API to read and update Office content in your application. Start with a product below, or look up initialization and plugin configuration further down.

## Try the API [#quick-start]

First complete [installation and basic usage](/guides/sheets/getting-started/installation). With the Sheets editor initialized and `univerAPI` returned by `createUniver`, this example writes to cell A1:

```ts
const workbook = univerAPI.getActiveWorkbook()
const worksheet = workbook?.getActiveSheet()
worksheet?.getRange('A1').setValue('Hello, Univer!')
```

`univerAPI` is an `FUniver` instance. In Sheets, navigate from the workbook to a worksheet and then a range. See [Facade API basics](/guides/sheets/getting-started/facade) for Plugin Mode initialization and feature-specific Facade imports.

## Find an API [#api-index]

| Task | Reference |
| --- | --- |
| Instances, lifecycle, and shared operations | [FUniver](/reference/facade/univer) |
| Workbooks, worksheets, and cells | [FWorkbook](/reference/facade/workbook) · [FWorksheet](/reference/facade/worksheet) · [FRange](/reference/facade/range) |
| Documents and text | [FDocument](/reference/facade/document) |
| Presentations | [Slides](/reference/facade/slides) |
| Whiteboards | [Boards](/reference/facade/boards) |
| Databases | [Bases](/reference/facade/bases) |
| PDF content | [PDF](/reference/facade/pdf) |
| Events | [Events](/reference/facade/events) |
| Collaboration | [Collaboration](/reference/facade/collaboration) |
| Comments | [Comments](/reference/facade/thread-comment) |

## Initialization and configuration [#configuration]

Use [createUniver](/reference/methods/create-univer) with presets, or [Univer](/reference/classes/univer) when registering plugins directly. The `Univer` instance manages plugins and units; `univerAPI` exposes application-facing content operations.

Configure [Sheets Core](/reference/packages/presets/univerjs/preset-sheets-core) or [Docs Core](/reference/packages/presets/univerjs/preset-docs-core) to start. Browse **Packages → Presets / Plugins** in the sidebar for individual package options. Feature APIs require the corresponding plugins to be registered; in Plugin Mode, also import the package’s Facade entry when the feature guide specifies one.

## Server and AI integration [#server-and-ai]

For backend collaboration and file conversion, start with [Server SDK](/server). For CLI workflows and AI Agent content operations, start with [AI SDK](/ai).
