---
title: '@univerjs/watermark'
icon: '#ref/P'
---

```typescript
// [!code word:UniverWatermarkPlugin]
// [!code word:IUniverWatermarkConfig]
import { UniverWatermarkPlugin } from '@univerjs/watermark'

univer.registerPlugin(UniverWatermarkPlugin, IUniverWatermarkConfig)
```

## IUniverWatermarkConfig

<AutoTypeTable
  name="IUniverWatermarkConfig"
  type={`
    export { IUniverWatermarkConfig } from '@univerjs/watermark'
  `}
/>

### IUserInfoWatermarkConfig

<AutoTypeTable
  name="IUserInfoWatermarkConfig"
  type={`
    import { IUniverWatermarkConfig } from '@univerjs/watermark'
    type ExtractPartial<T> = T extends Partial<infer U> ? U : never;
    export type IUserInfoWatermarkConfig = ExtractPartial<IUniverWatermarkConfig['imageWatermarkSettings']>
  `}
/>

### ITextWatermarkConfig

<AutoTypeTable
  name="ITextWatermarkConfig"
  type={`
    import { IUniverWatermarkConfig } from '@univerjs/watermark'
    type ExtractPartial<T> = T extends Partial<infer U> ? U : never;
    export type ITextWatermarkConfig = ExtractPartial<IUniverWatermarkConfig['textWatermarkSettings']>
  `}
/>

### IUserInfoWatermarkConfig

<AutoTypeTable
  name="IUserInfoWatermarkConfig"
  type={`
    import { IUniverWatermarkConfig } from '@univerjs/watermark'
    type ExtractPartial<T> = T extends Partial<infer U> ? U : never;
    export type IUserInfoWatermarkConfig = ExtractPartial<IUniverWatermarkConfig['userWatermarkSettings']>
  `}
/>
