---
title: '@univerjs/preset-docs-collaboration'
icon: '#ref/P'
---

```typescript
import { UniverDocsCollaborationPreset } from '@univerjs/preset-docs-collaboration'

createUniver({
  presets: [
    UniverDocsCollaborationPreset({
      universerEndpoint: 'https://your-backend.example.com',
      univerContainerId: 'app',
    }),
  ],
})
```

The preset registers the collaboration model, browser client, client UI, and Docs history UI in that order. It therefore adds realtime editing, collaboration UI, and document version history as one composition.

`universerEndpoint` points to your application backend. Compose the required [Univer Server SDK modules](/server) and expose their protocol routes there; the preset only registers browser plugins. Configure the History Service, Endpoint, storage, and permissions separately to enable [version history](/server/collaboration/extensions#web-history).

## Configuration

```typescript
interface IUniverDocsCollaborationPresetConfig {
  universerEndpoint?: string
  univerContainerId?: string
}
```
