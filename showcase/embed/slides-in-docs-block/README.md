# Lighthouse / Strategy announcement

Current language contract: native UI, startup alerts and authored data stay English under either host language. The legacy third locale argument remains accepted but is ignored. All complete English plugin packs, official CSS, native Grid menus and independent host/child models are retained. Earlier bilingual evidence below is historical; this language migration does not resolve its recorded SDK limitations or certify every interaction.

An original neighborhood repair-studio announcement embeds a native three-slide
strategy deck: a deep-ocean cover, warm-white evidence cards and sea-green learning
timeline. It proposes six weeks, 120 seats and two local communication channels;
these are fictional planning assumptions, not measured outcomes or a live campaign.

The saved Feishu marketing-plan reference informs the theme/evidence/action
sequence only. No competitor artwork is redistributed. Native Docs Grid chrome
and eight official SDK CSS imports remain intact. Preview and standalone use
the same factory and authored data, without generic fixture panels or duplicate
host buttons. The deck is a native child, not an iframe or screenshot.

## Selected evidence

The first test wrote through Facade while a native shape text editor was open;
the updated path exits that editor and selects the child canvas first. The failed
run is retained; concurrent editor/Facade writes are not certified. Screenshot
inspection also found card descriptions overflowing. Shorter descriptions,
smaller card-label fonts and a raised footer correct the authored layout without
changing native CSS. Three different slide layouts and palettes are retained.

## Still open

The actual Preview passes selected EN/ZH theme acceptance in
`embed-slide-docblock-theme-rendered/report.json`: native child activation,
rich-text and host paragraph edits, identical full host/child saves across
dark/light StorageEvents, the same owner and clean React unmount. The test waits
for SDK `isDarkMode()` and screenshots show the edited slide. The active native
text editor is exited before the Facade edit; arbitrary edit conflicts remain open.

The announcement and slide deck remain independent: this is not Formula Shape.
Complete native typing/commit-conflict tests, all menu/dialog/plugin dependencies,
broader history/focus/scrolling, touch/accessibility, narrow layouts, early or
pending removal, failed sources, repeated mounting/early theme transitions and reload
preservation remain open. Warm guide integration is not cold-start performance
acceptance. Native license watermarks remain visible and unmodified. No backend,
campaign execution, Exchange/Print conversion or persistence is claimed. Reload
loses edits. This case remains partial, not full SDK certification.
