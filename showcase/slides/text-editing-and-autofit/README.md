# Saffron / Text editing and autofit

## Current native-only revision

The editor opens directly on three authored pages: coral/cream emphasis, mint native lists and lilac paragraph alignment. All 17 host actions and their fixture panel have been removed, including collapsed controls and duplicated Undo/Redo. Use native thumbnails and edit actual canvas text. The Grid ribbon and native Print plugin remain visible. Six official SDK CSS imports travel with the exported factory; host CSS only sizes its root.

The same factory and data power Preview and the independent export. Source-only data variants remain available for developer diagnostics. The old actions module is retained locally for SDK regression reproduction, but is no longer shipped as unused runnable demo code. Autofit renderer failures are not fixed or hidden by this UI revision. Print settings registration is not output-fidelity acceptance; no server-backed Exchange menu is added to this frontend-only case.

## Historical evidence — preceding implementations

All reports and button descriptions below predate the native-only revision. They must not be used as acceptance evidence for the current UI or export.

Registered route: `slides/text-editing-and-autofit`. Blueprint `standalone-slides-007` remains partial; registration and passing scoped tests are not full visual acceptance. The documentation Preview and exported entry both import the same `create-demo.ts` and shared actions.

For the exported project, install with `pnpm install`, run `pnpm dev`, or build with `pnpm build`. Node must satisfy the generated package's engine requirement. No backend is needed. The five official SDK CSS imports in `src/create-demo.ts` are required; do not remove them or replace them with host styling. Dependencies are pinned by the source exporter. Trial-license watermarks remain visible.

`code/data.ts` creates three independently authored fictional print-studio pages: a welcome card, workshop menu, and invitation. Layouts and text differ. Variants cover the original briefing, long text in small boxes, empty text in existing boxes, and zero pages. Snapshots are independently allocated; pages explicitly use white backgrounds. No competitor media or participant data is redistributed.

`code/actions.ts` holds real Facade operations intended to be shared by the preview and standalone export. Text, font size, padding, wrapping and autofit inputs are validated before mutation. Rich-text examples demonstrate an emphasized span, differently aligned paragraphs, and native list metadata—not manually typed bullet characters. Autofit returns an explicit warning that stored configuration does not prove visual fitting.

Run `pnpm test:showcase:slides-text-sdk`. The passing `test-results/slides-text-shared-actions-style-contract/report.json` contains fifteen check groups: basic text and rich-text ownership, option persistence, complete serialized Shape Undo/Redo, shared actions, twelve invalid inputs preserving the whole Shape, and detached/distinct/empty fixtures. It asserts the emphasized run's exact color/font size/bold state, both paragraph alignments, and all three native list IDs. These are model checks, not canvas acceptance.

The separate `pnpm test:showcase:slides-text-render` still uses the existing Theme and Background editor to reproduce the autofit discrepancy. See [SDK observations](../../capabilities/sdk-issues.md) for its ordinary-shape/text-box and wrapping matrix. Those screenshots are not previews of the Saffron fixture.

`code/create-demo.ts` initializes the real SDK with Design, UI, Docs UI, Shape Editor UI and Slides UI CSS imports. `code/styles.css` styles only the host controls. Both `ribbonType: 'grid'` and `header: true` are necessary here: the initial `header: false` screenshot hid the native ribbon despite its Grid configuration. The revised screenshot shows the native Start/View tabs and grouped large toolbar icons. No CSS overrides paint over the SDK internals.

Run `pnpm test:showcase:slides-text-live`. The passing `test-results/slides-text-live-css/report.json` verifies initialization, visible native ribbon controls, official SDK white token and flex layout, native Canvas text changes, exact Shape Undo/Redo, three rich-text controls, invalid font-size preservation, a visible autofit limitation, zero-page rejection and Reset. The initial failed Reset assertion compared the old unit identity to the deliberately fresh one; the corrected assertion allows only the new `unitId`, retaining complete equality for the rest of the Shape. The first failure is preserved, not counted as an SDK bug. The browser runner builds only this demo and closes its server/browser after testing.

The actual exporter generated ten files in `<USERPROFILE>/AppData/Local/Temp/univer-saffron-8d1hQJ` with exact source parity. A fresh offline pnpm 12.1.0 install added 167 packages. Vite 8.2.2 production build passes: main JS `index-2JX-yJSX.js` is 14,684.09 kB (3,500.50 kB gzip), and CSS `index-CcdtJoIC.css` is 114.35 kB (17.09 kB gzip), with additional lazy language chunks. The large-chunk warning remains. `test-results/slides-text-export-production/report.json` passes the same selected browser checks against the built standalone project with no browser errors; its entry handles disposal through `pagehide`.

Expanded browser reports `test-results/slides-text-live-expanded/report.json` and `test-results/slides-text-export-expanded/report.json` both pass nine groups without browser errors. The same shared-factory and standalone-production test now exercises all seventeen host actions: edits on each of the three pages with actual Canvas text and opposite-page preservation; full snapshot reload with only the fresh root ID allowed to change and empty Undo history; padding/Undo and invalid padding; wrapping options; long copy; blank/overflow/zero-page fixtures; Inspect and Fit; sequential Tab access to host controls at 760/390/320px without outer-page horizontal overflow. The 320px screenshot still shows a dense native ribbon with commands extending beyond the visible width. These host-keyboard checks do not certify native ribbon reachability, screen-reader support, or mobile usability. Padding/wrapping option readback is not complete pixel-level layout verification.

`test-results/slides-text-next-guides/report.json` passes ten groups through the scoped Next development server: the expanded interaction suite plus served EN/ZH headings, 4 variants / 17 actions / 5 states, and real iframe text writes with native ribbon and official CSS. Webpack reported only `slides/text-editing-and-autofit` and zero document MDX modules. Two earlier harness failures are retained: an unscoped alert selector also matched Next's route announcer; a visibility wait targeted readback inside closed Details. The final harness scopes demo alerts and waits for matching attached readback, without changing app behavior to satisfy those tests.

Visual-first revision: the initial slides now contain native rich-text emphasis, a native three-item list, and left/center/right paragraphs. Only Show slide, Reset and Fit slide are visible by default; the fourteen editing actions remain in an optional Details panel. The shared Playground stays at its existing 640px height. `test-results/slides-text-visual-fixture-sdk/report.json` checks deterministic rich-text fixture IDs and initial formatting as well as the previous action contracts. `test-results/slides-text-visual-first/report.json` exercises the collapsed default, all editing actions after expansion, official white/flex CSS and keyboard access. These selected standalone checks do not yet certify readability in the documentation's 640px iframe. Older export results above predate this revision and must be regenerated before claiming current export acceptance.

The visual-first export has now been regenerated through the actual source exporter (ten files). Its selected Vite production build produces `index-CIvU-B1V.js` (14,684.98 kB, 3,500.77 kB gzip) and `index-D6pM2x7a.css` (114.59 kB, 17.13 kB gzip). `test-results/slides-text-visual-export/report.json` passes all ten current browser check groups against that build, with no browser errors and the official white/flex styles present. The large-chunk warning remains. The earlier export reports are historical, not evidence for the revised fixture.

Documentation layout follow-up: the collapsed controls alone still left only 170px of main canvas in the standard 640px preview (`slides-text-visual-next`, failed). This case now declares `previewHeight: 960`; the shared default stays 640px for other cases. `test-results/slides-text-visual-next-reset-same-layout/report.json` passes eleven groups, including both EN/ZH guide embeds with 471px canvas height (490px on the independent Playground). The editor screenshots show the complete slide, native ribbon, thumbnails and speaker notes. No SDK-internal CSS was overridden. Full snapshot Reset passes with only the new root identity allowed, using the same collapsed host controls as startup. The earlier Reset failure compared different viewport states: Fit legitimately changed the saved zoom ratio when optional controls were expanded.

`test-results/slides-text-visual-ownership` passes the existing selected Preview ownership harness: three theme changes detach old canvases, and all seven created SDK owners are disposed. This does not cover injected startup or cleanup failures.

Fault-injection follow-up: `test-results/slides-text-ownership-fault-pending/report.json` passes seven paths: lifecycle registration failure, presentation creation failure, post-observe failure, listener cleanup failure, combined creation/cleanup failure, disposal before readiness, and disposal after Reset has actually begun unloading. All fifteen SDK owners are released; no tracked event/host ResizeObserver handle or host node remains. The first run (`slides-text-ownership-fault-first`) caught a ResizeObserver cleanup omission after a thrown `observe`; registering its cleanup before calling `observe` fixes that path. Combined failures preserve both causes, and repeated disposal is harmless. This tests these specific paths, not every possible SDK constructor/plugin failure.

The cleanup fix is also included in the regenerated standalone export. Its current main chunk is `index-DCXeiFL8.js`; the CSS chunk remains `index-D6pM2x7a.css`. `test-results/slides-text-export-cleanup-fixed/report.json` passes all ten production-browser groups, including full snapshot Reset, with no browser errors.

Remaining verification: pixel-level rich-text/padding/wrapping layout; native editing, ribbon accessibility and touch usability; images/groups/connectors/overlap from the original blueprint; bundle performance. Do not simulate autofit in host code or mark the known failed render checks as passing.
