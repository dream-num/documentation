# Harbor modern quotes — native acceptance

Selected route: `docs-modern/quote-blocks`. Installed dependencies: exact `1.0.0-beta.2`; existing Vite `8.2.2`. **26 / 30 strict gates PASS; 23 / 23 literal README examples executed.** Partial acceptance, not a 100% SDK claim. Script exits 1 for four retained failures.

## Scope and changes

All 21 original Harbor Night Routes paragraphs are retained. Five original community/research paragraphs add meaningful two-/three-paragraph business quotes: four quotes, 26 paragraphs and six sections total. Original VOICE/CONTEXT/ATTR/COMPARE, overlapping bold+italic `visible route`, bold `clear directions`, two route bullets, two acceptance tasks, caution callout, two-line TypeScript block and hyperlink remain.

The host quote scope/style/color forms, edit/attribution/history/fixture actions, explanatory/status/readback panels and their handlers/internal history/scroll/zoom adapters were removed. Native Grid and real quote blocks remain. All nine variants, 15 actions and seven states have EN/ZH metadata; 23 literal README examples preserve the original Facade capability scope. No external mutation/debug panel is added.

Preview/export share a factory with six official CSS files and six complete EN/ZH locale packs. Initial Chinese UI, edited-data same-owner light/dark switching, and same controller double disposal pass. The React Preview effect separation is source-reviewed/typechecked; no independent Next mount test is claimed.

## Passing runtime evidence

- Native Grid insertion of a new CONTEXT quote and complete Undo/Redo model equality.
- Native keyboard append within VOICE, actual painted glyphs, and complete Undo/Redo model equality. At this width `native` is visibly hyphenated `na-` / `tive`; test checks exact model prose plus painted `reviewed`, without changing SDK text or normalizing model fields.
- Three literal quote scopes with stable block ID and duplicate guard; original prose and overlapping inline marks survive unwrap/rewrap. Sentinel removal during unwrap is intentional; full snapshot history/recovery checks are not normalized.
- Four semantic styles, independent line/text colors, effective glyph colors, real native rule draw calls, canvas pixel changes and unchanged comparison quote.
- Attribution append/duplicate guard, including the already-attributed three-paragraph scope. Missing ID and invalid color/channel reject without model mutation.
- Left-line-only and delete Undo/Redo restore complete models. Edited same-ID recreation and empty-to-exact-baseline restore preserve all data and produce the native canvas again.
- Zero browser runtime/console errors and zero backend requests. Official white native workbench and trial watermark remain untouched.
- Settled initial native footer **257 words**; opening screenshot waits for actual nonzero word-count UI rather than a fixed timer.

## Four strict failures / remaining work

1. Combined-style Undo returns false. Full before/failed snapshots are retained; no retry/reset masks the failure.
2. Text-only Undo likewise returns false.
3. Facade paragraph append Undo splits the final VOICE text-color run at offset 348. Restored prose is insufficient: complete snapshot equality fails.
4. Native quote floating toolbar does not open when clicking the visible VOICE region at the tested viewport. The recorded real quote hit region covers that point, but no toolbar appears. Its color picker and history are therefore not accepted. The root cause of this installed-build pointer path is not isolated; the successful Facade styles and native Grid insertion do not prove the floating menu works.

Redo after failed Undo is unaccepted on that path. The previous custom narrow-screen fit-width/scroll adapter used internal SDK services and is removed; automatic narrow viewport fit is not claimed, with native zoom controls retained. Every neighboring block's editing workflow and all responsive/browser configurations were not separately accepted. No SDK/node_modules patch, new IDs to bypass lifecycle issues, text-run coalescing or snapshot-field substitution.

## Commands and artifacts

- `node scripts/test-modern-quotes-native.mjs test-results/modern-quotes-export/manifest.json`, with `SHOWCASE_DEMO_URL=http://127.0.0.1:4414` set in the appropriate shell. Default URL matches localhost:3030 `dev:showcase`; `SHOWCASE_BASE_URL` and `SHOWCASE_RESULTS_DIR` supported.
- Normal source manifest: `test-results/modern-quotes-export/manifest.json`; nine exported source files verified byte-for-byte. Temp project: `C:/Users/wbfsa/AppData/Local/Temp/univer-harbor-quotes-lcfT3g`.
- 14 independent exact-version package junctions; no installation or whole-node_modules junction. Selected production build PASS (1794 modules), case TypeScript check PASS, Oxfmt/Oxlint PASS. Only this case built.
- Machine results: `report.json`; strict post-failure models `*-snapshot.json`; relevant baseline models `literal-*-before.json`.
- Cover candidate: `opening-settled.png`. Actually viewed: opening-settled, native-input, native-grid-quote, community-research, neighboring-blocks and attributed-scope. Original code, tasks, caution and hyperlink are visibly intact.
- The owned Vite preview on 4414 was stopped; a TCP check confirmed no listener. Shared package/ledger/public covers and SDK/dependencies were not modified; no commit.
