# Beacon / native Code Blocks acceptance

Scope: `showcase/docs-modern/code-blocks`, installed Univer 1.0.0-beta.2, Chromium on Windows. This is selected exported-demo runtime evidence, not checkout-only inference. No backend or conversion endpoint was used; no SDK/dependency patches, shared metadata ledger changes, or commits were made.

## Content and implementation

The original six-section Community Lighting Digest remains, including its TypeScript ingestion, independent SQL average query, two review bullets, two acceptance tasks, caution, quotation, fictional attribution and review link. The original alternate JSON manifest, Python filter and SQL daily summary now appear as separate real native blocks within that same story, without a sample loader. There are 48 paragraphs and five code blocks; settled native footer: **229 words**. TypeScript retains eight source paragraphs, including a blank line, tabs, a long line and a Unicode dash.

The native Grid and native code-language picker replace all host language/layout/tab/edit/history/fixture/readback panels. Shared Preview/export factory imports six official stylesheets and six complete EN/ZH pack pairs. Initial page locale, same-owner theme changes, same-ID reconstruction, fixed separate empty document, exact captured baseline restoration and idempotent disposal are implemented without model repair. Metadata retains 10 variants, 16 actions and 7 states in both languages. The README contains 32 literal executable examples; all 32 are exercised by the dedicated test.

## Strict result

Authoritative machine result: `report.json`. Latest completed run: **32 of 42 gates pass, 10 fail; process exits 1**. These are evidence gates, not a percentage of product feature completion.

Passing evidence includes:

- Nine normal exported source files exactly match `readShowcaseSources()`; selected production build passes (1,794 modules), case TypeScript/oxlint/oxfmt checks pass.
- Original source content of all four business samples and separate comparison SQL is exact, including tabs/blank lines. The white official Grid and native code glyphs are present.
- Six language configuration/source-isolation variants and explicit TypeScript → plaintext glyph-color/pixel changes pass. Native hover language picker opens and changes actual syntax drawing.
- Native Grid Insert → Code creates a sixth actual code block with its native rendered rectangle. Ordinary native keyboard input draws the new comment glyphs and passes complete unmodified Undo/Redo snapshot equality.
- Facade comment append and blank-line insertion pass complete Undo/Redo equality; unwrap/rewrap and duplicate guard preserve source.
- Public saved-model source and clipboard paths preserve whitespace, including exact edited source. On Windows only the expected clipboard transport string is constructed with CRLF; returned clipboard values and complete models are never normalized. `getText()` remains separately tested and failing.
- Edited same-ID reconstruction, empty-document exact baseline restore, complete locale packs, same-owner edited themes, initial Chinese, double disposal of the same controller, and zero runtime errors/backend requests pass.

Strict failures retained:

| Gate | Actual evidence |
| --- | --- |
| Wrap rendering | `wrap=false` leaves the long source paragraph on three soft-wrapped layout lines instead of one. |
| Line-number rendering | Config changes, but no line-number gutter space is reserved; first code glyph position remains unchanged. |
| Tab-width rendering | `tabSize=1` → `8` does not move the first glyph after the original tab. |
| Facade language history | Undo leaves `plaintext` in the code resource and instead changes the earlier acceptance checklist/paragraphs. Full model differs; no history stack clearing hides this. |
| Native language history | Actual picker works, but its Undo has the same unrelated checklist/resource difference. |
| Delete history | Undo restores document text/range without restoring `beacon-ingest` in `DOC_CODE_PLUGIN`. |
| Native Grid insertion history | Undo removes the block but leaves its generated entry in `DOC_CODE_PLUGIN`. The resource is not deleted or normalized by the test. |
| `getText()` whitespace | Paragraph separators and the internal blank line are missing. |
| Unmodified Facade clipboard | Copying `getText()` faithfully copies the same missing-newline text. |
| Slash inside code | Real `/` keyboard input is intercepted by the native slash menu instead of inserting the literal character. Ordinary prose typing is a distinct passing gate. |

Automatic narrow-viewport fit from the old internal adapter was removed and is not claimed. Native zoom is left intact. Not every native language option is exhaustively exercised; six original language variants are covered, and TS/plaintext drawing plus original four-language business blocks are checked. Trial watermark/license UI remains untouched. Host property panels were removed, not replaced with a simulated renderer.

## Commands and artifacts

Working directory: `<USERPROFILE>/Documents/GitHub/office_document/documentation`.

```powershell
$env:SHOWCASE_DEMO_URL='http://127.0.0.1:4414'
node scripts/test-modern-code-native.mjs test-results/modern-code-export/manifest.json
```

Without environment overrides the script uses `http://localhost:3030/en-US/playground/docs-modern/code-blocks`, matching `dev:showcase`. `SHOWCASE_BASE_URL` and `SHOWCASE_RESULTS_DIR` are also supported.

Selected export manifest: `test-results/modern-code-export/manifest.json`.
Selected directory: `<USERPROFILE>/AppData/Local/Temp/univer-beacon-code-avQuL2`.
Its 14 individual package junctions are 13 exact beta.2 SDK packages and existing Vite 8.2.2. No installation or whole-node_modules link was used. Build command in that selected directory:

```powershell
node <USERPROFILE>/AppData/Local/Temp/univer-aster-formula-SHm1UE/node_modules/vite/bin/vite.js build
```

Actually viewed images: `opening-settled.png` (229 words, proposed cover), `comparison-json.png`, `python-sql.png`, `native-language-picker.png`, `native-language-plaintext.png`, `native-grid-code.png`, `native-input.png` (230 words after typing). Runtime interactions may be captured while native word-count debounce is pending; only the settled opening is proposed as cover. Strict failing gates write their complete actual snapshot plus failure image; `report.json` identifies the current failures, while earlier probe artifacts may remain in this isolated directory.

The owned preview service (exec session 22032) was stopped with Ctrl+C after the final test; `Get-NetTCPConnection -LocalPort 4414 -State Listen` returned no listener. No other ports or processes were touched.
